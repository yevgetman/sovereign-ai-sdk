node_modules/
dist/
.DS_Store
*.log

# Bun
.bun/

# Env
.env
.env.local
.env.*.local

# IDE
.vscode/settings.json
.idea/
.claude/

# Brainstorming visual companion (transient mockup state)
.superpowers/

# Runtime outputs (if we ever write them here, belongs in a bundle's state/ instead)
*.jsonl.tmp
.harness/settings.local.json

# Default bundle's state/ is runtime-generated; keep only the placeholder.
bundle-default/state/*
!bundle-default/state/.gitkeep

# Built TUI binary (built by scripts/build-tui.ts on postinstall)
bin/sov-tui

# Phase 21 — release artifacts produced by scripts/release.ts
/build/
