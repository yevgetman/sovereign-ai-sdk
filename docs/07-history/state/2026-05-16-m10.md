# State of the build — Phase 16.1 M10 close-out (parity audit shipped; M11 blocked-pending #40)

**HEAD:** to be set on the close-out commit (this snapshot + ADRs M10-01..04 + backlog #40 + CLAUDE.md/AGENTS.md pointer). The chain since the M9.6 close-out (`47d35cb`) is: M10 spec/plan (`def43f9`) → M9.5 theme regression fix (`1f05ec6`) → HarnessInfo wire (`53fda9e`) → resume-repair wire (`a892f71`) → audit report + this close-out (next commits).
**Suite:** TS — **2003 pass / 0 fail / 5142 expect()** (1997 baseline + 2 schema regression tests + 2 HarnessInfo parity tests + 2 ResumeRepair parity tests; the 6 real-Anthropic smokes are gated by `SOV_M10_REAL_SMOKE=1` and skip by default). Go — `internal/render`, `internal/components`, `internal/theme` packages re-verified green.
**Lint + typecheck:** clean. Same 2 pre-existing `noNonNullAssertion` warnings in `src/permissions/shellSemantics.ts` (unrelated, untouched).
**Sov binary:** at the M10 close-out HEAD after the final `sov upgrade`.
**Manual smoke:** 4-prompt real-Anthropic Haiku 4.5 smoke against server-mode runtime, all pass. Transcripts at `docs/07-history/state/2026-05-16-tui-parity-audit-soak/`. Cost ~$0.05. M9.6's deferred real-Anthropic visual smoke is absorbed here.

This is a session close-out snapshot. The next session boots from CLAUDE.md and should read this file first. Supersedes `docs/07-history/state/2026-05-16.md` (the prior M9.6 close-out snapshot preserved verbatim in archive).

## Where we are

**Phase 16.1 M10 is shipped — the parity audit is complete and signed off.** The audit, conducted independently per Postmortem Rule 3 by 4 parallel Opus subagents reading the 92-import surface of `src/ui/terminalRepl.ts` literally (not by recall), found:

- **71 of 92 imports WIRED** in the server-mode runtime (the `--ui tui` boot path)
- **4 HIGH gaps** of which:
  - 2 fixed inline in M10 (cheap HIGH fixes per ADR M10-04): HarnessInfoTool wire + repairMissingToolResults resume wire
  - 1 scope-bounded (mission FSM legitimately CLI-only via `sov mission run`)
  - **1 deferred as M11 prereq backlog item #40** — server-side slash-command dispatch route (`/clear`, `/context`, `/status`, `/cost`, `/agents`, `/permissions`, `/memory`, `/model`, `/review` currently silently fall through to the model as plain text in `--ui tui`)
- **5 MEDIUM** + **1 LOW** documented (see audit report); most depend on or relate to the slash-dispatch HIGH

**Additionally surfaced and fixed in M10:** an M9.5 config-schema regression (top-level `theme` field rejected by strict-mode Zod) was discovered when the full test suite was re-run from real developer state. 118 silent unit-test failures resolved by adding `theme: z.string().optional()` to `SettingsSchema`. M9.5's tests used hermetic `t.TempDir()` so the developer's real `~/.harness/config.json` wasn't exercised; CI runs from a fresh state where `theme` is never written.

**4 ADRs locked** (M10-01 through M10-04) in `DECISIONS.md` covering the parallel-subagent audit methodology, the existing-test-coverage-as-server-mode-parity decision, the severity-classified gap disposition, and the inline-cheap-fix policy.

**Next gate: M10.5 / M11 prereq #40** — server-side slash-command dispatcher route. M11's default-flip is BLOCKED on this until shipped.

End-to-end verified (automated):

- **2003/2003 unit tests** including server-side route tests via `app.request()` against the same `buildRuntime` the `--ui tui` server boots
- **Go-side render + components + theme tests** all green (re-verified in M10)
- **4 real-Anthropic Haiku 4.5 smokes** all pass (transcripts captured)
- **M10 audit report** at `docs/07-history/state/2026-05-16-tui-parity-audit.md`

## What does NOT work / known gaps after M10

These are the M10-classified MEDIUM/LOW gaps plus the M11-blocking HIGH:

1. **(BLOCKS M11) Built-in slash-command dispatch UNWIRED in server.** `/clear`, `/context`, `/status`, `/cost`, `/agents`, `/permissions`, `/memory`, `/model`, `/review` etc. silently fall through to the model. Only `/compact`, `/skills`, `/theme` work via direct route calls. Backlog item #40 scoped to fix.
2. **(MEDIUM) `createClearedChildSession` UNWIRED** — `/clear` slash-command's child-session-with-system-prompt-only mint. Co-dependent on #40.
3. **(MEDIUM) `auditContextBudget` UNWIRED for `/context` slash** — partially addressed in M10 (HarnessInfoTool's `budget` snapshot section uses it); the slash-command surface remains co-dependent on #40.
4. **(MEDIUM) `appendProjectLocalPermissionRule` not wired in server approval queue** — "yes & remember (project)" persistence path missing. Approval still works as one-shot grants.
5. **(MEDIUM) `createDefaultMemoryManager` + `resolveProjectScope` partially-wired** — MemoryTool itself is in the pool; the manager (needed for `/memory` slash) is not. Co-dependent on #40.
6. **(LOW) `buildSkillCommands` design divergence** — server splits into discovery (GET /skills) + expansion (turns route). No functional gap; intentional per M8 design.

Inherited gaps from prior milestones (still valid):
- **Hover tooltips, click-and-drag selection, double-click word-select** not implemented (M9.6 era).
- **`/skills list` / `/skills show` subcommands** not implemented (M9.6 era).
- **RGBA forms + named colors in TOML loader** rejected by the M9.6 hex regex.
- **Click-on-prompt focus** not implemented in the M9.6 mouse-click handler.

## Behavioral notes worth knowing next session

1. **HarnessInfoTool now in server-mode tool pool** (M10 Fix 1). The snapshot is lazy — invoked at model tool-call time, reads permission settings + MCP server state live. `slashCommands` returns `[]` intentionally per server-mode design (no client-side slash registry yet).
2. **Resume hydrate path now repairs orphan tool_use blocks** (M10 Fix 2). `process.stderr` logs the synth count when repair fires. Clean histories pass through unchanged — no spurious inserts on the happy path.
3. **Top-level `theme` field accepted by SettingsSchema** (M10 inline fix for M9.5 regression). TS side doesn't render themes; field is accepted but not consumed.
4. **M10 audit subagents** found that the 24-subsystem prereq checklist enumerates subsystems but not slash-command-stack composition. Future audits should expand the methodology to include slash-command surface coverage in addition to subsystem coverage.
5. **Inherited notes from prior snapshots** (still valid):
   - Per-session `SessionContext` cluster; lazy-built; torn down via `disposeSession`.
   - Trajectory writes on disposal, not per-turn.
   - `runtime.dispose()` order — per-session → router audit logger → MCP pool → approval queue → sessionDb.
   - Compaction creates a new session id; the TUI hops `m.sessionID`.
   - On-disk `SessionDb` at `<harnessHome>/sessions.db`. Override with `--db <path>`.
   - Resume requires a valid UUID; unknown id → `SessionNotFoundError`.
   - Preflight runs once per `--ui tui` boot.
   - Ink TUI preserved on `origin/archive/ink-tui-2026-05-12`. Phase 16.0a daemon code remains dormant.
   - Constructor-injected themes (no globals); 4 built-in palettes (dark, light, tokyo-night, sovereign); TOML user themes at `~/.harness/themes/*.toml`.

## Postmortem-rule compliance check (M10 close-out)

Verified before declaring M10 complete:
- **Rule 1** — `src/ui/terminalRepl.ts` untouched: `git diff master -- src/ui/terminalRepl.ts` returns empty across the M10 commit range.
- **Rule 2** — no helper module deletion: `git diff master --diff-filter=D -- src/` returns empty.
- **Rule 3** — audit was independently conducted by 4 parallel Opus subagents reading literal imports. Audit report at `docs/07-history/state/2026-05-16-tui-parity-audit.md` is the Rule 3 attestation.
- **Rule 4** — `--ui tui` stays opt-in through M11: `src/main.ts` default still `repl`. M11 default-flip BLOCKED on backlog #40 until that ships.

## What's open / what's next

- **Open backlog items (3 total, was 2 before M10):**
  - **#17** (P4) — eval-gated auto-promote, deferred from prior sessions.
  - **#38** (P3) — `reviewAutoPromoteMemory` / `reviewAutoPromoteSkills` snapshot gap. Pre-existing parity gap.
  - **#40** (P1, NEW) — server-side slash-command dispatcher. **Blocks M11.**
- **Next milestones for Phase 16.1** — M10.5/M11-prereq (close #40), M11 (default flip — `--ui tui` becomes default), M12 (terminalRepl deprecation), M13 (terminalRepl removal). Each gets its own plan.
- **Real-Anthropic visual smoke** absorbed in M10 via the 4-agent server-mode soak (transcripts captured). No separate hardening session needed.

## Pointers to deeper M10 narrative

- Mechanical audit findings (per-slice tables with severity + recommendations): `docs/07-history/state/2026-05-16-tui-parity-audit.md` §3.
- M10 Fix commits: `1f05ec6` (M9.5 theme regression), `53fda9e` (HarnessInfo wire), `a892f71` (resume repair wire).
- ADRs M10-01..04: `DECISIONS.md`.
- Per-task narrative + mid-build bug catches: `docs/06-testing/testing-log.md` newest-first under the "Phase 16.1 M10" section.
