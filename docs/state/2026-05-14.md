# State of the build — 2026-05-14 close-out

**HEAD:** `91f727c` (M4 close-out + manual smoke + week-estimate rule). A Progressive Disclosure doc restructure landed later in this session — see [Progressive Disclosure docs restructure](#progressive-disclosure-docs-restructure-later-this-session) below for the post-restructure HEAD.
**Suite:** 1873/1873 unit, 4 Go tests in `internal/app/` (+ 3 `t.Skip`'d from M3), 12+ Go tests in `internal/transport/`. Lint + typecheck clean (2 pre-existing warnings in `src/permissions/shellSemantics.ts` unrelated).
**Sov binary:** in sync with master (`adc9026`).
**Manual smoke:** 11/11 ✅ (two regressions caught + fixed in-session; see Manual visual smoke section).

This is a session close-out snapshot. The next session boots from CLAUDE.md and should read this file first. Supersedes `docs/state/archive/2026-05-13.md`.

## Where we are

**Phase 16.1 M4 is shipped.** The "critical correctness" milestone group is complete: persistent on-disk `SessionDb` at `<harnessHome>/sessions.db`, provider preflight on TUI boot, full M4-supported CLI flag forwarding (`--permission-mode`, `--max-tokens`, `--db`, `--resume`, `--no-cache`, `--no-preflight`), and Bubble Tea transcript hydration on `--resume`. terminalRepl remains untouched per Postmortem Rule 1; `--ui tui` is still opt-in; `--ui repl` (default) launches the legacy `terminalRepl.ts` unchanged.

End-to-end verified (automated + headless smoke):
- Persistent SessionDb across process exits (M3's `:memory:` replaced with on-disk; phantom-review cleanup on boot)
- Resume validation rejects unknown UUIDs with stderr remediation + non-zero exit
- Provider preflight runs once per TUI boot; `--no-preflight` skips it
- Deferred flags (`--transcript`, `--capture-fixture`, `--replay-fixture`, `--agent`, `--state-dir`, `--verbose`) surface a warn-and-continue stderr line pointing to the milestone where they wire (M7 / M8 / M9)
- `--legacy-input` hard-errors against `--ui tui` (exit 2 + stderr) since it's REPL-only
- TUI `Init()` performs an HTTP `GET /sessions/:id/messages` before subscribing to SSE; prior transcript replays into the Bubble Tea viewport before the prompt accepts input
- `runTuiLauncher` integration smoke uses a real `buildRuntime` against a temp DB + temp bundle + temp harnessHome; mock-provider boots; lifecycle cleanup verified

**3 prereq boxes flipped today** in `docs/backlog/phase-16-rebuild-prereqs.md`:
- Row 6 (**Session DB persistence**) — `[x]` (M4 — 2026-05-14)
- Row 9 (**Preflight checks**) — `[x]` (M4 — 2026-05-14)
- Row 23 (**Full CLI flag forwarding**) — `[x]` (M4 — 2026-05-14)

**21 boxes remain `[ ]`.** They lie in M5 (User-noticed: hooks, permission modal, sub-agent scheduler), M6 (Long-session survival), M7 (Hermes-layer parity), and M8 (Polish surfaces). See spec §10 for full sequencing.

## What shipped today (2026-05-14)

20 commits from `2287a033` (M3 close-out) to `b49e5bc` (HEAD) covering Tasks 1–10 + this close-out. Newest first:

| Commit | Task | Summary |
|---|---|---|
| `b49e5bc` | T10 | style(test): T10 cleanup — document server lifecycle gap + name magic numbers |
| `29db54d` | T10 | test(cli): tuiLauncher end-to-end smoke with real buildRuntime + server |
| `b0e4b80` | T9 | fix(tui): remove misleading omitempty tag + add fetch timeout |
| `aa66a89` | T9 | feat(tui): hydrate transcript from GET /sessions/:id/messages on Init |
| `c85b4de` | T8 | fix(test): pin exit code 2 specifically for --legacy-input hard error |
| `c67911b` | T8 | feat(cli): warn-and-continue on deferred-subsystem flags; legacy-input hard-error |
| `c1d925b` | T7 | fix(cli): T7 cleanup — correct slash command + static error import |
| `0c90238` | T7 | feat(cli): tuiLauncher forwards M4 flags + handles resume/preflight errors |
| `dda7339` | T6 | fix(server): T6 cleanup — PreflightError accepts cause, mock counter renamed |
| `8e25d64` | T6 | feat(server): buildRuntime runs provider preflight unless disabled |
| `62e1d09` | T5 | style(server): T5 cleanup — drop bogus cast, sharpen preflight JSDoc, symmetric reset |
| `4754746` | T5 | feat(server): runtime.maxTokens + preflight RuntimeOptions |
| `e49ff43` | T4 | style(server): tighten T4 assertion + trim persist comments |
| `dab04fa` | T4 | feat(server): persist user/assistant/tool_result messages in turns route |
| `f45e3fd` | T3 | style(server): trim over-explanatory comments in T3 messages route |
| `0d4d94a` | T3 | feat(server): GET /sessions/:id/messages returns the message backlog |
| `66050ac` | T2 | fix(server): static import for SessionNotFoundError + tighten test assertion |
| `a66b3c4` | T2 | feat(server): buildRuntime validates --resume against sessionDb |
| `b0b0e8e` | T1 | fix(test): guarantee tmp-dir cleanup in T1 dispose-then-reopen test |
| `25c790d` | T1 | feat(server): buildRuntime opens on-disk SessionDb + cleanupPhantomReviews |

### Phases shipped

- **M4** — Critical correctness group complete. Three prereq boxes flipped (Session DB persistence, preflight checks, full M4-supported CLI flag forwarding). Two new server routes (`GET /sessions/:id/messages`, plus the existing `POST /sessions/:id/turns` now persists messages). One new error class (`PreflightError`). New `RuntimeOptions.maxTokens` (default 12000). The Go TUI gains a hydrate-then-subscribe `Init()` flow.

### Tasks 1–10 in detail

- **T1 — On-disk SessionDb + cleanupPhantomReviews** (`25c790d`, `b0b0e8e`). `buildRuntime` opens `<harnessHome>/sessions.db` (override via `--db`) instead of M3's `:memory:`. `cleanupPhantomReviews()` runs once at boot to sweep orphan review rows older than 60s. Test cleanup guarantees the tmp-dir is removed on every code path.
- **T2 — Resume validation in buildRuntime** (`a66b3c4`, `66050ac`). When `RuntimeOptions.resumeSessionId` is set, `buildRuntime` verifies it exists in the freshly-opened SessionDb before any provider work. Unknown UUIDs throw a static-imported `SessionNotFoundError`.
- **T3 — GET /sessions/:id/messages route** (`0d4d94a`, `f45e3fd`). New read-only route returns the message backlog (user + assistant + tool_result rows) for a given session id. Empty array when the session exists but has no messages; 404 when the session id doesn't exist. Comment trim landed on cleanup.
- **T4 — Persist user/assistant/tool_result during turn** (`dab04fa`, `e49ff43`). The `POST /sessions/:id/turns` handler now persists the incoming user message + every emitted assistant message + every tool_result block to the SessionDb mid-turn. Resume now sees real history. Assertion tightening landed on cleanup.
- **T5 — maxTokens + preflight RuntimeOptions** (`4754746`, `62e1d09`). New `RuntimeOptions.maxTokens` (default 12000) threaded through to the provider; new `RuntimeOptions.preflight` (default `true`) gates the preflight call. MockProvider gains a `streamCalls` recorder used by T6 tests. Drop bogus cast + sharpen JSDoc on cleanup.
- **T6 — Preflight execution in buildRuntime** (`8e25d64`, `dda7339`). When `preflight !== false`, `buildRuntime` invokes the provider's preflight contract before returning. Failure raises `PreflightError` (now accepts a `cause` for upstream surfacing). The mock-counter rename (`preflightCalls` → `streamCalls`) landed on cleanup.
- **T7 — tuiLauncher forwards M4 flags** (`0c90238`, `c1d925b`). The launcher reads `opts.permissionMode`, `opts.maxTokens`, `opts.db`, `opts.resume`, `opts.noCache`, `opts.noPreflight` and passes them through to `buildRuntime`. `SessionNotFoundError` and `PreflightError` surface as exit 1 + stderr remediation lines (the slash-command remediation text was corrected on cleanup).
- **T8 — Deferred-flag warnings + legacy-input hard-error** (`c67911b`, `c85b4de`). `--transcript / --capture-fixture / --replay-fixture / --agent / --state-dir / --verbose` emit a stderr "deferred to milestone Mx" line and proceed. `--legacy-input` hard-errors with exit 2 since it's REPL-only. Test was tightened to assert exit code 2 specifically.
- **T9 — Go TUI hydrates transcript via GET /messages** (`aa66a89`, `b0e4b80`). The Bubble Tea `Init()` returns a `fetchMessagesCmd` that performs `GET /sessions/:id/messages` over HTTP, then a `messagesFetchedMsg` repaints the transcript, then the SSE subscription opens. New `fetch` helper has a 10s timeout. Misleading `omitempty` JSON tag removed.
- **T10 — Integration smoke for runTuiLauncher** (`29db54d`, `b49e5bc`). New end-to-end test that drives `runTuiLauncher` with a real `buildRuntime` against a tmp DB + tmp bundle + tmp harnessHome, using mock provider + mock child process. Documents the server-lifecycle gap (parent doesn't outlive child today) + names the empirically-tuned 100ms child-exit delay constant.

### Quality polish passes

Nine task-by-task cleanup commits (the `style(...)` / `fix(...)` companions to each `feat(...)`) caught real issues during the build. Highlights:
- **T1** — tmp-dir leak path in the dispose-then-reopen test.
- **T2** — dynamic-import elimination (`SessionNotFoundError` now statically imported).
- **T3** — comment trim (over-explanatory comments in the messages route).
- **T4** — comment trim in the persistence path.
- **T5** — bogus `as ProviderRequest` cast removed; JSDoc on preflight sharpened; symmetric reset in MockProvider.
- **T6** — `PreflightError` constructor accepts a `cause`; the mock's stream counter was renamed.
- **T7** — slash-command remediation text corrected (`/resume` was incorrectly written as `/resume-session`); static import for the error class.
- **T8** — exit code 2 pinned specifically (not just "non-zero").
- **T9** — misleading `omitempty` removed (the field was always present); fetch timeout added.
- **T10** — server-lifecycle gap documented; 100ms magic number named (`childExitGraceMs`).

## What does NOT work / known gaps for M5+

Deliberate scope-outs documented in the spec / plan. The next milestone groups address each in turn:

1. **Permission modal still placeholder.** The server's `ask()` callback is still a 'deny' placeholder text (the M3 fix). With `permissionMode: bypass` (the typical user config) it never fires. Users in `default` or `ask` mode still see the yellow "permission_request" warning + `ESC + config change` remediation — M5 will wire the full `permission_request → POST /approvals/:requestId` round-trip with an interactive modal in the TUI.

2. **Sub-agent scheduler (M5).** AgentTool + task_create still error if invoked through the TUI server. M5 wires `src/runtime/scheduler.ts` + `src/runtime/laneSemaphores.ts` + `src/runtime/agentRunner.ts` into `buildRuntime`.

3. **Hooks (M5).** PreToolUse / PostToolUse / UserPromptSubmit / Stop hooks per `~/.harness/settings.json` are not yet honored through the TUI surface. M5 wires `src/hooks/runner.ts` + `src/hooks/consent.ts` into the turn pipeline.

4. **Compactor + microcompaction + context-overflow recovery (M6).** `/compact`, `shouldCompactProactively`, the per-part tool-result clearing under `shouldMicrocompact`, and `createClearedChildSession` all need server-side hooks. The TS implementations all exist (`src/compact/*`, `src/agent/sessionRecovery.ts`); they just aren't wired into the TUI's `query()` boundary yet.

5. **MCP client pool / TaskManager / trace writer / trajectory capture / learning observer / review manager (M7).** Six subsystems; six prereq rows. Each exists in the codebase but is gated to terminalRepl today.

6. **Local-model router / capture/replay / `@file:path` expansion / subdirectory hints / skill-as-slash-command / skill visibility filtering / goodbye summary / stall detection / tool-result expand registry (M8).** Nine prereq rows — the largest milestone by box count.

7. **Visual polish deferred to M9.** Tool cards remain minimal placeholders. No markdown rendering for assistant text yet, no syntax highlighting on code or diffs, no inline-diff hunk navigation, no slash autocomplete popup, no mouse, no light/dark theme switch, no custom theme loading.

8. **Parity audit (M10) and default flip (M11) still ahead.** terminalRepl removal (M12 deprecation + M13 removal) is downstream of those.

9. **Deferred flags surface warnings; `--legacy-input` hard-errors.** `--transcript`, `--capture-fixture`, `--replay-fixture`, `--agent`, `--state-dir`, `--verbose` emit a "not yet supported with --ui tui (targeting milestone Mx)" stderr line + continue. `--legacy-input` exits with 2.

10. **3 Go tests in `internal/app/` remain `t.Skip`'d from M3.** `TestApp_rendersTurnErrorVisibly`, `TestApp_showsThinkingIndicatorOnEnter`, `TestApp_thinkingClearedByFirstResponseEvent`. The teatest harness's `WaitFor` polling doesn't catch the rendered text within its 3s window despite the handler being reached. M5+ deep-dive worth-doing.

## Behavioral notes worth knowing next session

1. **On-disk DB at `<harnessHome>/sessions.db`.** The M3 `:memory:` DB is gone — every TUI turn now persists. Override the path with `--db <path>`. Phantom-review cleanup runs once at boot (sweep orphans > 60s old).

2. **Resume requires a valid UUID.** Unknown id → `SessionNotFoundError` raised inside `buildRuntime`. The launcher catches and surfaces an exit-1 with stderr remediation ("Use `/resume` from inside the TUI to pick a known session, or omit `--resume` to start fresh.").

3. **Preflight runs once per `--ui tui` boot.** `--no-preflight` skips it. The error class is `PreflightError`; the launcher surfaces it as exit 1 + stderr with the provider's failure detail in `cause`.

4. **Bun 1.3.13 mock.module cross-file leakage worked around** in `tests/cli/tuiLauncher.test.ts` (see TODOs there) and the new integration test (`tests/cli/tuiLauncherIntegration.test.ts`). The pattern is: rely on real `buildRuntime` + a real ephemeral server + real spawn-then-immediately-exit child rather than try to mock the launcher's modules. Worth a follow-up when Bun's mock surface stabilizes.

5. **3 Go tests in `internal/app/` are `t.Skip`'d** from M3 — same suite count as M3 close-out (4 active + 3 skipped). No new skips from M4.

6. **100ms mock-child-exit delay** in the integration test is empirical (the spawned mock binary writes a sentinel + exits, and we sleep before asserting cleanup). Well-headroomed but flag it if CI flakes there.

7. **TUI `Init()` is now hydrate-then-subscribe.** Before opening the SSE stream, the Bubble Tea program performs an HTTP `GET /sessions/:id/messages` and renders the backlog. The fetch has a 10s timeout. This is the M4 ADR (M4-01) — see `DECISIONS.md`.

8. **Inherited notes from 2026-05-13** (still valid): Ink TUI is preserved on `origin/archive/ink-tui-2026-05-12`. Phase 16.0a daemon code remains dormant in `src/daemon/` and `src/channels/`. `bin/sov-tui` is gitignored — the postinstall script builds it from `packages/tui/` on every `bun install` and `sov upgrade`. `bun pm -g trust @yevgetman/sov` is required on first global install before postinstall fires.

## What's open / what's next

- **Backlog item 17** (eval-gated auto-promote, P4) remains the only open prior backlog item.
- **Next milestones for Phase 16.1** — M5 (user-noticed group: hooks, permission modal, sub-agent scheduler — 3 more boxes), M6 (long-session survival — 3 more), M7 (Hermes-layer parity — 6 more), M8 (polish surfaces — 9 more), M9 (visual polish), M10 (parity audit), M11 (default flip). Each gets its own plan via `superpowers:writing-plans`. User has not yet given the go to start M5 plan-writing.
- **Phase 15** (provider breadth via `@ai-sdk/*`) is parallel-safe with M5 per the umbrella roadmap; user's call on order.
- **Phase 14** dropped permanently from the roadmap (proprietary product, distribution deferred until production-grade).

## Manual visual smoke — COMPLETE (11/11 ✅)

All 11 scenarios passed in this session. Two real M4 regressions surfaced and were fixed in-line; both have regression tests landed. See `docs/testing-log.md` 2026-05-14 entries for the full narrative.

| # | Scenario | Status | Notes |
|---|---|---|---|
| 1 | Fresh persistent session | ✅ | Autonomous (server bypass) + manual (real Anthropic, post-`8fc69cf` idleTimeout fix). |
| 2 | Resume hydration | ✅ | Manual, post-`adc9026` history-loading fix. LLM correctly recalled the prior turn. |
| 3 | Resume unknown id | ✅ | Autonomous — `SessionNotFoundError` + exit 1 + `/resume` remediation. |
| 4 | Preflight pass | ✅ | Autonomous — boot + turn + persist. |
| 5 | `--no-preflight` | ✅ | Autonomous. |
| 6 | `--no-cache` | ✅ | Autonomous. |
| 7 | `--max-tokens 100` | ✅ | Manual — real Anthropic response visibly truncated mid-story; `turn_complete` rendered. |
| 8 | Deferred-flag warning | ✅ | Autonomous — `--transcript` warning fires with M7 target milestone. |
| 9 | `--legacy-input` hard error | ✅ | Autonomous — exit 2 + REPL-only guidance. |
| 10 | `--db <custom path>` | ✅ | Autonomous — rows at custom path, default path absent. |
| 11 | Legacy REPL still works | ✅ | Manual — splash, `/resume` picker, `/quit` all green. |

Regressions caught + fixed: (a) `Bun.serve` 10s `idleTimeout` killed real-Anthropic SSE streams on slow first-token — fix `8fc69cf`. (b) `runTurnInBackground` sent only the new user message to the model on resume, defeating the persistence work — fix `adc9026` + regression test in `tests/server/turns.test.ts`.

## Resolutions of prior open questions (still applicable)

These remain resolved from prior sessions and don't need to be re-litigated:

1. **Daemon survival policy.** Keep `src/daemon/` + `src/channels/` dormant in tree.
2. **Eval-runner status.** Working — `sov eval run --filter <id>` continues to work against `terminalRepl.ts`.
3. **Capture / replay fixtures.** Working through `terminalRepl.ts`; warn-and-continue when used with `--ui tui` (M8 wires them).
4. **AGENTS.md.** Keep — byte-identical mirror of `CLAUDE.md`. Both files contain a "Subagent model policy" section that constrains future builds (Opus 4.7 primary; Sonnet 4.6 only for trivially mechanical; never Haiku).
5. **Phase numbering when retrying foreground refactor.** Phase 16.1 is active — DONE M0–M4, M5+ pending plans.
6. **`scheduled-mission` agent.** Stays in `bundle-default/agents/`.
7. **Phase 16 rebuild prerequisites.** Tracked in `docs/backlog/phase-16-rebuild-prereqs.md`. M4 closes 3 of the 24 boxes; 21 remain.

## Progressive Disclosure docs restructure (later this session)

After M4 + manual smoke shipped, the user pointed at the OpenAI "Harness Engineering" article and asked for Progressive Disclosure to be applied to this repo's docs: `CLAUDE.md` / `AGENTS.md` should be a lean **table of contents** into deeper docs, not a wall of inline content.

### What changed

- **New: `docs/conventions/` (7 files)** — extracted from CLAUDE.md content sections, each one focused on a single standing rule. Files: `lint-and-commit.md`, `sov-upgrade.md`, `testing-log.md`, `semantic-tests.md`, `subagent-policy.md`, `estimation.md`, `repo-layout.md`.
- **New: `docs/design-principles.md`** — the 9 locked design principles, extracted.
- **Rewritten: `CLAUDE.md` and `AGENTS.md`** — now ~100 lines each (was 216). Identity blurb + 7-item session boot + doc-index tables (Standing rules / Reference / State / Forward-looking / Postmortems / Operational log) + Hard Rules (one-liners with pointers to `docs/conventions/`) + Don't list. Byte-identical mirror preserved.
- **Rewritten: `README.md`** — stripped the historical wall of ~10 stacked phase paragraphs. Status section now points at `docs/state/2026-05-14.md` for current state and `CHANGELOG.md` + `docs/state/archive/` for history. `packages/tui/`, `src/server/`, `src/cli/` added to the "what this repo contains" table.

### Doc-gardening pass

Audited cross-references across all current docs. Fixes:

- `docs/backlog/phase-16-rebuild-prereqs.md` — 2 stale state-file paths (`docs/state/2026-05-12.md` → `docs/state/archive/2026-05-12.md`)
- `docs/plans/2026-05-14-phase-16-1-m4-critical-correctness.md` — 3 stale state-file paths (`docs/state/2026-05-13.md` → `docs/state/archive/2026-05-13.md`)
- Dropped volatile HEAD SHA from the CLAUDE/AGENTS index row for the state snapshot — the snapshot itself is the canonical place for HEAD pinning.

Verified that `docs/superpowers/` directory is absent (correct).

**Follow-up — `sov chat` keyword modernization (same session):** Modernized the flagged surfaces. `sov chat <flags>` → `sov <flags>` across `docs/usage.md`, `docs/architecture.md`, `evals/README.md`, and `docs/backlog/phase-16-rebuild-prereqs.md` (one occurrence). The deprecation row in `docs/usage.md:108` is preserved — it documents the deprecation itself. Historical files left untouched on purpose: `CHANGELOG.md` (frozen at 13.3), `docs/testing-log.md` (append-only), `docs/state/archive/`, `docs/plans/` (executed, left as record), `docs/specs/` (point-in-time), `docs/backlog/post-phase-13-4.md` (historical, item 20 already closed). The `chat subcommand action handler` reference in the backlog file's "Source location" column stays — that names a Commander code identifier still present in `src/main.ts`.

### Why

The previous `CLAUDE.md` loaded ~216 lines of mixed standing rules + duplicated state narrative at every session boot. Progressive Disclosure says: index in `CLAUDE.md`, depth on demand. Now every session boots ~100 lines of pure pointers, and each rule loads its own file only when relevant. Boot-time token cost goes down; on-demand depth available unchanged.
