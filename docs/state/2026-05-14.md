# State of the build — Phase 16.1 M6 close-out + 2026-05-15 hardening + post-hardening backlog batch

**HEAD:** `0459979` — `docs(backlog): close item #37 — sov --version surfaces git SHA` (last commit of the 2026-05-15 PM post-hardening batch). The chain since the M6 T7 close-out is: SSE multi-turn fix (`3365fb3` + `f1f5bda`) → backlog items #34, #35, #36, #33, #31 closed in 3-commit cycles (the hardening batch) → autonomous M6 smoke verified all three scenarios end-to-end against real Anthropic → backlog items #32 + #37 closed in 3-commit cycles (the post-hardening batch).
**Suite:** TS — 1943 pass / 0 fail / 4845 expect() (+19 tests since M6 T7 close-out: +1 SSE reconnect, +12 errors-matcher pinning, +1 alternation guard, +1 noOp guard, +1 turns route 400, +1 resume-after-compaction pin, +3 version SHA format/prefix/equality pins). Go — 4 packages green (`internal/app/`, `internal/components/`, `internal/transport/`, `cmd/sov-tui/` no test files); 8 PASS + 3 t.Skip in `internal/app` (the SAME 3 t.Skip'd teatest tests inherited from M3).
**Lint + typecheck:** clean. Same 2 pre-existing `noNonNullAssertion` warnings in `src/permissions/shellSemantics.ts` (unrelated, untouched).
**Sov binary:** at `4bd849c` (the last runtime commit — `fix(version)` wiring `.bun-tag` SHA resolver). `sov --version` now prints `0.1.0-4bd849c`. HEAD has since advanced via docs-only commits (no `sov upgrade` required per the convention).
**Manual smoke:** M4 — 11/11 ✅ (2026-05-14). M5 — Group A autonomous + Group B manual ✅ (2026-05-14). M6 — **autonomously verified end-to-end against real Anthropic 2026-05-15** (see [M6 pre-smoke hardening + autonomous verification](#m6-pre-smoke-hardening--autonomous-verification-2026-05-15) below). Visual / UX manual smoke remains the user's call but no longer load-bearing for confidence in the M6 logic.

This is a session close-out snapshot. The next session boots from CLAUDE.md and should read this file first. Supersedes `docs/state/archive/2026-05-14.md` (the prior same-day snapshot covering M4 + M5 + M5.1 — preserved verbatim in archive for the historical narrative).

## Where we are

**Phase 16.1 M6 is shipped.** The "long-session survival" milestone group is complete: per-part tool-result clearing (microcompaction) fires inside long turns, threshold-triggered proactive compaction summarizes the session before its next turn when system+history exceeds the configured budget, single-retry context-overflow recovery absorbs first-call overflows by compacting + reissuing once, and an explicit `POST /sessions/:id/compact` HTTP verb runs compaction inline for the TUI's `/compact` slash command. The Go TUI consumes both halves — the synchronous verb (T5) and the SSE `compaction_complete` event (T3 + T4) — and pivots `m.sessionID` to the new child session id on either signal. terminalRepl remains untouched per Postmortem Rule 1; `--ui tui` is still opt-in; `--ui repl` (default) launches the legacy `terminalRepl.ts` unchanged.

End-to-end verified (automated + integration smoke):
- `MicrocompactConfig` sourced from `userSettings.microcompaction` via `buildMicrocompactConfig`, threaded onto `Runtime.microcompactConfig` and forwarded to `query()` from the turns route — per-part tool-result clearing fires inside long turns regardless of who built the runtime
- `buildServerCompactor` primitive on `Runtime.compact` — closes over sessionDb / resolvedProvider / model / systemSegments and drives `compactSession` with a same-provider summarize callback (M6-06 inline decision)
- Proactive compaction in turns route — `shouldCompactProactively` probes against `runtime.proactiveCompactThreshold` before each `query()` call; on true, `runtime.compact()` runs synchronously, `compaction_complete` SSE event fires onto the parent session's bus carrying `{ activeSessionId, summary, beforeTokens, afterTokens }`, and the local `sessionId` reassigns to the child for the rest of the turn
- Context-overflow auto-recovery — `runOnce` closure wraps the `query()` iteration; on `isContextOverflowError(terminal.error)` the route runs `runtime.compact()`, publishes `compaction_complete`, and re-runs the same turn ONCE against the new child id; second overflow surfaces as `turn_error` (M6-02)
- `POST /sessions/:id/compact` synchronous route — runs compaction inline, returns `{ activeSessionId, parentSessionId, summary, ... }` JSON body; 400/404/500 envelope shapes pinned (M6-03)
- Go TUI `/compact` dispatch — intercepts user input client-side, POSTs to the synchronous verb, pivots `m.sessionID` from the response, renders a minimal transcript marker
- Go TUI `compaction_complete` SSE handling — pivots `m.sessionID` to `cc.ActiveSessionID`, renders a `─ auto-compacted — N→M tokens — new session <prefix>` marker
- Three `tuiLauncherIntegration.test.ts` scenarios verify the full launcher → buildRuntime → Hono → query() flow end-to-end (microcompact, proactive, overflow recovery)

**3 prereq boxes flipped today** in `docs/backlog/phase-16-rebuild-prereqs.md`:
- Row 7 (**Compactor**) — `[x]` (M6 — 2026-05-14)
- Row 8 (**Microcompaction**) — `[x]` (M6 — 2026-05-14)
- Row 15 (**Context-overflow auto-recovery**) — `[x]` (M6 — 2026-05-14)

**15 boxes remain `[ ]`.** They lie in M7 (Hermes-layer parity: MCP, TaskManager construction, trace writer, trajectory capture, learning observer, review manager — 6 boxes) and M8 (polish surfaces: local-model router, capture/replay, `@file:path` expansion, subdirectory hints, skill-as-slash + visibility, goodbye summary, stall detection, expand registry — 9 boxes). See spec §10 for full sequencing.

## What shipped today (2026-05-14)

**M6 — long-session survival.** 21 commits from `8beff6f` (M5 close-out) to HEAD covering Tasks 1–7 + this close-out. The seven feat-or-cleanup task pairs follow the same shape as M5: `feat(...)` lands the wiring + RED-then-GREEN test, `style(...)` / `fix(...)` lands the cleanup pass, `docs(testing-log): ...` lands the testing-log entry. Newest-first:

| Commit | Task | Summary |
|---|---|---|
| (this snapshot) | T7 | docs: M6 close-out — state snapshot + 3 prereq boxes flipped |
| `f53205d` | T6 | docs(testing-log): M6 T6 cleanup — Go polish applied |
| `ec1a8ca` | T6 | style(tui): M6 T6 cleanup — DRY shortSessionID + inline /compact + sync.Once |
| `4adf949` | T6 | docs(testing-log): M6 T6 — TUI /compact wiring verified |
| `59e5d9f` | T6 | feat(tui): M6 T6 — /compact dispatch + compaction_complete handling |
| `0152ab8` | T5 | docs(testing-log): M6 T5 cleanup — error contract pinned |
| `8bc4a22` | T5 | fix(server): M6 T5 cleanup — reconcile 404 envelope + add 400/500 tests |
| `d27cf71` | T5 | docs(testing-log): M6 T5 — synchronous compact route verified |
| `b4fc7b2` | T5 | feat(server): M6 T5 — POST /sessions/:id/compact synchronous route |
| `3cd77e3` | T4 | docs(testing-log): M6 T4 cleanup — proactive+recovery interaction pinned |
| `e464ffa` | T4 | fix(server): M6 T4 cleanup — proactive+recovery interaction + publish helper |
| `850eb56` | T4 | docs(testing-log): M6 T4 — overflow recovery verified |
| `a977c86` | T4 | feat(server): M6 T4 — context-overflow auto-recovery in turns route |
| `134c02b` | T3 | docs(testing-log): M6 T3 cleanup — proactive compact error path verified |
| `d1ac870` | T3 | fix(server): M6 T3 cleanup — proactive compaction errors route through turn_error |
| `26ac748` | T3 | docs(testing-log): M6 T3 — proactive compaction verified |
| `15ca6cf` | T3 | feat(server): M6 T3 — proactive compaction check in turns route |
| `92cec60` | T2 | style(server): M6 T2 cleanup — DRY constants + empty-summary guard |
| `5d8239c` | T2 | docs(testing-log): M6 T2 — server compactor primitive verified |
| `3803b46` | T2 | feat(server): M6 T2 — buildServerCompactor primitive on Runtime |
| `8602d46` | T1 | style(server): M6 T1 cleanup — direct type imports + descriptive test name |
| `07f7990` | T1 | feat(server): M6 T1 — wire microcompactConfig into query through turns route |

Plus 3 earlier-in-session commits that landed before T1 kicked off:
- `0e21299` — `docs(backlog): track item 30 — router-mode default provider/model in server build` (M5.1 follow-up tracking)
- `82023ca` — `docs(backlog): close items 25-27 — fixed in M5.1 (3b07110)` (M5.1 follow-up tracking)
- `3b07110` — `fix(server): M5.1 — thread router settings into scheduler + lane caps` (M5.1 — see "M5.1 — Settings cascade into scheduler + lane caps" section in `docs/state/archive/2026-05-14.md`)

### Phases shipped

- **M6** — Long-session survival group complete. Three prereq boxes flipped (Compactor, Microcompaction, Context-overflow auto-recovery). One new server primitive (`src/server/compactor.ts` exposing `buildServerCompactor`). One new server route (`POST /sessions/:id/compact` via `src/server/routes/compact.ts`). One new wire-event type (`CompactionCompleteEvent` in `src/server/schema.ts`). Three new fields on `Runtime` (`microcompactConfig`, `compact`, `proactiveCompactThreshold`). Two new fields on `RuntimeOptions` (test-time injection seams: `microcompactConfig`, `proactiveCompactThreshold`). Two Go TUI handlers (`/compact` slash dispatch + `compaction_complete` SSE handler). Three new server-test files (`tests/server/turns.microcompact.test.ts`, `tests/server/turns.proactiveCompact.test.ts`, `tests/server/turns.overflowRecovery.test.ts`, plus `tests/server/compact.test.ts` for the route). Three new integration scenarios in `tests/cli/tuiLauncherIntegration.test.ts`. Three ADRs (M6-01 / M6-02 / M6-03).

### Tasks 1–7 in detail

- **T1 — `microcompactConfig` wiring through the turns route** (`07f7990`, `8602d46`). `buildRuntime` now constructs a `MicrocompactConfig` from `userSettings.microcompaction` via `buildMicrocompactConfig` (or accepts a caller override via `RuntimeOptions.microcompactConfig` for tests). The turns route forwards `runtime.microcompactConfig` to `query()` so the configured `triggerThresholdPct` + `keepRecent` actually drives microcompaction inside the turn loop. Three regression tests pin: (1) the option-override path populates `runtime.microcompactConfig`; (2) the no-override path defaults to `DEFAULT_MICROCOMPACT_CONFIG`; (3) end-to-end — with seeded prior tool_results + a custom test transport that returns Bash on iteration 0 regardless of seeded history, the second provider call's messages array contains 3 cleared `[Tool result cleared` placeholders. Cleanup pass switched to direct type imports + a more descriptive test name.

- **T2 — `buildServerCompactor` primitive on `Runtime.compact`** (`3803b46`, `92cec60`). New `src/server/compactor.ts` exposes `buildServerCompactor({ sessionDb, resolvedProvider, model, systemSegments })` returning a `compact(history, sessionId, signal) => Promise<CompactResult>` callable. Wraps `compactSession()` (`src/compact/compactor.ts:87`) with a same-provider summarize callback per inline decision M6-06 — no auxiliary cheap-model selection in M6 (deferred to Phase 15 / M8 polish). The summarize callback drives `runtime.resolvedProvider.transport.stream(...)` against the same model the parent session uses, with the compactor's `compressionSystemPrompt()` swapped in for the session's normal system segments. Cleanup pass DRY'd up the construction constants + added an empty-summary guard (rejects compactions whose summary returns ''). The closure doesn't need a forward-reference to `Runtime` — it's built from locals already in scope inside `buildRuntime`.

- **T3 — Proactive compaction in turns route** (`15ca6cf`, `d1ac870`, `26ac748`, `134c02b`). Before the `query()` call inside `runTurnInBackground`, the route hydrates message history from `sessionDb`, calls `shouldCompactProactively(history, systemSegments, runtime.proactiveCompactThreshold)`, and if true invokes `runtime.compact(...)` synchronously, publishes `compaction_complete` onto the bus, and reassigns the local `sessionId` to the new child id for the rest of the turn. Threshold sources from `userSettings.compaction.proactiveThresholdPct` (1..99 percentage; converted to fraction) when present; otherwise the function's built-in default of `0.75` applies (M6-05). Cleanup pass moved the proactive block INSIDE the existing try/catch so summarizer throws route through the same `turn_error` publish path that handles `query()` failures — pre-fix, a summarizer throw escaped as an unhandled promise rejection and the SSE stream parked until client disconnect. Two regression tests pin: (1) happy path — `compaction_complete` precedes the first `text_delta`, lineage row exists, mid-turn persistence lands on the child; (2) error path — summarizer throw surfaces as `turn_error` with the parent's sessionId attribution, no lineage row, no `compaction_complete` event.

- **T4 — Context-overflow auto-recovery in turns route** (`a977c86`, `e464ffa`, `850eb56`, `3cd77e3`). Lifted the existing `while (true) { stream.next(); ... }` iteration into a `runOnce(currentMessages)` closure returning `Terminal | undefined`. Outer logic: first `runOnce(messages)` → if `terminal.reason === 'error' && isContextOverflowError(terminal.error)`, run `runtime.compact()` + publish `compaction_complete` + reassign `sessionId = compactResult.newSessionId` + `messages = hydrate()` + second `runOnce(messages)` → publish either `turn_error` (second overflow) or `turn_complete` (success). Cleanup pass: extracted the `compaction_complete` publish into a `publishCompactionComplete(bus, parentSessionId, result)` helper used by all three call sites (T3 proactive, T4 recovery, T5 explicit), and added a regression test pinning the proactive+recovery interaction (Path A — independent budgets; both can fire in the same turn). Three regression tests pin: (1) first overflow → compact + retry → success, no `turn_error`; (2) second overflow → `turn_error`, exactly one `compaction_complete`; (3) proactive + recovery in same turn → exactly two `compaction_complete` events, parent → proactiveChild → recoveryChild lineage chain.

- **T5 — `POST /sessions/:id/compact` synchronous route** (`b4fc7b2`, `8bc4a22`, `d27cf71`, `0152ab8`). New `src/server/routes/compact.ts` mounts the explicit-compaction verb. Reads the session's history from `sessionDb`, calls `runtime.compact(history, sessionId, signal)` inline, returns the JSON `CompactResult` once `compactSession` resolves (M6-03). 400 for malformed `:id` shape (`isValidSessionId` matches the sibling routes); 404 for unknown session id with `{ error: 'not found' }` envelope (matches sibling `sessions.ts`); 500 for downstream summarizer failures with `{ error: <thrown message> }`. Cleanup pass reconciled the 404 envelope (was `{ error: 'session not found', sessionId }` — drift from sibling `sessions.ts` which returns `{ error: 'not found' }`) and added explicit 400 + 500 regression tests. Four total tests pin the full error contract (200/400/404/500).

- **T6 — TUI `/compact` dispatch + `compaction_complete` SSE handling** (`59e5d9f`, `4adf949`, `ec1a8ca`, `f53205d`). Two Go-side pieces. (1) The ENTER handler intercepts `text == "/compact"` BEFORE the POST /turns dispatch — kicks off a `compactCmd` that POSTs to `/sessions/:id/compact`, awaits the JSON response, pivots `m.sessionID = response.activeSessionID`, and appends a `─ compacted — new session <prefix>` transcript marker. (2) The SSE handler dispatches `compaction_complete` events to a new branch that pivots `m.sessionID = cc.ActiveSessionID` and appends an `─ auto-compacted — N→M tokens — new session <prefix>` marker. New `transport.PostCompact(ctx, baseURL, sessionID)` helper mirrors the M5 `PostApproval` shape (separate 60s `compactClient` because the same-provider summarize path can take several seconds and the existing 5s `fetchClient` would prematurely abort). New `transport.DecodeCompactionComplete` follows the existing `Decode<Type>` pattern. 5 new Go tests (2 transport + 1 transport-decode + 2 app integration) pin the wire contract + the pivot semantics via transitive next-POST assertions. Cleanup pass: extracted a `shortSessionID(id) string` helper used by both transcript markers (was 2 byte-identical 4-line truncate-to-8-chars blocks); inlined the `/compact` placeholder + cmd dispatch directly at the intercept site (deleted `compactRequestedMsg` Update branch + the type entirely); used `sync.Once` for the SSE-pivot test's `eventsServedCh` close guard (was bool + sync.Mutex + branch).

- **T7 — Integration smoke + close-out** (this commit chain). Three new scenarios extend `tests/cli/tuiLauncherIntegration.test.ts` to drive the M6 paths through the full launcher → `buildRuntime` → Hono → `query()` flow. The launcher doesn't expose injection seams for `microcompactConfig` / `proactiveCompactThreshold` / transport overrides (those live on `RuntimeOptions` for unit-test convenience), so the M6 suite mocks the runtime module via `buildWrappedRuntimeModule` — wraps the real `buildRuntime` with `pre`/`post` hooks the tests use to override options before construction and mutate the produced runtime before the launcher hands it to `startServer`. The wrapper captures the function reference BEFORE the M6 mock is mounted so the wrapper's recursion into "the real" `buildRuntime` resolves to the production implementation rather than re-entering itself (avoids stack overflow). Three prereq boxes flipped (rows 7, 8, 15). Three ADRs (M6-01, M6-02, M6-03) appended. Old `2026-05-14.md` archived to `docs/state/archive/2026-05-14.md`. CLAUDE.md / AGENTS.md state-snapshot pointers updated; byte-identical mirror preserved.

### Quality polish passes

Six task-by-task cleanup commits (the `style(...)` / `fix(...)` companions to each `feat(...)`) caught real issues during the build. Highlights:
- **T1** — direct type imports (no `import type` round-trip through compiled stubs); more descriptive test name.
- **T2** — DRY'd construction constants; empty-summary guard on the same-provider summarize callback (rejects '' so a regression in the mock's summary path surfaces fast).
- **T3** — moved the proactive block INSIDE the existing try/catch so summarizer throws route through the same `turn_error` publish path that handles `query()` failures (pre-fix: SSE stream parked until client disconnect).
- **T4** — extracted `publishCompactionComplete` helper used by all three M6 call sites; pinned the proactive+recovery interaction with a regression test (Path A — independent budgets).
- **T5** — reconciled the 404 envelope to match sibling `sessions.ts` (`{ error: 'not found' }`); added explicit 400 + 500 regression tests pinning the full error contract.
- **T6** — extracted `shortSessionID` helper for the two transcript markers; inlined `/compact` placeholder + cmd dispatch at the intercept site (removed `compactRequestedMsg` Update branch + type entirely); used `sync.Once` for the SSE-pivot test's channel close guard.

## Critical fix landed post-T7 (2026-05-15)

**Bug:** Latent M3-era correctness bug in the Go TUI. The TUI subscribed to the SSE stream exactly ONCE in `New()` (`packages/tui/internal/app/app.go:101-103`) and never reconnected. The server closes the SSE response body and disposes the per-session bus on every `turn_complete` / `turn_error` (`src/server/routes/events.ts:63-74` — by design, the bus is per-turn). All turns AFTER the first delivered 202 from POST `/sessions/:id/turns` (the bus was recreated on the publish side) but the events were silently dropped client-side because the original SSE subscription had already terminated and was never replaced. The user saw their input echo, the dim "…thinking" placeholder, and then nothing — a `[stream closed]` line landed once at end-of-turn-1 and that was it.

**Discovery path:** Autonomous probe against the production server (`mock` provider, two consecutive POST `/sessions/:id/turns` calls on a single GET `/sessions/:id/events` subscription). The probe fired before the user's three M6 manual smoke scenarios had a chance to surface the bug. Caught it pre-user-smoke.

**Fix (`3365fb3`):** In `app.go`'s `sseDoneMsg` handler, re-Consume a fresh SSE stream against the current `m.sessionID` (which may have pivoted via `/compact` or `compaction_complete`). Skip when the app context is cancelled (user pressed ESC / Ctrl+C) or `baseURL == ""` (render-only test fixtures). Drop the `[stream closed]` transcript line — meaningful when the design was single-turn-per-launch, but with reconnect it's noise after every turn. Bubble Tea correctness: `waitEvent` is a value-receiver method (not a closure), so reassigning `m.events` / `m.errs` in Update and returning `m.waitEvent` lets the new channels take effect on the next wait — verified by reading `app.go:132-151` before applying the fix.

**Test (`TestApp_reconsumesSSEAfterTurnComplete`):** Pins the multi-turn contract by driving two consecutive turns through `teatest.NewTestModel` against an `httptest.Server`. Server's SSE handler counts connections and serves two distinct SSE responses for the two turns; subsequent reconnects (after turn 2's `turn_complete`) hold open via `<-r.Context().Done()` so `srv.Close()` doesn't block on test teardown. Final assertion bounds connection count to `[2, 3]` — 2 = bare minimum (1 per turn), 3 = steady state (initial + 1 reconnect after each turn_complete), > 3 would indicate a tight reconnect loop bug. RED-GREEN verified: stashed the fix, the new test timed out at 3s waiting for `TURN_TWO_REPLY` showing `[stream closed]`; restored the fix, PASS in 0.32s.

**Existing test fixture adjustment:** `TestApp_consumesMultipleEventsFromSingleConnection` — pre-existing handler emitted 3 events including `turn_complete` then returned. With reconnect, that handler would be invoked in a tight loop. Tightened the handler to hold connection #1 open via `<-r.Context().Done()` after emitting (so all 3 events are PROVEN to arrive on connection #1) and changed the assertion from `connectionCount == 1` to `connectionCount <= 2`. The invariant being guarded — "all 3 events arrive on a single connection, not via per-event reconnect" — is preserved.

**Compaction interaction (no regressions):** Mid-turn compaction events arrive on the SAME bus as the rest of the turn's events (the bus is captured into `bus` at `runTurnInBackground`'s start). The fix does NOT reconnect on `compaction_complete` — `m.sessionID` is mutated and the next `sseDoneMsg` (after the post-pivot `turn_complete`) triggers the re-Consume against the new id. `TestApp_compactionCompleteSSEPivotsSession` (M6) and `TestApp_compactSlashRoutesToCompactEndpoint` (M6) both still pass post-fix.

**Impact on M6 smoke viability:** The bug blocked all multi-turn `--ui tui` usage including Scenarios 2 and 3 of the M6 manual smoke (proactive compaction needs sustained turn count; overflow recovery needs a long tool-using turn that triggers re-issue against the new session). Scenario 1 (explicit `/compact`) was technically completable to the compact-marker step but the post-compact normal-turn dispatch was silently broken. Post-fix all three scenarios are actionable end-to-end.

## M6 pre-smoke hardening + autonomous verification (2026-05-15)

After the SSE multi-turn fix, the user requested a focused critical bug-hunt across the M6 surface before sinking real-API tokens into manual smoke. **Three parallel Opus reviewers** (server flow / Go TUI + wire / cross-cutting edge cases) surfaced a handful of real risks. The five highest-priority items were closed before the smoke ran. Then **all three M6 scenarios were autonomously verified end-to-end against real Anthropic** (cost: ~$0.05 across three retries, mostly rejected `BadRequestError` calls which Anthropic doesn't bill).

### Backlog batch closed (5 items)

Each item shipped as a 3-commit cycle: fix + test → testing-log entry → backlog item closed. All five items now closed in `docs/backlog/post-phase-13-4.md`. Per-item commit chain (newest-first):

| Item | Priority | Title | Commits | Suite delta |
|---|---|---|---|---|
| **#31** | P3 | M3.4 turns route does not validate `:id` shape | `b9a4ad8` + `3a2e87e` + `d888aa8` | +1 (1939) |
| **#33** | P4 | Asymmetric `bus.isClosed()` guards in turns route | `79a5c39` + `be9ece3` + `4197818` | 0 (1938 pure de-indent) |
| **#36** | P3 | `estimatedAfterTokens > estimatedBeforeTokens` cosmetic on small sessions | `59aaee4` + `c755fb6` | +1 (1938) |
| **#34** | P2 | Anthropic strict-alternation hazard with `assistant`-role compaction summary | `4653737` + `0dc959e` + `c3b537e` | +1 (1937) |
| **#35** | P2 | `isContextOverflowError` substring matcher unverified vs real provider | `1212a42` + `223e553` + `b48cf9c` | +12 (1936) |

**Item #35 — `isContextOverflowError` matcher verified vs real Anthropic.** Probed once against `claude-haiku-4-5-20251001` with a 330K-token user message. Anthropic returns `BadRequestError` (status 400, `type: 'invalid_request_error'`, `err.message` containing `'prompt is too long: 200039 tokens > 200000 maximum'`). The existing `'prompt is too long'` substring catches it — no matcher extension needed. JSDoc updated with verified format; new `tests/providers/errors.test.ts` (12 cases) pins the contract for the real Anthropic + OpenAI-style + synthetic + 413 + negative shapes. Cost: $0 (Anthropic does not bill `invalid_request_error` failures).

**Item #34 — Anthropic strict-alternation hazard.** `compactSession` persisted the summary as `role: 'assistant'`. If the post-compact tail's first message was also `assistant` (which `alignTailStart` may produce when walking backward to keep tool_use/tool_result pairs intact), the new child's history was `[assistant_summary, assistant_tail0, ...]` → Anthropic 400 (`messages: roles must alternate`). Mock provider in tests accepts anything → integration tests passed but real-Anthropic users would hit this on non-trivial sessions. **Fix (Option A — synthetic bridge):** when `tail[0]?.role === 'assistant'`, prepend `{role: 'user', content: [{type: 'text', text: '(continuing from summary)'}]}` between summary and tail. Preserves the conversational frame. Regression test asserts alternation invariant.

**Item #36 — empty-head no-op semantics.** For small histories where `selectTailStart` returns 0 (everything fits in tail budget), `head = []` → summarizer is called on nothing → returns small text → after-tokens > before-tokens (overhead of summary message added to unchanged tail). Looked broken to a user even though algorithm was correct. **Fix (Option B — explicit `noOp: true` flag):** `compactSession` returns early when `head.length === 0` with `parentSessionId === newSessionId`, `noOp: true`, no DB writes. The `noOp` flag is plumbed through `runtime.compact` → `Runtime.compact` callers (turns route proactive + recovery, `/compact` route) → Go TUI's `compactCompleteMsg` + SSE handler. TUI renders `─ nothing to compact (history already fits)` instead of the misleading `─ compacted — new session <prefix>` when `noOp` is true; session id is NOT pivoted. Recovery branch surfaces `turn_error` if compaction returns no-op (a no-op compact wouldn't help with overflow). Five existing tests updated to seed enough history to clear the no-op threshold.

**Item #33 — drop redundant `bus.isClosed()` guards.** `ServerEventBus.publish()` is idempotent on closed buses, so the guards in turns route were no-ops. Backlog described "two guards + one unguarded" but current code had three (M6 T4 added one). Dropped all three for full symmetry — eventBus is now the single source of truth for closed-state behavior across all four publish sites in `runTurnInBackground`.

**Item #31 — turns route `:id` validation.** `POST /sessions/:id/turns` was the lone outlier among session-scoped routes (all others validate via `isValidSessionId` and 400 on malformed). Added the canonical guard. Bonus empirical evidence: pre-fix RED test showed the bad id then triggered `SQLITE_CONSTRAINT_FOREIGNKEY` cascade in the persistence test running after it.

### Autonomous M6 smoke — all 3 scenarios passed against real Anthropic

The Go TUI's visual rendering can't be autonomously driven, but the BACKEND (server routes + SSE wire + sessionDb persistence + reconnect behavior) can. The smoke was executed as three /tmp/m6-real-smoke-*.ts scripts that mimic the TUI's wire interactions: subscribe SSE, POST turn, watch events, re-Consume after `turn_complete`, post next turn, etc. All against `claude-haiku-4-5-20251001` with the user's real Anthropic credentials.

| # | Scenario | Result | Evidence |
|---|---|---|---|
| **1a** | `/compact` no-op (small session) | ✅ | 3-message history → `noOp: true`, `before=after=2444`, no marker, follow-up turn delivered on same id |
| **1b** | `/compact` real compaction | ✅ | 15-message seeded (random hex to defeat BPE compression) → `noOp: false`, `before=14171 → after=7559` (47% reduction), 1 lineage row, follow-up turn delivered on child id |
| **2** | Proactive at threshold | ✅ | 18K-token seed (6 messages × 3K tokens of `lorem ipsum`-style filler) + `proactiveCompactThreshold = 0.05` → `compaction_complete` SSE event fired BEFORE `text_delta`, 1 lineage row, turn completed normally on child id |
| **3** | Overflow recovery (full happy path) | ✅ | 60 messages × 6KB random hex (~360KB / ~180K real tokens) → Anthropic 400 → `compaction_complete` SSE fired → retry against child id → `turn_complete`, NO `turn_error`, 1 lineage row |

**Implicitly verified by these runs** (couldn't be tested in isolation without real Anthropic):
- Multi-turn SSE reconnect (every scenario does multiple turns over the same flow — the fixed re-Consume pattern is exercised end-to-end against real provider)
- Real Anthropic `BadRequestError` matched by `isContextOverflowError` (Scenario 3 exercised the full recovery flow against the live error shape, beyond what item #35's single-call probe verified)
- Anthropic strict alternation (every recovery hop produced a valid post-compact history that real Anthropic accepted — no 400 on retry)
- Empty-head no-op semantics (Scenario 1a's noOp path)
- Lineage persistence across all three scenarios

**Surprises during the smoke runs (worth noting for next session):**
- **Anthropic's tokenizer is significantly more efficient than `chars/4`.** First S3 attempt with 800K chars of `'The quick brown fox...'` (which BPE compresses heavily) stayed UNDER 200K tokens — Anthropic accepted the "overflow" prompt cleanly. Switched to random hex (BPE-incompressible) at 360K chars to actually trigger overflow at ~180K tokens. The harness's `tokenEstimate.ts` uses the rough heuristic, which is fine for proactive triggering (intentionally conservative) but misleading for "this WILL overflow" predictions.
- **First S1 attempt was noOp because Haiku's responses to story prompts are short.** Six story-continuation turns totaled only ~3.7K tokens — under the 4K tail budget — so head was empty. Switched to seeding 15 messages of ~800 tokens each with random hex prefixes (defeats BPE) to clear both the message-count and token-budget thresholds.

### Net summary of 2026-05-15 work

15 commits since the SSE multi-turn fix (`f1f5bda`). Suite delta: +15 tests (1924 → 1939). Five backlog items closed. Three M6 scenarios autonomously verified end-to-end. M6 logic is now load-bearing-verified against the real provider it'll be used with; only TUI visual UX remains as user-discretion smoke.

## 2026-05-15 PM — post-hardening backlog batch (#32, #37)

After the M6 hardening + autonomous smoke landed (`2e7ffa1`), the next session closed two more accumulated M6 follow-up items: a regression-pin test for resume-after-compaction (#32) and a small `--version` ergonomic surfacing the resolved git SHA (#37).

### Backlog batch (2 items)

Each item shipped as a 3-commit cycle (4 for #37 — `feat` + empirical-discovery `fix` cleanup + testing-log + backlog close). All commits pushed to `origin/master`.

| Item | Priority | Title | Commits | Suite delta |
|---|---|---|---|---|
| **#32** | P3 | Resume-after-compaction regression test | `09da469` + `2c9c272` + `28b43e6` | +1 (1939 → 1940) |
| **#37** | P4 | `sov --version` should print the git SHA | `a89b03c` + `4bd849c` + `575e92e` + `0459979` | +3 (1940 → 1943) |

**Item #32 — resume-after-compaction regression pin.** `SessionDb.loadMessages(parentId)` returns the parent's pre-compaction history verbatim — it does NOT auto-walk the `compactions` lineage table forward to the child. The behavior works by construction (resume passes the parent id directly to `loadMessages`, which filters the `messages` table by exact `session_id` with no lineage join); the test (`tests/compact/compactor.test.ts`) seeds a 6-message parent, runs `compactSession` with `tailTokenBudget: 1` / `minTailMessages: 1` to force real compaction (defeats the no-op short-circuit at `compactor.ts:130`), then asserts `loadMessages(parent)` returns the original 6 messages while `loadMessages(child)` returns the summary+tail. The strongest assertion: `parentMessages[0].content !== childMessages[0].content` — would fail loud if `loadMessages` were ever changed to auto-pivot to the latest descendant. Guards the "go back to where I was when this happened" resume semantic.

**Item #37 — `sov --version` surfaces git SHA.** Pre-fix the output was bare `0.1.0`; post-fix it's `0.1.0-<short-sha>`. `src/version.ts` now resolves the SHA at module load via two resolvers tried in order:

1. **`.bun-tag`** — Bun's canonical artifact for `bun install -g git+ssh://...` global installs (40-hex full SHA written to `<install-root>/.bun-tag`, sliced to the 7-char short form). Matches what `bun pm ls -g 2>&1 | grep sov` reports.
2. **`git rev-parse --short HEAD`** — fallback for dev `bun link` working-tree mode (the repo root one level above `src/` IS a git checkout in this case).

Falls back to bare semver if neither resolves (e.g., tarball install). The `--version` Commander output, `/health` route, and any future VERSION-consuming surface picks up the SHA transparently with no downstream changes. **Empirical discovery during build:** the initial `git rev-parse`-only implementation (`a89b03c`) failed empirically because `bun install -g git+ssh://...` does NOT ship `.git/` — only `.bun-tag`. Post-`sov upgrade` the global install printed bare `0.1.0`, defeating the whole fix. The `fix(version)` cleanup commit (`4bd849c`) added the `.bun-tag` reader as the preferred resolver. Three regression tests (`tests/version.test.ts`) pin the format (semver or semver-with-7+-hex-SHA), the prefix (must start with `package.json` version), and the SHA-equality when run inside a git checkout (dev tree exercises path 2; the global install exercises path 1, covered by post-upgrade empirical smoke recorded in the testing-log).

### Net summary of 2026-05-15 PM work

7 commits since the hardening close-out (`2e7ffa1`). Suite delta: +4 tests (1939 → 1943). Two backlog items closed. `sov --version` now surfaces `0.1.0-4bd849c` (matches the actual binary's runtime SHA). Open backlog drops from 5 items to **4** — all P4, all latent-until-their-milestone or warrant-own-phase (see [What's open / what's next](#whats-open--whats-next) below).

## What does NOT work / known gaps for M7+

Deliberate scope-outs documented in the spec / plan. The next milestone groups address each in turn:

1. **Compaction visual polish deferred to M9.** The `─ compacted — new session <prefix>` and `─ auto-compacted — N→M tokens — new session <prefix>` transcript markers are minimal one-line dim text. M9 owns the styled "compaction summary" card with token-count visualization, summary preview, and rollback affordance. Visual signal that the parent session paused while compaction runs (some kind of spinner / placeholder) also lands with M9.

2. **Auxiliary cheap-model selection deferred to Phase 15 / M8.** Per inline decision M6-06: the `summarize` callback uses the parent's own provider/model rather than routing to a smaller cheap model. Routing to an auxiliary model (e.g., Haiku) for summarization saves cost on the parent's main calls but requires the full Phase 15 provider-breadth work before there's a meaningful set of cheap models to route to. Same-provider is the safe default — it never surprises the user with a model they didn't configure.

3. **Multi-retry on context-overflow not exposed.** M6 ships single-retry per ADR M6-02. The retry-once contract matches terminalRepl and avoids the failure mode where a runaway summarizer that emits the same context every time loops indefinitely. If user demand surfaces, a `userSettings.compaction.maxRetries` field is the obvious config seam.

4. **Proactive threshold not user-configurable beyond the existing settings cascade.** `userSettings.compaction.proactiveThresholdPct` (1..99 integer) is honored when set; otherwise the built-in default (0.75) applies. Per inline decision M6-05, no new schema field was added in M6.

5. **MCP client pool / TaskManager / trace writer / trajectory capture / learning observer / review manager (M7).** Six subsystems; six prereq rows. Each exists in the codebase but is gated to terminalRepl today. M7 wires them into the server-side `query()` boundary.

6. **Local-model router / capture/replay / `@file:path` expansion / subdirectory hints / skill-as-slash-command / skill visibility filtering / goodbye summary / stall detection / tool-result expand registry (M8).** Nine prereq rows — the largest milestone by box count.

7. **Visual polish deferred to M9.** Tool cards remain minimal placeholders. No markdown rendering for assistant text yet, no syntax highlighting on code or diffs, no inline-diff hunk navigation, no slash autocomplete popup, no mouse, no light/dark theme switch, no custom theme loading.

8. **Parity audit (M10) and default flip (M11) still ahead.** terminalRepl removal (M12 deprecation + M13 removal) is downstream of those.

9. **3 Go tests in `internal/app/` remain `t.Skip`'d from M3.** `TestApp_rendersTurnErrorVisibly`, `TestApp_showsThinkingIndicatorOnEnter`, `TestApp_thinkingClearedByFirstResponseEvent`. The teatest harness's `WaitFor` polling doesn't catch the rendered text within its 3s window despite the handler being reached. M6 T6 worked around the same teatest-framebuffer-flush race in the new `TestApp_compactionCompleteSSEPivotsSession` test by using a wire-level signal (`eventsServedCh`) + a 200ms settle window instead of polling on `tm.Output()` — that pattern is the new template for SSE-side teatest scenarios. The 3 inherited skips remain a worth-doing M9 cleanup (or earlier if a test pattern needs `Output()` polling for a non-SSE signal).

## Behavioral notes worth knowing next session

1. **Compaction creates a new session id; the TUI hops `m.sessionID`.** Per ADR M6-01: every compaction (proactive, overflow recovery, or explicit `/compact`) creates a fresh child session id. The TUI updates `m.sessionID` on receiving either the `compaction_complete` SSE event OR the JSON response from `POST /sessions/:id/compact`. Subsequent POSTs (turns, approvals, further compactions) route to the new child. SessionDb persists parent→child lineage via `recordCompactionLineage` for audit; the SSE subscription stays on the parent id (the bus is keyed on parent and continues to surface post-compaction events under the parent id).

2. **Single retry on context-overflow.** Per ADR M6-02: first overflow → compact + retry once. Second overflow → `turn_error`. The proactive + recovery branches operate on independent budgets — a proactive compaction earlier in the same turn does NOT prevent the recovery branch from firing, so a single turn can emit TWO `compaction_complete` events. The TUI's compaction handler is idempotent — it pivots `m.sessionID` to the LATEST `activeSessionId` (the recovery child id wins over the proactive child id when both fire).

3. **`POST /sessions/:id/compact` is synchronous.** Per ADR M6-03: the explicit verb runs compaction inline and returns the JSON `CompactResult` body. No SSE event fires for this path — the JSON response IS the notification. Errors return JSON-shaped responses (400/404/500) with `{ error: <message> }` envelope matching sibling routes.

4. **Microcompaction triggers based on tool-result token share.** `shouldMicrocompact(history, config)` returns true when the per-part `tool_result` blocks exceed `config.triggerThresholdPct` of the estimated total context. Default 40%. The microcompact pass replaces older tool_result content with `[Tool result cleared`-prefixed placeholders, keeping the most recent `config.keepRecent` (default 5) intact. Fires inside the turn loop AFTER `runTools` and BEFORE the next provider call — mutates the in-flight history array but never persists. Default `compactableTools` covers Bash + WebFetch + Read; configurable via `userSettings.microcompaction` (though `compactableTools` is not in the schema today — M6 left it as a code default).

5. **Proactive threshold stored as percentage.** `userSettings.compaction.proactiveThresholdPct` is a 1..99 integer (human-friendly editing); `buildRuntime` divides by 100 to get the fraction `shouldCompactProactively` expects. Default 0.75 from the function's own default. The compactor self-guards against the frozen system prompt alone exceeding the threshold (`compactor.ts:177-183`) — without that guard, compaction would loop indefinitely because the child still has the same system prompt.

6. **Same-provider summarization (no auxiliary model).** Per inline decision M6-06: the `summarize` callback uses `runtime.resolvedProvider.transport.stream(...)` with `runtime.model`. No auxiliary cheap-model routing. Auxiliary selection is Phase 15 / M8 polish.

7. **The wrapped-runtime test pattern.** The M6 integration suite (`tests/cli/tuiLauncherIntegration.test.ts:M6 describe block`) uses a `buildWrappedRuntimeModule` factory to inject `pre`/`post` hooks before/after `buildRuntime` runs. The wrapper captures the function reference BEFORE the mock is mounted so the wrapper's recursion into "the real" `buildRuntime` resolves to the production implementation rather than re-entering itself (avoids stack overflow). This pattern is the right shape for any future integration test that needs to inject behavior the launcher's CLI-shaped opts bag doesn't expose.

8. **The microcompact event is NOT yet on the wire.** `query()` emits a `microcompact` event onto its internal stream (`src/core/query.ts:421`), but the turns route's `mapServerStreamEvent` currently returns null for it (deferred to M4+ per the file header comment). Integration tests that need to observe microcompaction must inspect the provider's call-2 messages array directly (cleared `[Tool result cleared`-prefixed placeholders) rather than relying on an SSE event. M9 visual polish is the natural place to wire it through.

9. **`CompactResult.noOp: true` indicates "nothing to compact" (item #36).** When `compactSession`'s `head` is empty (small history fits in tail budget), the function returns early without writes — `parentSessionId === newSessionId`, `noOp: true`, no `recordCompactionLineage` call. The flag is plumbed through `runtime.compact` → server routes → Go TUI's `compactCompleteMsg` and SSE handler. TUI renders `─ nothing to compact (history already fits)` (no session-id pivot). Recovery branch surfaces `turn_error` if compaction returns no-op (a no-op compact wouldn't help with overflow). Tests that construct minimal sessions need to seed enough history to clear the `selectTailStart` floor (≥5 messages with ≥4K tokens of compactable content above the tail budget).

10. **Compaction summary is `role: 'assistant'` BUT a synthetic `user` bridge gets prepended when the tail starts with assistant (item #34).** `compactSession` checks `tail[0]?.role` after building the new history; on `'assistant'`, prepends `{role: 'user', content: [{type: 'text', text: '(continuing from summary)'}]}` between summary and tail to maintain Anthropic's strict alternation. Mock provider doesn't care; real Anthropic 400'd on the original shape. The bridge is included in the returned `result.tail` so both consumers (server route via `hydrate()` and terminalRepl via `result.tail` directly) receive the alternation-safe history without caller-side changes.

11. **Real Anthropic `BadRequestError` on overflow is `'prompt is too long: N tokens > 200000 maximum'` (item #35 verified).** The matcher's `'prompt is too long'` substring catches it. `tests/providers/errors.test.ts` (12 cases) pins the verified format alongside synthetic + 413 + negative shapes. If a future Anthropic SDK change reformats the error message and the substring stops matching, T4's overflow recovery will silently fail (turn_error instead of recovery); the test would catch that on the next CI run.

12. **Inherited notes from prior snapshots** (still valid):
   - On-disk `SessionDb` at `<harnessHome>/sessions.db`. Override with `--db <path>`. Phantom-review cleanup runs once at boot.
   - Resume requires a valid UUID. Unknown id → `SessionNotFoundError` raised inside `buildRuntime`; launcher surfaces exit 1 + stderr remediation.
   - Preflight runs once per `--ui tui` boot. `--no-preflight` skips it.
   - Bun 1.3.13 `mock.module` cross-file leakage worked around in `tests/cli/tuiLauncher.test.ts` and `tests/cli/tuiLauncherIntegration.test.ts` via defensive re-pin.
   - 100ms mock-child-exit delay in the bare-server-up integration test is empirical; the M5/M6 SSE scenarios use 5s.
   - Ink TUI preserved on `origin/archive/ink-tui-2026-05-12`. Phase 16.0a daemon code remains dormant in `src/daemon/` and `src/channels/`.
   - `bin/sov-tui` is gitignored — postinstall builds from `packages/tui/` on every `bun install` and `sov upgrade`.

## What's open / what's next

- **Open backlog items (4, all P4):**
  - **#17** (P4) — eval-gated auto-promote, deferred from prior sessions; warrants its own phase.
  - **#28** (P4) — DaemonEventBus integration for server-mode TaskManager. M7 consumer; lands with the broader review/learning subsystems.
  - **#29** (P4) — lipgloss `Style.Copy()` deprecation in Go TUI permission modal. Cosmetic; M9 cleanup.
  - **#30** (P4) — router-mode default `subagentDefaultProvider` / `subagentDefaultModel` in server build. Latent until router server-mode lands.
- **Next milestones for Phase 16.1** — M7 (Hermes-layer parity: MCP client pool, TaskManager construction, trace writer, trajectory capture, learning observer, review manager — 6 boxes), M8 (polish surfaces — 9 boxes), M9 (visual polish — including compaction marker styled card + the 3 inherited M3 t.Skip's), M10 (parity audit), M11 (default flip). Each gets its own plan via `superpowers:writing-plans`. User has not yet given the go to start M7 plan-writing.
- **Phase 15** (provider breadth via `@ai-sdk/*`) is parallel-safe with M7 per the umbrella roadmap; user's call on order. Phase 15 is also a precondition for the auxiliary cheap-model selection deferred from M6-06.
- **Phase 14** dropped permanently from the roadmap (proprietary product, distribution deferred until production-grade).

## Manual smoke — autonomous backend pass complete; TUI visual smoke optional

The plan calls for three M6 manual smoke scenarios (T7 step 11). **All three were autonomously verified end-to-end against real Anthropic on 2026-05-15** — see [M6 pre-smoke hardening + autonomous verification](#m6-pre-smoke-hardening--autonomous-verification-2026-05-15) for evidence (lineage rows, SSE event ordering, before→after token reductions). The backend logic (server routes + SSE wire + sessionDb persistence + reconnect behavior) is fully verified.

**What remains optional:** Bubble Tea visual UX — the marker glyph/color rendering, the prompt's paste handling, the placeholder dim italic styling, the keyboard intercept timing. None of those change M6 correctness; they affect the user's qualitative experience of the feature. Run the manual smoke if you want UX confidence; skip if the backend verification is enough.

| # | Scenario | Autonomous status | Manual visual check |
|---|---|---|---|
| 1 | Explicit `/compact` from inside the TUI | ✅ Real-compaction path verified (`before=14171→after=7559`, 47% reduction); ✅ noOp path verified (small session → `─ nothing to compact`) | Optional: type `/compact` in the TUI, confirm `[compacting…]` placeholder + marker render styled correctly |
| 2 | Proactive compaction fires after sustained turn count exceeds threshold | ✅ Verified with `proactiveCompactThreshold = 0.05` + 18K-token seeded history → `compaction_complete` SSE before `text_delta` | Optional: edit `~/.harness/config.json` to `"compaction": { "proactiveThresholdPct": 30 }`, drive a long real session, confirm the auto-compacted marker renders before the response |
| 3 | Overflow on a real provider triggers recovery | ✅ Full happy path verified (60×6KB random hex seed → Anthropic 400 → recovery → retry → `turn_complete`, no `turn_error`) | Optional: paste a 250K+ char prompt against Haiku 4.5 in the TUI, confirm the recovery marker + normal response render |

**Notes for the manual smoke (if you choose to run it):**
- `sov --version` only prints `0.1.0` (the package.json version, not the git SHA). To verify your binary is at the latest commit: `bun pm ls -g 2>&1 | grep sov` shows the git ref. Worth a backlog item if you want `sov --version` to print the SHA — small ergonomics fix.
- The autonomous smoke discovered Anthropic's tokenizer is much more efficient than `chars/4` — synthetic Latin filler stayed under 200K tokens at 800K characters. To force overflow with a paste, use random/varied content (~3 chars/token Anthropic rate vs ~6 chars/token for repetitive Latin).
- Lineage verification command: `sqlite3 ~/.harness/sessions.db "select substr(parent_session_id,1,8) as parent, substr(child_session_id,1,8) as child, datetime(created_at,'unixepoch') from session_compactions order by created_at desc limit 3;"`

## Resolutions of prior open questions (still applicable)

These remain resolved from prior sessions and don't need to be re-litigated:

1. **Daemon survival policy.** Keep `src/daemon/` + `src/channels/` dormant in tree.
2. **Eval-runner status.** Working — `sov eval run --filter <id>` continues to work against `terminalRepl.ts`.
3. **Capture / replay fixtures.** Working through `terminalRepl.ts`; warn-and-continue when used with `--ui tui` (M8 wires them).
4. **AGENTS.md.** Keep — byte-identical mirror of `CLAUDE.md`. Both files contain a "Subagent model policy" section that constrains future builds (Opus 4.7 primary; Sonnet 4.6 only for trivially mechanical; never Haiku).
5. **Phase numbering when retrying foreground refactor.** Phase 16.1 is active — DONE M0–M6, M7+ pending plans.
6. **`scheduled-mission` agent.** Stays in `bundle-default/agents/`.
7. **Phase 16 rebuild prerequisites.** Tracked in `docs/backlog/phase-16-rebuild-prereqs.md`. M4 closed 3 of 24 boxes; M5 closed 3 more; M6 closed 3 more. **15 boxes remain.**

## Pointers to deeper M6 narrative

The two-line "what landed" cells in the table above are deliberately terse. For the per-task development narrative (the in-the-moment design decisions, the test-pass / test-fail counts, the cleanup pass content, the regressions caught and fixed mid-build), read the corresponding entries in `docs/testing-log.md` newest-first under the "Phase 16.1 M6" section. Each task has a `feat(...)` testing-log entry plus a cleanup `style(...)` / `fix(...)` testing-log entry — the cleanup entry is where the design lessons land most densely.
