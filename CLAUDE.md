# Instructions for Claude Code sessions developing this repo

You are working on the **Sovereign AI agent runtime** — TypeScript code, not documents. This repo is a Claude-Code-style harness (per ADR H-0003 in the sister `sovereign-ai-docs` repo) that reads a *harness bundle* (the docs repo, or a client's extracted bundle) and drives an LLM conversation against it.

Business context lives in `~/code/sovereign-ai-docs/`. This repo contains code and code conventions only.

This file is a **lean index** — a table of contents into the deeper docs. Read only what your current task requires. Don't try to load it all at boot.

## Session boot

1. **This file** (`CLAUDE.md`) — index and standing rules
2. **`README.md`** — repo intro, install, layout
3. **`docs/state/2026-05-31-post-m2-hardening.md`** — most recent close-out snapshot (**post-M2 hardening run, v0.6.1 → v0.6.13; not a new phase**). Latest release **v0.6.13**; HEAD `bb8240e`. Captures the 13 patch releases shipped since Phase 21 M2 (v0.6.0, 2026-05-25): the TUI global style guide (`packages/tui/internal/style/`), the visual TUI QA / VHS screenshot loop (`bun run visual`), UX fix rounds + end-of-turn gap tuning, the **v0.6.12 deep bug-hunt audit (21 fixes — 1 critical / 11 high / 8 medium / 1 low)**, and the **v0.6.13 markdown heading-color change (sky-200 `#bae6fd` → sky-100 `#e0f2fe`)**. TS suite ~2660 (~2646 pass clean / 3 known env-only learning-test fails / 14 skip). No phase opened or closed; open product backlog still #17 (+ new CI-maintenance #49). Predecessor: `docs/state/2026-05-25-phase-21-m2.md` (Phase 21 M2 — GitHub Actions release automation shipped 2026-05-25; first cut v0.6.0; ADR P21-C). Replaced each session — find the latest via `ls docs/state/*.md | sort -r | head -1`.
4. **`docs/backlog/post-phase-13-4.md`** — open backlog items not in the canonical build plan.
5. **`~/code/sovereign-ai-docs/harness/docs/runtime/runtime-scaffold-plan.md`** — Phase-0/1 scaffold contract this repo was seeded against.
6. **`~/code/sovereign-ai-docs/harness/docs/runtime/harness-build-plan.md`** — canonical remaining phased plan.
7. **`~/code/sovereign-ai-docs/harness/docs/reference/agent-harness-design-lessons.md`** — unifying design principles and Claude Code reference lessons.
8. **`docs/plans/2026-06-03-learning-loop-spike-kickoff.md`** — **CURRENT #1 PRIORITY: the learning-loop spike.** Start here — points at the canonical specs in `sovereign-ai-docs` (the spike spec, the portable-learning-layer design, ADR H-0010) and instructs authoring the implementation specs + build plans in this repo.

`~/code/claude-code/src/` is the architectural reference. Look up specific patterns there when a design question comes up.

## Doc index

Each link is a chapter loaded on demand. Don't pre-read.

### Standing rules — operating conventions

| File | When to read |
|---|---|
| [`docs/conventions/lint-and-commit.md`](docs/conventions/lint-and-commit.md) | Before any commit — `lint`, `typecheck`, `test` all required; atomic commits; push autonomously |
| [`docs/conventions/sov-upgrade.md`](docs/conventions/sov-upgrade.md) | After any `src/` or `bundle-default/` or `packages/tui/` change — keep global binary current |
| [`docs/conventions/cutting-releases.md`](docs/conventions/cutting-releases.md) | After any `src/` / `bundle-default/` / `packages/tui/` change — cut the next binary release in the same session so `~/.sov/bin/sov` picks it up |
| [`docs/conventions/testing-log.md`](docs/conventions/testing-log.md) | Before testing — append-only log obligation |
| [`docs/conventions/semantic-tests.md`](docs/conventions/semantic-tests.md) | When triaging whether to run `bun run test:semantic` |
| [`docs/conventions/subagent-policy.md`](docs/conventions/subagent-policy.md) | Before dispatching any subagent — HARD RULE on Opus / Sonnet / never-Haiku |
| [`docs/conventions/estimation.md`](docs/conventions/estimation.md) | Before quoting effort/timeline — sessions/dispatches/wall-minutes, never weeks |
| [`docs/conventions/repo-layout.md`](docs/conventions/repo-layout.md) | Before adding files in `src/`, naming a plan/spec, or moving things |
| [`docs/conventions/tui-style-guide.md`](docs/conventions/tui-style-guide.md) | **Before ANY visual work in `packages/tui/`** — all spacing, padding, margins, borders, glyphs, brand colors, and typography tokens MUST come from `style.S.*`; never hardcode layout values in components |
| [`docs/conventions/tui-color-rendering.md`](docs/conventions/tui-color-rendering.md) | Before adjusting any text color in `packages/tui/` — body text MUST inherit terminal default; never assume a "bright" hex/ANSI value renders bright |
| [`docs/conventions/tui-ux-patterns.md`](docs/conventions/tui-ux-patterns.md) | Before changing TUI layout, chrome, or visual behavior — flow layout, splash, spinner, separator, tool events, file-ref auto-wrap, prompt/status styling |
| [`docs/conventions/visual-tui-qa.md`](docs/conventions/visual-tui-qa.md) | **Before evaluating any TUI UX visually** — drives sov-tui via VHS, renders PNG screenshots to `.harness/visual/output/`, agent `Read`s the result. `bun run visual [name]` |

### Design reference

| File | What's in it |
|---|---|
| [`docs/design-principles.md`](docs/design-principles.md) | The 9 locked design principles. Don't relitigate. |
| [`docs/architecture.md`](docs/architecture.md) | Current runtime flow — request lifecycle, system prompt, tools, permissions, persistence, sub-agents, microcompaction, REPL UX layers, learning + review pipelines, trajectory capture |
| [`docs/usage.md`](docs/usage.md) | Day-to-day operation — CLI flags, subcommands, slash commands, eval suite, local-model router, profiles, providers, themes, web tools |
| [`docs/extending.md`](docs/extending.md) | Recipes for adding extension points — tools, providers, slash commands, skills, hooks, MCP servers, agents, permission rules, trajectory redaction |
| [`docs/semantic-testing.md`](docs/semantic-testing.md) | Semantic test framework — judge backends, suites, coverage inventory |

### Current state

| File | What's in it |
|---|---|
| [`docs/state/2026-05-31-post-m2-hardening.md`](docs/state/2026-05-31-post-m2-hardening.md) | **Canonical current-state snapshot — post-M2 hardening run (v0.6.1 → v0.6.13); not a new phase.** Latest release **v0.6.13**; HEAD `bb8240e`. Captures the 13 patch releases since Phase 21 M2 (v0.6.0): TUI global style guide (`packages/tui/internal/style/`) + routing-output sky-300 color fix; visual TUI QA / VHS loop (`bun run visual`, multi-screenshot scenarios); UX fix rounds + end-of-turn gap tuning; **v0.6.12 deep bug-hunt audit (21 fixes — 1 critical / 11 high / 8 medium / 1 low, test-first, `eaee313..0400255`)**; **v0.6.13 markdown heading color sky-200 `#bae6fd` → sky-100 `#e0f2fe`** (clearly lighter than the sky-300 `#7dd3fc` bold/inline-code emphasis). TS suite ~2660 (~2646 pass clean / 3 known env-only learning-test fails / 14 skip); Go all packages green. No phase opened/closed; no bundle changes; no new ADRs. |
| [`docs/state/2026-05-25-phase-21-m2.md`](docs/state/2026-05-25-phase-21-m2.md) | **Prior — Phase 21 M2 (release automation) shipped 2026-05-25.** GitHub Actions workflow at `.github/workflows/release.yml` drives binary releases on `v*.*.*` tag push; `scripts/release.ts` refactored into a thin orchestrator over new extracted scripts (`release-shared.ts` + `release-build-target.ts` + `release-upload.ts`); both local + CI paths share the same code. Idempotent upload step keeps local-cut-then-CI scenarios green. Fine-grained PAT scoped to `sov-releases` only, stored as `SOV_RELEASES_TOKEN`. First cut: v0.6.0 (~4 min wall time end-to-end). ADR P21-C in DECISIONS.md. |
| [`docs/state/2026-05-24-config-ux-rebuild.md`](docs/state/2026-05-24-config-ux-rebuild.md) | Prior — **Config UX rebuild shipped 2026-05-24; both `sov config` and `/config` now share a single branded Bubble Tea TUI driven by a curated 10-group catalog.** Replaces the hand-rolled raw-mode `sov config` picker (`src/ui/configMenu.ts`, 389 LoC, deleted) and the JSON-dump `/config` slash command. Hierarchical drill-in menu with `valueColumn` + `badge` (`✓ live` / `⟳ next session`) per row. Editor-aware coercion (string fields preserve numeric-looking input — fixes the legacy `parseValueLiteral` over-coercion). On validation failure, `runSet` re-emits the SAME editor with the typed value preserved + the validation error as subtitle (HIGH from final review) so the user corrects in place. v0 live-apply set: `theme`, `defaultModel`, `providers.<x>.model` (conditional on active provider), `verbose`, `webSearch.*`. New `src/config/catalog.ts` (817 LoC, 10 groups + 4 provider subgroups, every field in `SettingsSchema`), `src/config/liveApply.ts` (6 hooks keyed by dotpath, all gracefully handle `commandCtx === undefined` for `sov config` standalone mode), `src/commands/configOps.ts` (slash dispatcher with root menu / group submenu / edit / set / unset verbs; legacy `show|path|get` preserved as scriptable escape hatches), `src/cli/configMode.ts` (`runConfigOnlyMode` boots minimal Runtime literal — real `SessionDb` rooted at `<harnessHome>/sessions.db` plus stub everything else: empty toolPool, empty agents, throw-on-invoke task/review/scheduler callbacks; no `buildRuntime`, no preflight, no providers, no bundle load; sub-50ms boot; spawns sov-tui with `--initial-command=/config`). Go: extended `PickerCard` with right-aligned ValueColumn + Badge wide layout (backwards-compat preserved — /model, /resume, /export, /theme stay byte-identical via the no-extras code path), new `InputCard` Bubble Tea component on `bubbles/textinput` for string/number/secret edits with `EchoPassword` masking + same `CardBorderStyle` box, `app.go` integration for `inputOpen` + `verboseChanged` SSE side-effects + `--initial-command` boot fire on first WindowSizeMsg (guarded by `initialFired`). Wire: `PickerOpenItemSchema` gains optional `valueColumn` + `badge`, new `InputOpenConfigSchema`, `CommandSideEffectsSchema` gains `inputOpen` + `verboseChanged` (pointer bool so absence is distinct from explicit false). Final-review subagent surfaced 1 HIGH + 5 MEDIUM; HIGH (validation failure now re-emits same editor with typed value preserved + error subtitle via `reopenEditorWithError`) + 2 MEDIUMs (string-field over-coercion fixed via `coerceValueForEditor` switching on editor.kind: string/secret/enum preserve verbatim, number uses `Number()` with NaN guard, boolean accepts true/false case-insensitive; theme hook respects `commandCtx === undefined` before mutating singleton) fixed inline with 3 new tests. TS suite **2420/0/14** (+114 from Phase 2 close-out's 2306). Go suite +30 tests across PickerCard/InputCard/app/transport. Semantic suite gained `23-config-ux.cases.ts` (5 cases: JSON-dump regression, reload badge, theme live-apply, validation error, secret masking). No bundle changes (rebuild surface entirely in `src/`, `tests/`, `packages/tui/`). No ADRs (purely additive at the wire; all decisions captured in the spec at `docs/specs/2026-05-24-config-ux-rebuild-design.md` and plan at `docs/plans/2026-05-24-config-ux-rebuild.md`). Release v0.5.1 follows. |
| [`docs/state/2026-05-23-phase-2-task-routing.md`](docs/state/2026-05-23-phase-2-task-routing.md) | Prior — **Phase 2 (multi-provider task routing observability) shipped 2026-05-23; Phase 2 closed.** Wraps full atom-level observability around the Phase 1 smart router. The runtime synthesizes four new SSE event types (`delegator_plan`, `delegator_atom_started`, `delegator_atom_complete`, `delegator_complete`) by observing the scheduler's delegation lifecycle — no delegator-prompt changes required. New `src/router/progressEvents.ts` — `DelegationLifecycleEvent` discriminated union + `synthesizeDelegationEvents({ bus, rootSessionId, agentRegistry })` factory producing the per-turn closure that tracks active delegator state (its `childSessionId`, atom counter, lane-distribution accumulator) and publishes the four SSE events. Scheduler gets two new optional callbacks on `DelegateInput`: `delegationLifecycleRecorder?: (event) => void` (invoked at start + every return path) and `perChildTimeoutMsOverride?: number` (three-step precedence: override → opts → agent-derived). `ToolContext` gets two new optional fields: `laneRegistry?: LaneRegistry` (set to runtime's registry; `AgentTool.call` resolves `agent.role → lane.timeoutMs`) and `delegationLifecycleRecorder?` (threaded into `scheduler.delegate()`). Per-atom + per-delegator metadata persisted in SessionDb: `{kind: 'routing-delegator', parentSessionId}` for delegator sessions; `{kind: 'routing-atom', laneName, laneProvider, laneModel, parentDelegatorSessionId}` for cost-lane atoms; legacy `{agentName, kind: 'subagent'}` preserved for non-router children. Scheduler's `createChildSession` callback signature gained `lane: { name, provider, model } | null` and `isDelegator: boolean` (both computed from `agent.role` + `resolveLane`). Two new SessionDb helpers via `json_extract`: `listRoutingAtomsByParent(parentSessionId)` (atom rows under any delegator under the given parent, ordered by `created_at ASC`) and `listRoutingAtomsAll()` (cross-session). New `/routing-stats` slash command at `src/commands/routingStats.ts` + pure aggregator at `src/router/stats.ts` — `computeRoutingStats(rows)` returns `{ scope, totalAtoms, byLane: { count, pctOfTotal, successCount, successRate, avgDurationMs, totalDurationMs }, overallSuccessRate, overallAvgDurationMs }`; success heuristic v0 is `outputTokens > 0` (Phase 2.5 refinement candidate); `--all` flag aggregates across sessions. New `getRoutingStats` on `CommandContext`. TUI rendering via new `packages/tui/internal/components/delegatorline.go` (glyph vocabulary: `◇` plan, `→` atom start, `✓` success, `✗` failure, `◆` done; lane distribution sorted by count desc then name asc for deterministic output) + `packages/tui/internal/transport/delegator_events.go` (Go struct decoders) + four new cases in `app.go` SSE switch. `sov drive` four new cases in `EventRenderer.handle` produce plain-text bracketed lines (`[delegator_plan] dispatching <n> atom(s)`, `[delegator_atom <idx>] starting on <lane>: <preview>`, etc.). `sov serve` streaming branch of `POST /v1/chat/completions` subscribes to the per-session bus and writes `event: hermes.delegator.progress\ndata: <json>\n\n` side-channel SSE frames interleaved with the OpenAI-shaped main stream (filtered to delegator_* event types; unsubscribe in `finally`); new `buildDelegatorProgressPayload(event)` JSON helper in `src/openai/streaming/chunks.ts`. New `resolveTaskRoutingEnabled(envValue, settingsValue)` pure helper honoring `SOV_TASK_ROUTING_ENABLED='1'`/`'0'` env override (anything else falls through to settings). Lane `timeoutMs` enforcement (Phase 1's R-D R-D deferred follow-up) wired end-to-end; the Phase 1 skipped `atomTimeout.test.ts` is unskipped and green. Purely additive — `taskRouting.enabled: false` (default) is byte-identical: no synthesis closure constructs, no side-channel writes, no `/routing-stats` data accrues. No bundle changes (Phase 2 surface entirely in `src/`, `tests/`, `packages/tui/`). No ADRs (purely additive — all design decisions captured in the spec at `docs/specs/2026-05-23-multi-provider-task-routing-design.md` and the plan at `docs/plans/2026-05-23-phase-2-task-routing.md`). TS suite **2306/0/14** (+63 from Phase 1's 2243). Go suite +21 tests across decoders / formatters / app SSE switch. T12 (cut v0.5.0 release) follows. |
| [`docs/state/2026-05-23-phase-1-task-routing.md`](docs/state/2026-05-23-phase-1-task-routing.md) | Prior — **Phase 1 (multi-provider task routing smart-router foundation) shipped 2026-05-23 late afternoon; Phase 1 closed.** New `taskRouting` config block in `src/config/schema.ts` (`LaneConfigSchema` + `TaskRoutingSchema`: `{ enabled: false, delegator: { model: 'claude-sonnet-4-6' }, lanes: { 'cheap-task'?, 'moderate-task'?, 'frontier-task'? } }`; per-lane partial overrides inherit `LANE_DEFAULTS`). New `src/router/` mini-suite (~250 LoC): `lanes.ts` (`LANE_DEFAULTS` per-lane Haiku/Sonnet/Opus defaults + pure `resolveLane(name, cfg)`); `laneRegistry.ts` (`buildLaneRegistry(cfg)` pre-resolves all four known lane names at construction, serves `lookup(role) | entries()`); `preflight.ts` (`runLanePreflight` aggregates failures across every configured cost lane — skipping `delegator` since its model rides the parent's existing preflight — into a single `LanePreflightError` so credentials can be fixed in one pass). Four new agent definitions under `bundle-default/agents/`: `cheap-task.md` (`role: cheap-task`, `inheritParentTools: true`, `maxTurns: 30`), `moderate-task.md` (`role: moderate-task`, `inheritParentTools: true`, `maxTurns: 50`), `frontier-task.md` (`role: frontier-task`, synthesis-aware prompt with explicit failed-atom handling, `inheritParentTools: true`, `maxTurns: 50`), `delegator.md` (`role: delegator`, `allowedTools: [AgentTool]`, `allowedSubagents: [cheap-task, moderate-task, frontier-task]`, `maxTurns: 50` — the load-bearing decision prompt with lane catalogue + trivial/compound/hard-reasoning decision rule + synthesis-atom pattern + failure handling). New `bundle-default/prompts/smart-router.md` segment injected into the parent's frozen system prompt when `taskRouting.enabled: true` (parent's first action on every turn MUST be `AgentTool(subagent_type: "delegator", ...)`; parent relays delegator's `summary` verbatim). Two surgical scheduler edits land the wire (`src/runtime/scheduler.ts`): `SubagentSchedulerOpts.resolveLane?: (role) => LaneConfig | undefined` consulted in `resolveProviderModel()` BEFORE the Phase 13.2 capability table; `filterToolsForChild(parentPool, allowedTools)` → `buildChildToolPool(parentPool, agent)` with new `inheritParentTools: true` branch returning `parentPool.filter(t => !exclusions.has(t.name))`. `AgentDefinition.inheritParentTools: boolean` + `allowedSubagents: string[]` added to `src/agents/types.ts`. `AgentTool.call` enforces `allowedSubagents` recursion guard. Runtime wiring (`src/server/runtime.ts`): lane registry built UNCONDITIONALLY (so cost-lane sub-agents stay reachable via `AgentTool` even when `enabled: false` — the B-via-D bridge baseline). Atom-failure handling: delegator does NOT re-dispatch on `terminal !== 'completed'`; continues to synthesis with `Atom N (failed: <reason>)` labeling. Synthesis is the final atom — same dispatch mechanism, lane chosen dynamically. Per-lane provider mixing flows through transparently. MockProvider extended (`src/providers/mock.ts`): static `toolUseScript: ToolCallScript[] | undefined` (`'tool_use'`/`'text'`/`'throw'` script kinds) for integration tests. 5 semantic test cases at `tests/semantic/suites/22-task-routing.cases.ts`. No ADRs (purely additive). TS suite **2243/0/15** (+51 from Phase 18 close-out's 2192). T18 (testing-log entry) + T19 (cut v0.4.1 release) followed; Phase 2 then opened. |
| [`docs/state/2026-05-23-phase-18-openai-api-server.md`](docs/state/2026-05-23-phase-18-openai-api-server.md) | Prior — **Phase 18 (OpenAI-compatible HTTP API server) shipped 2026-05-23 mid-day; Phase 18 closed.** New `src/openai/` top-level subsystem: bearer-auth middleware (`SOV_OPENAI_API_KEY` env > `openaiServer.apiKey` config > refuse to boot per D5/OQ1) + `/health` (unauthenticated, probe-friendly) + `POST /v1/chat/completions` (non-streaming JSON + SSE streaming branches, driven via `query()` directly NOT `AgentRunner` because the wire carries full multi-message history — D7) + `GET /v1/models` (catalog from SUPPORTED_MODELS) + per-request model resolution (`harness-default` → runtime bootstrap; explicit `claude-*` / `gpt-*` → fresh `resolveProvider(family, model, { harnessHome })` per call — T9) + pure Zod request/response schemas + pure ContentBlock ↔ OpenAI mappers + stream chunk builders + generator-driven SSE translator. New `sov serve` CLI subcommand boots `Bun.serve` on a stable port (default 8765, env `SOV_OPENAI_PORT` / config `openaiServer.port`, host defaults to `127.0.0.1`); SIGINT/SIGTERM → `server.stop()` + `runtime.dispose()` + exit 0. Tool execution loops INSIDE the single `/v1/chat/completions` call (D9 — `finish_reason` ALWAYS `'stop'` or `'length'`, NEVER `'tool_calls'`); tool invocations surface as `delta.tool_calls[]` chunks (D8 — whole arguments JSON in one chunk, no partial streaming) + `event: hermes.tool.progress\ndata: {tool_use_id, output?, is_error?}\n\n` SSE side-channel events for harness-aware UIs. Permission policy mirrors cron headless: `mode: 'default'` + auto-deny `ask` (D11); tool pool filtered against `SUBAGENT_EXCLUDED_TOOLS` (D12). SessionDb rows minted per request tagged `metadata.kind='openai-api'` with PK namespaced `openai:<id>` to prevent cross-surface pollution (post-H1 audit fix `7303e96`); the wire (`chatcmpl-<id>`) echoes the CLIENT's unprefixed `X-Session-Id` view. Stateless per D10 — request body's `messages[]` is the sole source of conversation history; the row exists purely for trace + learning observability. Abort-on-disconnect (T10): `c.req.raw.signal` → request-scoped `AbortController` → `query()`'s signal — no wasted provider tokens after the client gives up. Post-audit fixes layered in pre-close-out: H1 namespace X-Session-Id (`7303e96`); H2 wrap non-streaming drain in catch + return OpenAI error envelope via `buildProviderErrorResponse` classifier (`573a45b` — `CredentialUnavailableError` → 401 `invalid_api_key`; `ProviderHttpError` → mirror upstream status + `upstream_error`; else 500 `api_error`); H3 collapse non-spec `finish_reason` values to `'stop'` (`ad74f0c` — TypeScript narrows return type to `'stop' \| 'length'`). No ADRs (purely additive — all decisions captured inline in the plan at `docs/plans/2026-05-23-phase-18-openai-api-server.md`, D1-D14 + OQ1-OQ5). TS suite **2192/0/14** (+117 from morning Phase 17 close-out's 2075 baseline; +196 from prior session's 1996). T12 (cut v0.4.0 release) follows. |
| [`docs/state/2026-05-22-phase-17-cron.md`](docs/state/2026-05-22-phase-17-cron.md) | Prior — **Phase 17 (cron / scheduled jobs) shipped 2026-05-22 evening; Phase 17 closed.** New `src/cron/` subsystem (types / schedule parser supporting relative + interval + cron + ISO / atomic jobs.json CRUD / `CronRunner` with file-lock + fake-clock-injectable tick / executor factory / production glue) + `sov cron add | list | show | pause | resume | delete | run | tick` CLI surface + 60-second tick loop embedded in `buildRuntime` lifecycle (`.unref?.()` so tests don't hang; `cronEnabled: false` opts out). Each job runs in a fresh `metadata.kind='cron'` session via `AgentRunner` with auto-deny on permission asks (matches `sov drive` headless mode). Skill chaining via `expandSkillPrompt` (user-message injection — matches harness convention from M8); pre-agent scripts via `spawnSync` with interpreter inference (`.py → python3`, `.ts/.js → bun`, `.sh → bash`) + 120s default timeout + 16 KiB stdout cap. Delivery: `<harnessHome>/cron/outbox/<jobId>/<ts>.txt`; `[SILENT]` first-line prefix (case-insensitive, post-trim) short-circuits the file write. Recursion guard: six cron CRUD tool names (`cron_add` / `cron_list` / `cron_show` / `cron_pause` / `cron_resume` / `cron_delete`) blocked in `SUBAGENT_EXCLUDED_TOOLS`. No ADRs (purely additive — all decisions captured inline in the plan at `docs/plans/2026-05-22-phase-17-cron-scheduled-jobs.md`). TS suite **2053/0/14** (+57 from morning's 1996 baseline). T11 (cut v0.3.0 release) followed; Phase 18 then opened. |
| [`docs/state/2026-05-22-semantic-suite-revival.md`](docs/state/2026-05-22-semantic-suite-revival.md) | Prior — **semantic suite revival shipped 2026-05-22 late PM.** New `sov drive` subcommand restores the headless line-driven conversation surface that semantic tests need (M13 collapsed the readline REPL into the TUI launcher, which crashes on non-TTY stdin). Drive boots the same Hono server the TUI talks to, reads stdin line-by-line, emits plain-text events to stdout. Reconnects SSE after each turn (server closes on turn_complete). Auto-POSTs to `/approvals` on permission_request, auto-sends `promptToSend` for prompt-type slash commands. Also fixed an M10.5-era bug where `/init` + `/commit` + skill-sourced commands interpolated `ContentBlock[]` as `[object Object]`. Semantic suite re-baselined at **55/58 pass** against the installed binary (3 known flakes — all model-behavior or test-design issues, documented in the state file). TS suite **1996/0/14** (+14: 11 driveCommand + 3 promptToSend). |
| [`docs/state/2026-05-22-tui-tool-call-abstraction.md`](docs/state/2026-05-22-tui-tool-call-abstraction.md) | Prior — **TUI tool-call abstraction + two surgical UX fixes shipped 2026-05-22 PM.** Default tool rendering flipped to one-liner per `tool_result` (`Verb target stats ›`, Claude mobile aesthetic). New `packages/tui/internal/components/compactline.go` owns the verb mapping. `ui.toolOutput.mode = 'detailed'` opts back into the bordered ToolCard (output capped to `inlineLines`, default 10). `-v / --verbose` wired again — forwards as `--verbose-raw` (orthogonal raw escape hatch). Glyphs: `⚠` for permission-denied, `✗` for runtime errors. Fix A removed the `tui server listening` stderr boot line; Fix B switched dark chroma style to `monokai` for visible code highlighting. Spec at `docs/specs/2026-05-22-tui-tool-call-abstraction-design.md`. TS **1982/0/14** (+10); Go all packages green (+29 new tests); lint+typecheck clean. No new ADRs (post-close-out polish). |
| [`docs/state/2026-05-22-phase-21-m1.md`](docs/state/2026-05-22-phase-21-m1.md) | Prior — **Phase 21 M1 shipped 2026-05-22 AM.** Binary distribution: per-platform Bun `--compile` + Go cross-compile + side-car `bundle-default/` + public `github.com/yevgetman/sov-releases` repo with `curl ... | bash` installer. **Two releases shipped: `v0.2.0` (initial) + `v0.2.1` (splash/MCP version fix; current latest).** Tarball sizes: darwin-arm64 31.9 MB / darwin-x64 34.8 MB / linux-x64 47.5 MB; SHA256SUMS verified. Runtime: `shippedBundlePath()` + `findTuiBinary()` learn `process.execPath`-relative discovery; `sov upgrade` auto-detects binary mode + re-runs public installer; `src/version.ts` switched to build-time JSON import; harness version threaded into splash via `--harness-version` flag. ADRs P21-A + P21-B. TS suite **1972/0/14** (+14 from baseline). macOS smoke PASS; **end-to-end `sov upgrade` 0.2.0→0.2.1 PASS**; linux-x64 binary structural check PASS; linux container smoke deferred (Docker daemon issue — re-run separately); darwin-x64 deferred to first Intel-Mac beta user. |
| [`docs/state/2026-05-21-ux-fixes-r5.md`](docs/state/2026-05-21-ux-fixes-r5.md) | Prior ux-fixes round 5 snapshot — inline-mode refactor: drop `tea.WithAltScreen()` + mouse capture → terminal owns scrollback (wheel + trackpad scroll) and text selection natively. Committed history flows via `tea.Println`; View() is just a bottom live region (`LiveRegion` at `packages/tui/internal/components/liveregion.go`) + prompt + status. Round 3 fixed textbox auto-grow / splash polish / spinner alignment / markdown wrap. Round 4 added paste abstraction + ESC turn-cancel + prompt prefix first-line-only. TS suite green at **1955/0/14**; Go all packages green. Phase 16.1 stays closed. |
| [`docs/state/2026-05-20-m13.md`](docs/state/2026-05-20-m13.md) | Prior M13 close-out snapshot — **Phase 16.1 M13 shipped; Phase 16.1 closed.** terminalRepl removal complete: deleted `src/ui/terminalRepl.ts` (2334 LoC) + 9 REPL-only `src/ui/*` modules + their tests + M12 deprecation infra + M11 surface resolver + readline asker bits from `permissions/prompt.ts`. Dropped `--ui` flag + `SOV_UI` env + `ui.surface` config field. main.ts boot flow collapsed ~65 → ~13 lines. Missing binary = hard error (no fallback surface). **4 ADRs M13-01..04; suite green at 1949/1949**; 4/4 smoke pass; 4-Opus parity audit clean. |
| [`docs/state/2026-05-20-m13-smoke/`](docs/state/2026-05-20-m13-smoke/) | M13 smoke output — 4 boot-decision scenarios (default-TUI boot / missing-binary hard error / Commander rejects unknown `--ui` flag / `sov dispatch` headless round-trip). |
| [`docs/state/2026-05-19-m12.md`](docs/state/2026-05-19-m12.md) | Prior M12 close-out — Phase 16.1 M12 shipped (REPL deprecation warning: stderr line on explicit `--ui repl` / `SOV_UI=repl` / `ui.surface=repl`; silent on missing-binary fallback; `SOV_NO_DEPRECATION_WARNING=1` suppresses; **2 ADRs M12-01..02; suite green at 2073/2073**; 6/6 boot smoke pass). |
| [`docs/state/2026-05-19-m12-smoke/`](docs/state/2026-05-19-m12-smoke/) | M12 smoke output — 6 boot-decision scenarios asserting deprecation presence/absence across opt-in sources, suppression flag, missing-binary fallback, default-TUI. |
| [`docs/state/2026-05-19-m11-5.md`](docs/state/2026-05-19-m11-5.md) | Prior M11.5 close-out — Phase 16.1 M11.5 shipped (inline picker card: `pickerOpen` side-effect protocol; new `PickerCard` Go component; `/model`, `/resume`, `/export` migrated; T8 spacing fix; **3 ADRs M11.5-01..03; suite green at 2061/2061**; real-Anthropic smoke 2/2 pass). |
| [`docs/state/2026-05-19-m11-5-smoke/`](docs/state/2026-05-19-m11-5-smoke/) | M11.5 smoke output — 2 real-Anthropic picker round-trip scenarios + README summary. |
| [`docs/state/2026-05-17-m11.md`](docs/state/2026-05-17-m11.md) | Prior M11 close-out — Phase 16.1 M11 shipped (default-flip: `--ui` defaults to `'tui'`; new surface resolver with CLI > env > config > default precedence; auto-fallback to REPL when sov-tui missing; **3 ADRs M11-01..03; suite green at 2033/2033**; audit PASS-with-followups). |
| [`docs/state/2026-05-17-m11-parity-audit.md`](docs/state/2026-05-17-m11-parity-audit.md) | The M11 audit report — Opus subagent re-audit per Postmortem Rule 3 verifying M10 HIGH gaps remain closed and M11 code surface introduces no new HIGH/CRITICAL/MEDIUM gaps. PASS-with-followups (1 LOW fixed inline). |
| [`docs/state/2026-05-17-m11-smoke/`](docs/state/2026-05-17-m11-smoke/) | M11 smoke output — 13 boot-decision scenarios (`run-smoke.ts`) + 1 real-Anthropic dispatcher rerun + README summary. |
| [`docs/state/2026-05-16-m10-5.md`](docs/state/2026-05-16-m10-5.md) | Prior M10.5 close-out (predecessor to M11): slash-command dispatcher route + Go TUI slash router; closed #40; unblocked M11. |
| [`docs/state/2026-05-16-m10.md`](docs/state/2026-05-16-m10.md) | M10 close-out: parity audit by 4 parallel Opus subagents; 2 HIGH gaps fixed inline (HarnessInfoTool + repair-missing-tool-results); 1 HIGH scope-bounded (mission CLI-only); 1 HIGH deferred (closed by M10.5); 4 ADRs M10-01..04. |
| [`docs/state/2026-05-16-tui-parity-audit.md`](docs/state/2026-05-16-tui-parity-audit.md) | The M10 audit report itself — slice-by-slice findings, severity classification, fixes-applied, M11 disposition. Postmortem Rule 3 attestation. |
| [`docs/state/archive/`](docs/state/archive/) | Historical snapshots: `2026-05-07.md` (Phase 13.4), `2026-05-11.md` (Phase 16.0a), `2026-05-12.md` (Phase 16 revert), `2026-05-13.md` (Phase 16.1 M0–M3), `2026-05-14.md` (Phase 16.1 M4 + M5 + M5.1), `2026-05-14-pm.md` (Phase 16.1 M6 close-out + 2026-05-15 hardening + autonomous M6 smoke + PM #32/#37), `2026-05-15.md` (Phase 16.1 M7 close-out + post-close-out hardening + autonomous M7 smoke), `2026-05-16.md` (Phase 16.1 M8 close-out), `2026-05-16-m9.md` (Phase 16.1 M9 close-out), `2026-05-16-m9-5.md` (Phase 16.1 M9.5 close-out), `2026-05-16.md` again (Phase 16.1 M9.6 close-out). |

### Forward-looking

| File | What's in it |
|---|---|
| [`docs/backlog/post-phase-13-4.md`](docs/backlog/post-phase-13-4.md) | Open backlog (2 items): **#17 eval-gated auto-promote (P4, conditional)**; **#49 Node-20 GitHub Actions deprecation (P3, CI-maintenance — `actions/checkout@v4` forced to Node 24 from 2026-06-16)**. #48 (Phase 21 M2 release automation) closed 2026-05-25 — shipped as v0.6.0. #47 closed 2026-05-22 evening (dead `transcript.go` removed). #40 closed M10.5; #41 + #43 + #44 + #45 + #46 closed 2026-05-19; #29 / #38 / #39 closed 2026-05-19. **Phase 16.1 closed with M13 (2026-05-20); Phase 21 M1 closed 2026-05-22; Phase 17 (cron / scheduled jobs) closed 2026-05-22 evening; Phase 18 (OpenAI HTTP API server) closed 2026-05-23; Phase 1 + Phase 2 (multi-provider task routing) closed 2026-05-23; Phase 21 M2 (release automation) closed 2026-05-25.** Post-M2: v0.6.1 → v0.6.13 hardening/UX releases (no new phase) — see the post-M2 state snapshot. |
| [`docs/backlog/phase-16-rebuild-prereqs.md`](docs/backlog/phase-16-rebuild-prereqs.md) | 24 subsystems any Phase 16.1 foreground refactor must re-wire — **24/24 complete after M8; M9 visual polish landed 2026-05-16; M10 audit verified independently with caveat that slash-command-stack composition (#40) was not covered by the original prereq methodology** |
| [`docs/specs/2026-05-13-phase-16-1-tui-rebuild-design.md`](docs/specs/2026-05-13-phase-16-1-tui-rebuild-design.md) | Design spec for the Phase 16.1 TUI rebuild (executed; Phase 16.1 closed at M13, 2026-05-20) |
| [`docs/specs/2026-05-13-production-harness-roadmap-design.md`](docs/specs/2026-05-13-production-harness-roadmap-design.md) | Umbrella production polish roadmap |
| [`docs/specs/2026-05-16-phase-16-1-m10-parity-audit-design.md`](docs/specs/2026-05-16-phase-16-1-m10-parity-audit-design.md) | M10 audit design spec |
| [`docs/specs/2026-05-16-phase-16-1-m10-5-slash-dispatcher-design.md`](docs/specs/2026-05-16-phase-16-1-m10-5-slash-dispatcher-design.md) | M10.5 slash-dispatcher design spec |
| [`docs/specs/2026-05-17-phase-16-1-m11-default-flip-design.md`](docs/specs/2026-05-17-phase-16-1-m11-default-flip-design.md) | M11 default-flip design spec |
| [`docs/specs/2026-05-19-phase-16-1-m11-5-inline-picker-card-design.md`](docs/specs/2026-05-19-phase-16-1-m11-5-inline-picker-card-design.md) | M11.5 inline picker card design spec |
| [`docs/specs/2026-05-19-phase-16-1-m12-repl-deprecation-design.md`](docs/specs/2026-05-19-phase-16-1-m12-repl-deprecation-design.md) | M12 REPL deprecation design spec |
| [`docs/specs/2026-05-19-phase-16-1-m13-terminalrepl-removal-design.md`](docs/specs/2026-05-19-phase-16-1-m13-terminalrepl-removal-design.md) | M13 terminalRepl removal design spec (just shipped — Phase 16.1 closed) |
| [`docs/specs/2026-05-21-binary-distribution-design.md`](docs/specs/2026-05-21-binary-distribution-design.md) | Phase 21 binary distribution design — `bun build --compile` + Go cross-compile + public `sov-releases` repo + one-line installer script; M1 manual pipeline, M2 GitHub Actions automation |
| [`docs/plans/`](docs/plans/) | Implementation plans (executed; left as record). Latest: `2026-05-24-phase-21-m2-release-automation.md` (Phase 21 M2 — GitHub Actions release automation; shipped + closed 2026-05-25). |

### Postmortems — required reading before similar work

| File | When to read |
|---|---|
| [`docs/postmortems/2026-05-12-phase-16-revert.md`](docs/postmortems/2026-05-12-phase-16-revert.md) | **Before any future foreground-surface refactor.** Rules 1–4. |
| [`docs/postmortems/loop-detector-orphaned-tool-use.md`](docs/postmortems/loop-detector-orphaned-tool-use.md) | When debugging tool_use/tool_result lifecycle bugs |

### Operational log

| File | What's in it |
|---|---|
| [`docs/testing-log.md`](docs/testing-log.md) | Append-only testing log (newest-first). Per the testing-log rule, every testing pass must be logged here |

### Source-adjacent

The `src/bundle/` subdirectory carries its own README — `src/bundle/README.md` — for surface-specific context that doesn't belong in the top-level docs.

## Hard rules

These apply every session and override defaults:

- **Subagent model policy** — Opus 4.7 default; Sonnet 4.6 only for trivially mechanical fully-specified tasks; **never Haiku**. Details: [`docs/conventions/subagent-policy.md`](docs/conventions/subagent-policy.md).
- **Pre-commit gate** — `bun run lint && bun run typecheck && bun run test`. All three. Details: [`docs/conventions/lint-and-commit.md`](docs/conventions/lint-and-commit.md).
- **Atomic commits + autonomous push** — one logical change per commit; push `origin/master` without asking. Same rule as the docs repo.
- **`sov upgrade` after runtime changes** — any `src/`, `bundle-default/`, or `packages/tui/` change. Details: [`docs/conventions/sov-upgrade.md`](docs/conventions/sov-upgrade.md).
- **Testing log obligation** — append to `docs/testing-log.md` for every testing pass. Details: [`docs/conventions/testing-log.md`](docs/conventions/testing-log.md).
- **No week-based estimates** — sessions / dispatches / wall-minutes only. Details: [`docs/conventions/estimation.md`](docs/conventions/estimation.md).
- **TUI style guide** — all spacing, padding, glyphs, brand colors, and typography in `packages/tui/` MUST reference `style.S.*` from `packages/tui/internal/style/`. Never hardcode layout values in components. Details: [`docs/conventions/tui-style-guide.md`](docs/conventions/tui-style-guide.md).
- **Plans and specs paths** — `docs/plans/YYYY-MM-DD-<feature>.md`, `docs/specs/YYYY-MM-DD-<topic>-design.md`. Never `docs/superpowers/`.
- **AGENTS.md ≡ CLAUDE.md** — byte-identical mirror. Verify with `diff` before commit.

## Don't

- Don't relitigate the 9 locked design principles ([`docs/design-principles.md`](docs/design-principles.md)).
- Don't put product-specific content under `src/` — Sovereign-AI-specific content belongs in the bundle.
- Don't delete empty `src/` subdirectories — they mark phase landing zones.
- Don't bypass the pre-commit gate with `--no-verify` unless you can name why.
- Don't dump content into `CLAUDE.md` — extend a conventions file and link to it.
