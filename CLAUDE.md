# Instructions for Claude Code sessions developing this repo

You are working on the **Sovereign AI agent runtime** — TypeScript code, not documents. This repo is a Claude-Code-style harness (per ADR H-0003 in the sister `sovereign-ai-docs` repo) that reads a *harness bundle* (the docs repo, or a client's extracted bundle) and drives an LLM conversation against it.

Business context lives in `~/code/sovereign-ai-docs/`. This repo contains code and code conventions only.

This file is a **lean index** — a table of contents into the deeper docs. Read only what your current task requires. Don't try to load it all at boot.

---

## ⚠️ ACTIVE FOCUS — Learning-loop soak (TEMPORARY — the founder will say when to remove this)

**Read this first. For the next several sessions the learning loop is the #1 thing in motion — keep it front of mind during all work, whatever the task.**

**What's live (harness v0.6.16, 2026-06-04):** the learning loop is **closed and running by default**. The portable four-port learning layer (`src/learning-layer/` — Observe / Recall / Reason / Persist, per ADR H-0010) is wired into the runtime:
- **Recall is ON by default** (`learning.recall.enabled`): before each turn (server / TUI / `sov drive`), relevant synthesized *instincts* are spliced into the latest user message as a `<learned-context>` block. Fail-open; a no-op when the corpus is empty.
- **Capture + synthesis are ON** (`learning.disabled: false`): tool use is observed every session; synthesis runs in the background and writes instincts to the corpus.

**Proven vs not (be precise):** Phase 1 cleared the spike's *scripted-scenario* bar — a recalled lesson measurably changes behavior (5/5 curated scenarios, **3/3 reps each, 0 regressions, 0 variance**; one full end-to-end run; real-corpus synthesis yields useful instincts). That proves the **mechanism under controlled conditions — NOT real-world or longitudinal value.** **This soak exists to test exactly that:** does the loop actually help in real day-to-day use as the corpus accrues depth? Do not describe it as "proven" beyond the mechanism.

**What to look for during normal work — and surface to the founder:**
- **Did recall help or hurt?** If a `<learned-context>` block was injected, was the instinct relevant + correct, and did it change what you did for better or worse? Flag any recall that was irrelevant, stale, wrong, or harmful.
- **Synthesis quality:** when instincts get synthesized, are they specific + correct or trivial/noisy? (Known caveat: cluster keys are coarse — the synthesizer LLM carries the specificity.)
- **Corpus growth + depth:** `sov learning status`. (Known caveat: only one project has real depth so far — payoff stays thin until depth accrues.)
- Anything surprising about the loop's behavior. **Record observations in `docs/testing-log.md`** and tell the founder.

**Do NOT:** disable recall or learning (they are intentionally on for the soak); decide the founder-reserved calls — the **Phase-2 rented engine** (TS-vs-Python is a *major* decision), the **go/no-go**, or **auto-promote-by-default**.

**Deeper context:** state snapshot `docs/state/2026-06-04-learning-loop-spike-phase-1.md` · spec `docs/specs/2026-06-03-portable-learning-layer-adapter-1-design.md` · plan `docs/plans/2026-06-03-learning-loop-spike-phase-1.md` · evals `bun run eval:learning` + `bun run eval:synthesis-audit` · canonical open-question `learning-loop-closure-and-proof` (still **OPEN**) in `~/code/sovereign-ai-docs`.

---

## Session boot

1. **This file** (`CLAUDE.md`) — index and standing rules
2. **`README.md`** — repo intro, install, layout
3. **`docs/state/2026-06-09-plugin-system-v1.md`** — most recent close-out snapshot (**Plugin System v1 — consent-gated skill/command packs shipped; arc D of harness ecosystem-openness**). Latest release **v0.6.35** (2026-06-09). A new `src/plugins/` module installs a Claude-Code-format **skills + slash-commands** pack as one **consent-gated, integrity-hashed** unit; a richer CC plugin's hooks/MCP/agents are **disclosed but inert** (deferred to v2/v3). Three load-bearing controls: **S1** load-time consent gate + whole-tree SHA-256 integrity, re-verified **every boot** (no valid hash-matching `.consent.json` ⇒ the plugin is inert + flagged; a hand-dropped plugin contributes nothing); **S2** inline-shell expansion **disabled** for `source:'plugin'` skills (forced at the loader chokepoint + double-gated in `expandSkillText`); **S3** `installPlugin` is the only `writeConsent` minter and `/plugins install` is **TTY-only** (the server/gateway/channel command contexts never set `confirm`, so install is unreachable from any remote surface). Precedence `project > user > plugin > bundle` (a plugin can override a bundle skill but never shadow a user/project one); built-ins beat plugin commands; `${CLAUDE_PLUGIN_ROOT}` threaded into plugin skill/command bodies; opt-in `plugins: { enabled?, disabled? }`; restart-to-apply. Built **subagent-driven** (Opus implementer + spec-compliance + code-quality review per task; dedicated adversarial security reviews on the consent/inline-shell/install trio; **whole-feature adversarial review at T9 → SECURE-TO-SHIP**, no Critical/High — every cross-task attack blocked in the integrated, wired system). Notable catches fixed pre-ship: a symlink-to-dir EISDIR boot-DoS (T3), proven C2 plugin-skill shell-exec (T5, RED→GREEN), and a disclosure-fidelity gap where a malicious reference file was invisible at consent but blocked at load (T6). Prerequisite: **backlog #55 fixed** — `buildRuntime` now threads `harnessHome` into `SessionDb.open` + `readConfig` (was opening the global 1.18 GB `sessions.db` → contention → the learning-test false fails; now resolved). Gate: lint/typecheck clean, **3555 pass / 0 fail / 16 skip** (clean `HARNESS_HOME`). **This snapshot is ALSO the catch-up** for the un-snapshotted wave since Phase F (v0.6.23): SMS channel (v0.6.26), subscription-executor spike (v0.6.27, bypass-default v0.6.32), CC skill import + tool-scope enforcement (v0.6.29 — ecosystem-openness arcs A+B), remote MCP transport (v0.6.30 — arc C), the keyless `sov` local-engine provider Bucket A (v0.6.34), and the v0.6.33 CRITICAL infinite-turn SSE re-stream fix. No new ADRs, no bundle changes (built per ADR H-0010, canonical in `sovereign-ai-docs`). **Cross-repo `sovereign-ai-docs` sync (the whole post-Phase-F wave + Plugin v1) still pending** for a docs session. The **learning-loop soak continues, untouched** — recall ON by default since v0.6.16; the #55 fix de-races its observer — the standing #1 ACTIVE-FOCUS track (see the banner above). Predecessor: `docs/state/2026-06-06-phase-f-channels.md` (Phase F — channels; run-anywhere roadmap A–F COMPLETE; v0.6.23). Replaced each session — find the latest via `ls docs/state/*.md | sort -r | head -1`.
4. **`docs/backlog/post-phase-13-4.md`** — open backlog items not in the canonical build plan.
5. **`~/code/sovereign-ai-docs/harness/docs/runtime/runtime-scaffold-plan.md`** — Phase-0/1 scaffold contract this repo was seeded against.
6. **`~/code/sovereign-ai-docs/harness/docs/runtime/harness-build-plan.md`** — canonical remaining phased plan.
7. **`~/code/sovereign-ai-docs/harness/docs/reference/agent-harness-design-lessons.md`** — unifying design principles and Claude Code reference lessons.
8. **`docs/specs/2026-06-03-portable-learning-layer-adapter-1-design.md`** + **`docs/plans/2026-06-03-learning-loop-spike-phase-1.md`** — the learning-loop spike's implementation spec (D1–D15) + Phase-1 plan (T1–T19). **Phase 1 is shipped (loop CLOSED; Q1 PASS) — see the state snapshot (item 3).** Read these before any Phase-2 learning work (the deferred items + founder-reserved decisions are listed in the snapshot). Background: `docs/plans/2026-06-03-learning-loop-spike-kickoff.md` (kickoff) + ADR H-0010 (canonical in `sovereign-ai-docs`).

`~/code/claude-code/src/` is the architectural reference. Look up specific patterns there when a design question comes up.

## Doc index

Each link is a chapter loaded on demand. Don't pre-read.

### Standing rules — operating conventions

| File | When to read |
|---|---|
| [`docs/conventions/autonomous-feature-builds.md`](docs/conventions/autonomous-feature-builds.md) | **Before any large feature build with a planning phase** — spec → plan → subagent-execute → ship, fully autonomous, NO approval gates; ends in docs + tests + commit/push + `sov upgrade` + skills + release-when-applicable |
| [`docs/conventions/lint-and-commit.md`](docs/conventions/lint-and-commit.md) | Before any commit — `lint`, `typecheck`, `test` all required; atomic commits; push autonomously |
| [`docs/conventions/sov-upgrade.md`](docs/conventions/sov-upgrade.md) | After any `src/` or `bundle-default/` or `packages/tui/` change — keep global binary current |
| [`docs/conventions/cutting-releases.md`](docs/conventions/cutting-releases.md) | After any `src/` / `bundle-default/` / `packages/tui/` change — cut the next binary release in the same session so `~/.sov/bin/sov` picks it up |
| [`docs/conventions/testing-log.md`](docs/conventions/testing-log.md) | Before testing — append-only log obligation |
| [`docs/conventions/semantic-tests.md`](docs/conventions/semantic-tests.md) | When triaging whether to run `bun run test:semantic` |
| [`docs/conventions/subagent-policy.md`](docs/conventions/subagent-policy.md) | Before dispatching any subagent — HARD RULE on Opus / Sonnet / never-Haiku |
| [`docs/conventions/estimation.md`](docs/conventions/estimation.md) | Before quoting effort/timeline — sessions/dispatches/wall-minutes, never weeks |
| [`docs/conventions/repo-layout.md`](docs/conventions/repo-layout.md) | Before adding files in `src/`, naming a plan/spec, or moving things |
| [`docs/conventions/tui-style-guide.md`](docs/conventions/tui-style-guide.md) | **Before ANY visual work in `packages/tui/`** — all spacing, padding, margins, borders, glyphs, brand colors, and typography tokens MUST come from `style.S.*`; never hardcode layout values in components |
| [`docs/conventions/tui-color-rendering.md`](docs/conventions/tui-color-rendering.md) | Before adjusting any text color in `packages/tui/` — body text MUST inherit terminal default; never assume a "bright" hex/ANSI value renders bright |
| [`docs/conventions/tui-ux-patterns.md`](docs/conventions/tui-ux-patterns.md) | Before changing TUI layout, chrome, or visual behavior — flow layout, splash, spinner, separator, tool events, file-ref auto-wrap, prompt/status styling |
| [`docs/conventions/visual-tui-qa.md`](docs/conventions/visual-tui-qa.md) | **Before evaluating any TUI UX visually** — drives sov-tui via VHS, renders PNG screenshots to `.harness/visual/output/`, agent `Read`s the result. `bun run visual [name]` |

### Design reference

| File | What's in it |
|---|---|
| [`docs/design-principles.md`](docs/design-principles.md) | The 9 locked design principles. Don't relitigate. |
| [`docs/architecture.md`](docs/architecture.md) | Current runtime flow — request lifecycle, system prompt, tools, permissions, persistence, sub-agents, microcompaction, REPL UX layers, learning + review pipelines, trajectory capture, OpenAI + native gateway server surfaces (auth, multi-client transport, supervisor, multi-user isolation, channels) |
| [`docs/usage.md`](docs/usage.md) | Day-to-day operation — CLI flags, subcommands, slash commands, eval suite, local-model router, profiles, providers, themes, web tools |
| [`docs/extending.md`](docs/extending.md) | Recipes for adding extension points — tools, providers, slash commands, skills, hooks, MCP servers, agents, permission rules, trajectory redaction |
| [`docs/semantic-testing.md`](docs/semantic-testing.md) | Semantic test framework — judge backends, suites, coverage inventory |

### Current state

| File | What's in it |
|---|---|
| [`docs/state/2026-06-09-plugin-system-v1.md`](docs/state/2026-06-09-plugin-system-v1.md) | **Canonical current-state snapshot — Plugin System v1: consent-gated skill/command packs shipped (arc D of harness ecosystem-openness).** Release **v0.6.35** (2026-06-09). A pure-aggregator `src/plugins/` module installs a Claude-Code-format **skills + slash-commands** pack as one **consent-gated, integrity-hashed** unit; a richer CC plugin's hooks/MCP/agents are **disclosed but inert** (deferred to v2/v3 per spec §5). **Three load-bearing controls** — **S1** load-time consent gate + whole-tree SHA-256 integrity (`loader.ts` `evaluateGate`→`verifyConsent`→`hashPluginTree`), re-verified at **every boot**: no valid hash-matching `.consent.json` (or a tree edited post-consent) ⇒ the plugin is inert + flagged `needs-consent`/`tampered`; directory-presence discovers but NEVER enables. **S2** inline-shell expansion **disabled** for `source:'plugin'` skills — forced at the skill-loader chokepoint (`allowShellInterpolation:false`, not manifest-controlled) + double-gated in `expandSkillText` (`&& source !== 'plugin'`); proven (C2 RED→GREEN: a plugin skill's `` `!cmd` `` executed pre-fix, inert after). **S3** `installPlugin` is the sole `writeConsent` minter; `/plugins install` runs every gate (manifest secret-scan, M1 path-containment, symlink-reject, guard-scan of skills/commands + bundled scripts) **before** the capability disclosure + `y/N` consent, and is **TTY-only** — the server/gateway/channel command contexts never set `confirm`, so install is unreachable from any remote surface. **Composition** — plugin skills splice `project > user > plugin > bundle` (override a bundle skill, never shadow a user/project one); built-in slash commands beat plugin commands (first-wins registry); `${CLAUDE_PLUGIN_ROOT}` resolves to the install dir in plugin bodies (carried on the `Skill`, preserved through `reloadSkill`); opt-in `plugins: { enabled?, disabled? }` (`.strict()`; `disabled` wins); restart-to-apply. **Wiring** — `loadPluginRuntime` (`loadPlugins`+`composePluginContributions`) into `buildRuntime` + `dispatchCommand`; `extraRoots` into `loadSkills`; plugin commands into both command seams; plugins surfaced in HarnessInfo. **Built subagent-driven** — Opus implementer + spec-compliance + code-quality review per task; dedicated adversarial security reviews on the consent/inline-shell/install trio; **whole-feature adversarial review at T9 → SECURE-TO-SHIP** (no Critical/High; every cross-task / emergent attack blocked in the integrated system — drop→invoke, post-consent tamper, cross-seam shell laundering, command-precedence shadowing, remote-surface consent minting, multi-user/channel leakage, DoS). Pre-ship catches fixed: a symlink-to-dir EISDIR boot-DoS (T3 `c873f68`), the C2 shell-exec (T5), and a disclosure-fidelity gap — a malicious reference file invisible at consent but blocked at load (T6 `8aa437a`, install now reuses the loader's aggregating `guardSkillLoad`). **Prerequisite — backlog #55 fixed** (`79a1b7f`): `buildRuntime` now threads `harnessHome` into `SessionDb.open` + `readConfig` (was opening the global 1.18 GB `sessions.db` → contention → the learning-test false fails). **Gate:** lint clean (740 files) + typecheck clean + **3555 pass / 0 fail / 16 skip** (clean `HARNESS_HOME`). Spec `docs/specs/2026-06-08-plugin-system-design.md`, plan `docs/plans/2026-06-08-plugin-system-v1.md`. Docs: `usage.md`/`architecture.md`/`extending.md`. No new ADRs, no bundle changes (built per ADR H-0010, canonical in `sovereign-ai-docs`). **This snapshot is also the catch-up** for the un-snapshotted post-Phase-F wave (SMS v0.6.26, subscription-executor v0.6.27/0.6.32, CC skill import + tool-scope v0.6.29, remote MCP v0.6.30, `sov` provider Bucket A v0.6.34, the v0.6.33 SSE fix). **Cross-repo `sovereign-ai-docs` sync still pending.** The **learning-loop soak continues, untouched** — the standing #1 ACTIVE-FOCUS track; the #55 fix de-races its observer. |
| [`docs/state/2026-06-06-phase-f-channels.md`](docs/state/2026-06-06-phase-f-channels.md) | **Prior — Phase F: Channel framework + Slack/Telegram/webhook adapters shipped; inbound channels drive isolated, safe-by-default harness sessions on the gateway. THIS COMPLETES THE RUN-ANYWHERE ROADMAP (A–F).** Release **v0.6.23** (2026-06-06). The sixth and final module (M6 + M7) of the **run-anywhere harness roadmap** (`docs/specs/2026-06-05-run-anywhere-harness-roadmap-design.md`): activates the dormant `src/channels/` contract into a working **inbound → session → headless turn → outbound** pipeline hosted by the gateway — a **Slack / Telegram / generic-webhook message drives a real harness session and gets a reply**, each channel an isolated Phase-E principal under a safe-by-default permission posture. **Framework (F-T2; `src/channels/pipeline.ts`)** — `runChannelTurn({ runtime, msg, principalId, permissionMode? })`: deterministic per-`(channel,sender)` session via `buildSessionKey`+`runtime.sessionDb.upsertSession({ owner: principalId, platform, metadata.kind:'channel' })` (find-or-create → continuous conversation, owned by the channel principal → Phase-E-isolated memory/learning); persists the inbound message; **hydrates prior history** (`loadHistoryAsMessages` + `repairMissingToolResults`, F-T2 follow-up `eb67031`) so multi-message conversations are coherent; runs ONE headless turn (cron pattern: `AgentRunner`, tool pool minus `SUBAGENT_EXCLUDED_TOOLS`, drain, `extractFinalText`); persists the assistant turn; returns `{ text }` / `{ silent }`; `disposeSession` in `finally` (DB row persists; supervisor evicts idle). **Security posture (X1 — the crux; F-T1; `src/channels/permission.ts`)** — `buildChannelCanUseTool` NEVER calls `loadPermissionSettings` (no local-allow inheritance — a remote sender can't ride `allow: Bash(*)`) + `ask = () => 'deny'` (auto-deny), so `Bash`/`Write`/`Edit` are denied by default while read-only tools run; `assertChannelPermissionMode` rejects `'bypass'` (remote bypass = RCE). **Stricter than cron** (cron loads the local layers; a channel message is untrusted). **Three adapters over injectable transports** — **generic webhook (F-T4/keystone; `adapters/webhook.ts`)** `POST /channels/webhook/:id`, HMAC-SHA256 constant-time-verified over the raw body (`X-Signature: sha256=<hex>`), synchronous `{ reply }` (bad sig → 401, bad body → 400); **Telegram (F-T5; `adapters/telegram.ts`)** `getUpdates` long-poll over an `unref`'d loop (no public endpoint), offset advances past every update, per-update try/catch, `sendMessage` reply; **Slack (F-T6; `adapters/slack.ts`)** `POST /channels/slack/events`, `v0=` signing-secret HMAC over `v0:{ts}:{rawBody}` + 300 s replay window + `url_verification` challenge + **ack-fast-then-async** `chat.postMessage` + `event_id` retry dedupe. **Config (F-T3; `src/config/schema.ts`)** — `gateway.channels: { webhook?, telegram?, slack? }`, each `{ enabled?, principalId, <secret(s)>?, permissionMode? }`; `permissionMode` enum `['default','ask']` (so `bypass` is a parse error); `superRefine` requires each enabled channel's secret(s) present (env-merged pre-parse) + `principalId` ∈ `gateway.principals`. Secrets env-first (`SOV_WEBHOOK_SECRET` / `SOV_TELEGRAM_BOT_TOKEN` / `SOV_SLACK_SIGNING_SECRET`+`SOV_SLACK_BOT_TOKEN`), never logged. **Gateway hosting (F-T7; `src/channels/listeners.ts`, `src/server/routes/channels.ts`, `gatewayCommand.ts`)** — webhook + Slack mount **OPEN** inbound routes (before the `/sessions/*` bearer/principal auth, like `/health`; verified by the channel's own credential); `resolveChannelsConfig` resolves env secrets pre-parse + fails boot loudly on a missing one; Telegram is a poll worker started after the supervisor + `stop()`ped before `runtime.dispose()`. **Security — found + fixed a CRITICAL (F-T8/`260de9f`):** a **path-traversal arbitrary-file-write** — an attacker-controlled webhook `chatId` with `../` flowed through `buildSessionKey` into the trace FILENAME, escaping the trace dir before the model ran. Fixed **defense-in-depth at both boundaries**: source (`parseWebhook` validates `sender`/`chatId`/`threadId` against a safe-segment allowlist → 400) + sink (`TraceWriter` in `src/trace/writer.ts` collapses `..`, replaces separators/control chars, and containment-asserts the resolved path stays under the traces dir). **Re-reviewed SECURE-TO-SHIP** (no remaining Critical/High; posture holds — proven with a seeded local `allow Bash(*)`; auth constant-time + replay-resistant; channel→principal isolation holds; secrets never logged). **Known v1 limitations (documented, not fixed):** (a) channel turns uncompacted (a very long conversation could overflow context — same as cron); (b) channel sessions use colon-delimited ids → not individually `/sessions/:id*`-addressable (fail-closed/secure, channel-managed, rows accrue like cron); (c) LOW hardening follow-ups (no inbound body-size cap, gateway-wide; Slack/Telegram inbound ids rely on the airtight trace-sink sanitizer rather than source-level validation); (d) live Slack/Telegram need real external credentials (operator setup in `usage.md`, **not live-verified here** — built against injected transports + a real-HMAC webhook e2e); (e) a minor `StartServerOptions.channels` type carries the `telegram` key (harmless, structural). TS suite **~3067 pass / 0 fail / 14 skip** (clean). Post-upgrade binary smoke proved channels ship in the binary (valid webhook HMAC → reply; bad HMAC → 401). No new ADRs, no bundle changes (built per ADR H-0010, canonical in `sovereign-ai-docs`). **A–F of the roadmap all shipped — the run-anywhere program is COMPLETE.** **Cross-repo `sovereign-ai-docs` sync (D + E + F shipped + roadmap COMPLETE) still pending** for a future docs session. **Learning-loop soak continues in parallel, untouched** (recall ON by default since v0.6.16 + capture on — the standing #1 ACTIVE-FOCUS track; this gateway track is separate). |
| [`docs/state/2026-06-06-phase-e-multi-user.md`](docs/state/2026-06-06-phase-e-multi-user.md) | **Prior — Phase E: Multi-user identity + state scoping shipped; named principals with isolated sessions, memory, and learning, served by the gateway.** Release **v0.6.22** (2026-06-06). The fifth phase (modules M4 + M5) of the **run-anywhere harness roadmap** (`docs/specs/2026-06-05-run-anywhere-harness-roadmap-design.md`): makes the long-lived `sov gateway` **multi-user** — multiple distinct, named users share one self-hosted gateway with isolated sessions, memory, and learning (resolves the standing `multi-user-memory-scoping` blocker). **Principals registry (E-T1; `src/server/principals.ts`, `src/config/schema.ts`)** — `gateway.principals: [{ id, token, name? }]`, **XOR** the single `gateway.token` (schema refinement rejects both); each `id` validated as a safe path segment (`^[A-Za-z0-9_-]+$`), tokens non-empty + unique. `resolvePrincipal(token, principals)` constant-time-compares against **every** principal (no early-exit timing leak) → `{ id, name? } | null`. **Principal-aware auth (E-T2; `src/server/auth.ts`, `app.ts`, `index.ts`, `gatewayCommand.ts`)** — `principalAuth(principals)` resolves the bearer token, 401 on no match, attaches the principal to the Hono context. **No anonymous bypass** — in principals mode a resolving token is required on **every** request, including loopback (the implicit single principal / legacy paths apply only in single-`token`/no-auth mode). **Session ownership (E-T3/E-T4; `src/agent/sessionDb.ts`, `src/server/routes/{sessions,turns,events,approvals}.ts`)** — nullable `owner_id` column (migration **4→5**, indexed `(owner_id, last_updated DESC)`); `POST /sessions` stamps the caller; **every `/sessions/:id/*` route returns 404 on owner mismatch** (existence-hiding, not 403 — the chokepoint covers messages/turns/events/approvals/cancel/compact/commands/skills/DELETE, checked before any per-session state is built); `GET /sessions` owner-filtered. **Per-user state (E-T5/E-T6; `src/memory/*`, `src/learning/*`, `src/learning-layer/`, `src/server/sessionContext.ts`)** — memory under `$HARNESS_HOME/users/{id}/memory/…` + learning under `…/users/{id}/learning/{projectId}/…`, **derived from the session's `owner_id`** (never caller input), re-validated as a safe segment at the path boundary; the implicit single principal keeps the existing top-level paths (byte-identical). **Two-layer isolation (D6)** — authz (route ownership) + owner-derived scoping (memory/learning namespace from the session owner); both must hold. **Within-org / single-trust-domain scope** (trusted-but-separate users); **hostile cross-tenant / managed-multi-tenant isolation is OUT (founder-reserved).** **Adversarial security review (E-T8)** found + fixed **two cross-user leaks** before ship (`d7559a8`): **C1 (Critical)** — the `memory` tool ignored the user scope (dropped `ctx.userId` → shared legacy store); **H1 (High)** — the compaction child dropped the owner (post-compaction `getSessionContext(childId)` rebuilt with `ownerId=null` → legacy namespace). A sibling-bug sweep found none. **Final verdict: SECURE-TO-SHIP — A cannot reach B's sessions/memory/learning.** **Known v1 limitations (documented, not fixed):** operator-side traces + fine-tune trajectories aren't per-user-partitioned (operator-only, never API-served); the admin learning CLI (`learningStatus/Export/Prune`) operates on the legacy top-level corpus; a synthesizer prompt-label cosmetically shows the legacy observations path (display-only, no data exposure); `timingSafeEqual` early-returns on length mismatch (pre-existing, codebase-wide, acceptable within the non-hostile within-org model); plus the **E-T4 tightening** (events/approvals now require the session to exist in the DB). TS suite **~2957 pass / 0 fail / 14 skip** (clean). Post-upgrade binary smoke proved the multi-user 404 isolation ships in the binary. No new ADRs, no bundle changes (built per ADR H-0010, canonical in `sovereign-ai-docs`). **A/B/C/D/E of the roadmap shipped; F (channels) remains.** **Cross-repo `sovereign-ai-docs` sync (A/B/C/D/E shipped) still pending** for a future docs session. **Learning-loop soak continues in parallel, untouched** (recall ON by default since v0.6.16 + capture on — the standing #1 ACTIVE-FOCUS track; this gateway phase is separate; Phase E only gave the learning layer a per-user namespace). |
| [`docs/state/2026-06-06-phase-d-supervisor.md`](docs/state/2026-06-06-phase-d-supervisor.md) | **Prior — Phase D: Persistent multi-session supervisor shipped; idle eviction + session lifecycle, served by the gateway.** Release **v0.6.21** (2026-06-06). The fourth module (M3) of the **run-anywhere harness roadmap** (`docs/specs/2026-06-05-run-anywhere-harness-roadmap-design.md`): turns the long-lived `sov gateway` into a **persistent multi-session host** owning many concurrent sessions across clients + restarts. **`SessionSupervisor` (`src/server/sessionSupervisor.ts`)** — a periodic, `unref`'d idle sweep (mirrors `CronRunner`) that reclaims a session's in-memory state (`runtime.disposeSession` + `disposeBus`) only when it is **NOT turn-active**, has **NO SSE subscribers**, and is idle past `gateway.idleSessionTimeoutMs` (default **30m**), swept every `gateway.idleSweepIntervalMs` (default **5m**). Eviction is **graceful** (the dispose path flushes learning/trace/trajectory — the **learning corpus is written**) and **transparent** (the durable SQLite row stays; the next request **lazily rebuilds** the session from disk → restart-resume; an evicted session is indistinguishable from a live one bar one cold rebuild). **Lifecycle routes (`src/server/routes/sessions.ts`)** — `GET /sessions` (list + immutable `live`/`turnActive`/`subscribers` annotations via `peekBus`, `?limit` clamped `[1,100]`) + `DELETE /sessions/:id` (dispose context+bus → FK-safe `SessionDb.deleteSession` in `src/agent/sessionDb.ts` → **204**; 404 before any teardown on a miss). **`POST /sessions` concurrency cap** — opt-in `gateway.maxConcurrentSessions` (default **0 = unlimited**); at the ceiling it sweeps then re-checks, admits if room freed else **429** (`{error:"session capacity reached"}`); consulted via a structural `SessionSupervisorLike` so absent-supervisor ⇒ byte-unchanged create path. **Bus liveness surface (`src/server/eventBus.ts`)** — `getSubscriberCount`/`getLastActivityAt`/`isTurnActive` + module-level `peekBus`/`liveBusSessionIds`/`setDefaultRingSize`. **Config (`src/config/schema.ts`)** — three new optional gateway-scoped fields; boot banner prints the effective `idle-evict: reclaim sessions idle >30m every 5m; max-sessions: unlimited`. **Decision D1: extend the gateway** (the dormant `src/daemon/` skeleton stays dormant); the supervisor is constructed + `start()`ed in `runGateway` ONLY, so the **TUI / `sov serve` / `sov drive` paths are byte-unchanged**. **Hardening (`657f4e6`)** — `sweep()` retains an in-flight promise (re-entrant calls no-op rather than overlap) + an awaitable, idempotent `stop()` that clears the interval then drains the in-flight sweep, called BEFORE `runtime.dispose()` on SIGINT/SIGTERM (no DB-read-after-`sessionDb.close()` race; mirrors `CronRunner` stop-then-teardown). **Restart-resume proven** (lazy rebuild test). Reviewed **READY TO SHIP** (no Critical/High; dispose interaction safe; the shutdown race was LOW and is now closed). **Two known-LOWs (documented, not fixed):** a tiny TOCTOU in `sweep()` (a turn/subscribe landing during one eviction's `disposeSession` await — recoverable, gated by the 30-min idle floor); and a pre-existing `buildMockRuntime` test-isolation smell (opens the global `~/.harness/sessions.db`). Closes Phase B's deferred idle-bus-eviction (D9) + the interactive-buses-accumulate carry-forward. Ships a **systemd + launchd service-install** story (X2) in `usage.md`. TS suite **~2861 pass / 0 fail / 14 skip** (clean). No new ADRs, no bundle changes (built per ADR H-0010, canonical in `sovereign-ai-docs`). **A/B/C/D of the roadmap shipped; E (multi-user) + F (channels) remain.** **Cross-repo `sovereign-ai-docs` sync (A/B/C/D shipped) still pending** for a future docs session. **Learning-loop soak continues in parallel, untouched** (recall ON by default since v0.6.16 + capture on — the standing #1 ACTIVE-FOCUS track; this gateway phase is separate). |
| [`docs/state/2026-06-06-phase-c-webui.md`](docs/state/2026-06-06-phase-c-webui.md) | **Prior — Phase C: Reference Web UI shipped; embedded single-file browser client, served by the gateway.** Release **v0.6.20** (2026-06-06). The third module (M8) of the **run-anywhere harness roadmap** (`docs/specs/2026-06-05-run-anywhere-harness-roadmap-design.md`): a real, polished **browser chat client** served by the gateway, driving the rich native HTTP+SSE protocol. **Single self-contained client (D1)** — `src/server/webui.html`, inline CSS + vanilla JS, no framework, no build. **Served + embedded (D2)** — `src/server/webui.ts` inlines it via `import … with { type: 'text' }` (`WEB_UI_HTML`, `--compile`-safe like `version.ts`'s JSON import); `app.get('/', …)` + `app.get('/ui', …)` mounted in `src/server/app.ts` BEFORE the `/sessions/*` bearer-auth (open like `/health`); `buildAppWithRuntime` serves it. **Same-origin → no CORS (D3).** **Token UX (D4)** — connect screen prompts for the bearer token, stores it in `localStorage`, sends it as `Authorization` (never embedded in the served HTML), with a disconnect/forget control; loopback-no-token connects token-less. **Protocol = Phase A/B native (D5)** — `POST /sessions` (201) → `?follow` SSE via `fetch()`+`ReadableStream` reader → `POST /sessions/:id/turns` (202); captures each frame `id` (seq) + reconnects with `Last-Event-ID`; renders `text_delta`, collapsible `thinking_delta`, tool cards, `status_update`, `turn_complete`/`turn_error`. **Permission UI (D6)** — `permission_request` → inline Approve/Deny card (+ "always allow") → `POST /sessions/:id/approvals/:requestId`. **Scope v1 (D7)** — connect → new session → multi-turn streaming + tool cards + permission prompts + reconnect, plus new-chat + cancel. **Browser e2e (D8, Playwright, real model)** — connect+turn / tool-use→permission→Approve / reconnect all PASS, after finding & fixing the **SSE header-flush bug** (`a77ed32`: idle `?follow` stream wedged — Bun doesn't flush headers until the first body write → leading `: connected` comment frame in `src/server/routes/events.ts`); `tests/server/webui.test.ts` proves the open route serves the HTML. Reviewed **SECURE + CORRECT** (XSS-clean, token-safe, reconnect correct). **Two known-minor LOW follow-ups (non-blocking):** duplicate permission card on mid-turn reconnect-replay (cosmetic; second answer no-ops); SSE parser splits on `\n\n` (LF — fine vs this server; a CRLF gateway would leave a stray `\r`). TS suite **~2814 pass / 0 fail / 14 skip** (clean). No new ADRs, no bundle changes (built per ADR H-0010, canonical in `sovereign-ai-docs`). D–F of the roadmap remain (A→B→C is the first complete "run-anywhere from a browser" arc). **Earlier-flagged Phase-D item still pending:** interactive-session buses accumulate until shutdown (bounded per-session by the ring; idle/TTL eviction or `DELETE /sessions/:id` is Phase D's job). **Learning-loop soak continues in parallel, untouched** (recall ON by default since v0.6.16 + capture on — the standing #1 ACTIVE-FOCUS track; this gateway phase is separate). |
| [`docs/state/2026-06-05-phase-b-transport.md`](docs/state/2026-06-05-phase-b-transport.md) | **Prior — Phase B: Multi-Client Session Transport shipped; robust core + 4 hardening fixes.** Release **v0.6.19** (2026-06-05). The second module (M2) of the **run-anywhere harness roadmap** (`docs/specs/2026-06-05-run-anywhere-harness-roadmap-design.md`): makes the native HTTP+SSE session transport **multi-client + reconnect-safe**. **Multi-subscriber bus (D1)** — `src/server/eventBus.ts` replaces the single `subscriber` with a `Set` fanned out by `publish()`; multiple clients watch one session. **Bounded replay ring (D2)** — the drain-and-empty buffer becomes a ring retaining the last N events (N = `gateway.eventBufferSize`, default 512; invalid → clamped), oldest evicted. **`Last-Event-ID` reconnect (D3/D4)** — `subscribe(fn, { lastEventId })` replays ring events with `seq >` cursor (no dupes) then live; below-window cursor replays best-effort from oldest. **Fresh-subscriber current-turn replay (D5)** — no cursor → replays only the in-progress turn via the new `markTurnStart()` + `currentTurnStartSeq`. **Bus lifecycle per-turn → per-session (D6)** — events route no longer disposes in `finally` (only unsubscribes); `runtime.disposeSession → disposeBus`, full shutdown `dispose → abortAllBuses → walk → clearAllBuses`. **Per-turn contract preserved by default (D7)** — no `?follow` ⇒ stream still ends on the turn terminal (`sov drive` byte-compatible). **`?follow=true` persistent stream (D8)** — `GET /sessions/:id/events?follow=true` stays open across turns. **Config (X3)** — `gateway.eventBufferSize` (`src/config/schema.ts`); `buildRuntime` calls `setDefaultRingSize(...)` at boot. Idle-session bus eviction deferred to Phase D (D9). New suites `tests/server/eventBus.test.ts` + `tests/server/eventsReconnect.test.ts`. **Deep correctness review → core robust + 4 hardening fixes:** `6e9e054` isolate throwing subscribers in fan-out; `587690a` non-follow reconnect-after-complete ends (not parks) via `turnActive`/`isTurnActive()`; `6d8ec10` `?follow` closes on bus dispose (bus `abortSignal` as second stop source); `9f58fc2` full `dispose()` reclaims subscribe-only bus map entries via `clearAllBuses()`. TS suite **2810 pass / 0 fail / 14 skip** (clean). No new ADRs, no bundle changes (built per ADR H-0010, canonical in `sovereign-ai-docs`). C–F of the roadmap remain. **Known Phase-D item:** interactive-session buses accumulate until shutdown (bounded per-session by the ring; idle/TTL eviction or `DELETE /sessions/:id` is Phase D's job). **Learning-loop soak continues in parallel, untouched** (recall ON by default since v0.6.16 + capture on — the standing #1 ACTIVE-FOCUS track; this gateway phase is separate). |
| [`docs/state/2026-06-05-phase-a-gateway.md`](docs/state/2026-06-05-phase-a-gateway.md) | **Prior — Phase A: Secure Remote Gateway shipped; SECURE-TO-SHIP; hardened.** Release **v0.6.18** (2026-06-05; Phase A shipped at v0.6.17). The first module (M1) of the **run-anywhere harness roadmap** (`docs/specs/2026-06-05-run-anywhere-harness-roadmap-design.md`): a long-lived, headless **`sov gateway`** entrypoint (`src/cli/gatewayCommand.ts`, registered in `src/main.ts` with `--host`/`--port`) that exposes the harness's **native HTTP+SSE protocol** off-loopback safely. New `gateway` config block (`{ host?, port?, token?, corsOrigins? }`, `.strict().optional()`, mirrors `openaiServer`). **Host-configurable bind (D2)** — `src/server/index.ts` + `port.ts` thread optional `hostname` (default `127.0.0.1`; resolution `--host` > `SOV_GATEWAY_HOST` > `gateway.host` > loopback; port `--port` > `SOV_GATEWAY_PORT` > `gateway.port` > 8766). **Bearer auth (D3)** — `src/server/auth.ts` (constant-time compare, mirrors `src/openai/auth.ts`), mounted on `/sessions/*` incl. SSE; `/health` open; token `SOV_GATEWAY_TOKEN` > `gateway.token`, never logged. **CORS (D5)** — `src/server/cors.ts`, echoes ACAO for allow-listed origins only + preflight `OPTIONS`; closed by default. **Refuse-to-boot guard (D4)** — `src/server/gatewaySafety.ts` (`assertGatewaySafe` + `isLoopbackHost`); exit 1 when off-loopback (not `127.0.0.1`/`::1`/`localhost`/`127/8`) AND no token. **Options-gated middleware (D6)** — `startServer` + `buildAppWithRuntime` gained backward-compatible `auth?`/`corsOrigins?`; **TUI / `sov serve` / `sov drive` paths byte-unchanged**. Single-token full-access principal (D7; per-principal authz is Phase E); session model unchanged (D8; multi-subscriber bus is Phase B). e2e test `tests/server/gatewayEndToEnd.test.ts` (full authed turn via MockProvider; 401 without token) + unit suites (auth/cors/gatewaySafety). **Security-reviewed SECURE-TO-SHIP.** **Hardening pass (v0.6.18, 2026-06-05):** deep correctness review + **live cross-origin real-model browser E2E** (streamed clean; tool-use/permission round-trip worked; CORS clean; browser `EventSource` can't auth → `fetch()`+`ReadableStream` client pattern documented in `usage.md`) → 3 fixes: malformed-body→400 on turns+approvals (`adba2c6`), resolved-port `[1,65535]` validation (`3cae86e`), abort in-flight buses before DB close on dispose (`b5dbbee`). TS suite **2778 pass / 0 fail / 14 skip** (clean). No new ADRs, no bundle changes (built per ADR H-0010, canonical in `sovereign-ai-docs`). B–F of the roadmap remain. **Learning-loop soak continues in parallel, untouched** (recall ON by default since v0.6.16 + capture on — the standing #1 ACTIVE-FOCUS track; this gateway phase is separate). |
| [`docs/state/2026-06-04-learning-loop-spike-phase-1.md`](docs/state/2026-06-04-learning-loop-spike-phase-1.md) | **Prior — Learning-Loop Spike Phase 1; the learning loop is now CLOSED; Q1 PASS.** HEAD `bb0cb78`; interim release **v0.6.14** (D6 memory fix + inert recall scaffolding); completed feature releases as the next patch separately. New sealed `src/learning-layer/` module behind a portable four-port contract (Observe / Recall / Reason / Persist, per ADR H-0010): `ports.ts` + `recall/` (assemble + format + readInstincts) + `index.ts` + `adapters/harness/` (FS Persist + provider Reason — adapter #1) + `eval/` (scorer + runner + Track-A/B scenarios). **Recall closed** — deterministic instinct assembly → fenced `<learned-context>` snapshot spliced into the latest user message in `src/core/query.ts` (mirrors MEMORY.md injection), gated by `learning.recall.enabled` (default FALSE); the turns route passes the recall thunk + memoryManager. **D6 latent-bug fix** — the server turns route previously omitted `memoryManager`, so MEMORY.md never injected on the server/TUI surface; now fixed. **Synthesis-yield fix** (was 185 trajectories → 2 instincts): saturating `confidenceFromEvidence` curve + normalized cluster keys + end-of-session synthesis trigger (`learning.synthesizeOnSessionEndAfter`) + fail-loud synthesizer + `maxTurns` 8→16. **Project-id fix** — recall reads `getProjectId(cwd).id` (the write path) so synthesized instincts are recallable under any bundle. **Eval** (`bun run eval:learning`): with-vs-without correctness-flip, Track A (curated seeded) + Track B (full loop) + deterministic MockProvider proof (`tests/server/turns.recall.test.ts`) + semantic suite (`tests/semantic/suites/24-learning-recall.cases.ts`). **Q1 RESULT: PASS — 6 flips / 0 regressions, no human in the loop.** New config: `learning.recall.{enabled,maxLessons,tokenBudget}`, `learning.evidenceSaturation`, `learning.synthesizeOnSessionEndAfter`; new `'instinct'` ComponentKind. TS suite ~2708 pass / 14 skip / 3 known env-only learning-test fails. Phase-2 deferred items (mock-host isolation suite + 4 portability gates; full Persist extraction; synthesizer onto Reason; adapter #2; recall on other surfaces) + founder-reserved decisions (rented engine; go/no-go; auto-promote default; recall-on default) captured in the snapshot. Spec `docs/specs/2026-06-03-portable-learning-layer-adapter-1-design.md` + plan `docs/plans/2026-06-03-learning-loop-spike-phase-1.md`. No new ADRs in-repo (built per ADR H-0010, canonical in `sovereign-ai-docs`). Cross-repo: a `sovereign-ai-docs` session should mark the spike spec's Phase-1 Status + the `learning-loop-closure-and-proof` open-question + the dev status page as Q1 PASS. |
| [`docs/state/2026-05-31-post-m2-hardening.md`](docs/state/2026-05-31-post-m2-hardening.md) | **Prior — post-M2 hardening run (v0.6.1 → v0.6.13); not a new phase.** Latest release **v0.6.13**; HEAD `bb8240e`. Captures the 13 patch releases since Phase 21 M2 (v0.6.0): TUI global style guide (`packages/tui/internal/style/`) + routing-output sky-300 color fix; visual TUI QA / VHS loop (`bun run visual`, multi-screenshot scenarios); UX fix rounds + end-of-turn gap tuning; **v0.6.12 deep bug-hunt audit (21 fixes — 1 critical / 11 high / 8 medium / 1 low, test-first, `eaee313..0400255`)**; **v0.6.13 markdown heading color sky-200 `#bae6fd` → sky-100 `#e0f2fe`** (clearly lighter than the sky-300 `#7dd3fc` bold/inline-code emphasis). TS suite ~2660 (~2646 pass clean / 3 known env-only learning-test fails / 14 skip); Go all packages green. No phase opened/closed; no bundle changes; no new ADRs. |
| [`docs/state/2026-05-25-phase-21-m2.md`](docs/state/2026-05-25-phase-21-m2.md) | **Prior — Phase 21 M2 (release automation) shipped 2026-05-25.** GitHub Actions workflow at `.github/workflows/release.yml` drives binary releases on `v*.*.*` tag push; `scripts/release.ts` refactored into a thin orchestrator over new extracted scripts (`release-shared.ts` + `release-build-target.ts` + `release-upload.ts`); both local + CI paths share the same code. Idempotent upload step keeps local-cut-then-CI scenarios green. Fine-grained PAT scoped to `sov-releases` only, stored as `SOV_RELEASES_TOKEN`. First cut: v0.6.0 (~4 min wall time end-to-end). ADR P21-C in DECISIONS.md. |
| [`docs/state/2026-05-24-config-ux-rebuild.md`](docs/state/2026-05-24-config-ux-rebuild.md) | Prior — **Config UX rebuild shipped 2026-05-24; both `sov config` and `/config` now share a single branded Bubble Tea TUI driven by a curated 10-group catalog.** Replaces the hand-rolled raw-mode `sov config` picker (`src/ui/configMenu.ts`, 389 LoC, deleted) and the JSON-dump `/config` slash command. Hierarchical drill-in menu with `valueColumn` + `badge` (`✓ live` / `⟳ next session`) per row. Editor-aware coercion (string fields preserve numeric-looking input — fixes the legacy `parseValueLiteral` over-coercion). On validation failure, `runSet` re-emits the SAME editor with the typed value preserved + the validation error as subtitle (HIGH from final review) so the user corrects in place. v0 live-apply set: `theme`, `defaultModel`, `providers.<x>.model` (conditional on active provider), `verbose`, `webSearch.*`. New `src/config/catalog.ts` (817 LoC, 10 groups + 4 provider subgroups, every field in `SettingsSchema`), `src/config/liveApply.ts` (6 hooks keyed by dotpath, all gracefully handle `commandCtx === undefined` for `sov config` standalone mode), `src/commands/configOps.ts` (slash dispatcher with root menu / group submenu / edit / set / unset verbs; legacy `show|path|get` preserved as scriptable escape hatches), `src/cli/configMode.ts` (`runConfigOnlyMode` boots minimal Runtime literal — real `SessionDb` rooted at `<harnessHome>/sessions.db` plus stub everything else: empty toolPool, empty agents, throw-on-invoke task/review/scheduler callbacks; no `buildRuntime`, no preflight, no providers, no bundle load; sub-50ms boot; spawns sov-tui with `--initial-command=/config`). Go: extended `PickerCard` with right-aligned ValueColumn + Badge wide layout (backwards-compat preserved — /model, /resume, /export, /theme stay byte-identical via the no-extras code path), new `InputCard` Bubble Tea component on `bubbles/textinput` for string/number/secret edits with `EchoPassword` masking + same `CardBorderStyle` box, `app.go` integration for `inputOpen` + `verboseChanged` SSE side-effects + `--initial-command` boot fire on first WindowSizeMsg (guarded by `initialFired`). Wire: `PickerOpenItemSchema` gains optional `valueColumn` + `badge`, new `InputOpenConfigSchema`, `CommandSideEffectsSchema` gains `inputOpen` + `verboseChanged` (pointer bool so absence is distinct from explicit false). Final-review subagent surfaced 1 HIGH + 5 MEDIUM; HIGH (validation failure now re-emits same editor with typed value preserved + error subtitle via `reopenEditorWithError`) + 2 MEDIUMs (string-field over-coercion fixed via `coerceValueForEditor` switching on editor.kind: string/secret/enum preserve verbatim, number uses `Number()` with NaN guard, boolean accepts true/false case-insensitive; theme hook respects `commandCtx === undefined` before mutating singleton) fixed inline with 3 new tests. TS suite **2420/0/14** (+114 from Phase 2 close-out's 2306). Go suite +30 tests across PickerCard/InputCard/app/transport. Semantic suite gained `23-config-ux.cases.ts` (5 cases: JSON-dump regression, reload badge, theme live-apply, validation error, secret masking). No bundle changes (rebuild surface entirely in `src/`, `tests/`, `packages/tui/`). No ADRs (purely additive at the wire; all decisions captured in the spec at `docs/specs/2026-05-24-config-ux-rebuild-design.md` and plan at `docs/plans/2026-05-24-config-ux-rebuild.md`). Release v0.5.1 follows. |
| [`docs/state/2026-05-23-phase-2-task-routing.md`](docs/state/2026-05-23-phase-2-task-routing.md) | Prior — **Phase 2 (multi-provider task routing observability) shipped 2026-05-23; Phase 2 closed.** Wraps full atom-level observability around the Phase 1 smart router. The runtime synthesizes four new SSE event types (`delegator_plan`, `delegator_atom_started`, `delegator_atom_complete`, `delegator_complete`) by observing the scheduler's delegation lifecycle — no delegator-prompt changes required. New `src/router/progressEvents.ts` — `DelegationLifecycleEvent` discriminated union + `synthesizeDelegationEvents({ bus, rootSessionId, agentRegistry })` factory producing the per-turn closure that tracks active delegator state (its `childSessionId`, atom counter, lane-distribution accumulator) and publishes the four SSE events. Scheduler gets two new optional callbacks on `DelegateInput`: `delegationLifecycleRecorder?: (event) => void` (invoked at start + every return path) and `perChildTimeoutMsOverride?: number` (three-step precedence: override → opts → agent-derived). `ToolContext` gets two new optional fields: `laneRegistry?: LaneRegistry` (set to runtime's registry; `AgentTool.call` resolves `agent.role → lane.timeoutMs`) and `delegationLifecycleRecorder?` (threaded into `scheduler.delegate()`). Per-atom + per-delegator metadata persisted in SessionDb: `{kind: 'routing-delegator', parentSessionId}` for delegator sessions; `{kind: 'routing-atom', laneName, laneProvider, laneModel, parentDelegatorSessionId}` for cost-lane atoms; legacy `{agentName, kind: 'subagent'}` preserved for non-router children. Scheduler's `createChildSession` callback signature gained `lane: { name, provider, model } | null` and `isDelegator: boolean` (both computed from `agent.role` + `resolveLane`). Two new SessionDb helpers via `json_extract`: `listRoutingAtomsByParent(parentSessionId)` (atom rows under any delegator under the given parent, ordered by `created_at ASC`) and `listRoutingAtomsAll()` (cross-session). New `/routing-stats` slash command at `src/commands/routingStats.ts` + pure aggregator at `src/router/stats.ts` — `computeRoutingStats(rows)` returns `{ scope, totalAtoms, byLane: { count, pctOfTotal, successCount, successRate, avgDurationMs, totalDurationMs }, overallSuccessRate, overallAvgDurationMs }`; success heuristic v0 is `outputTokens > 0` (Phase 2.5 refinement candidate); `--all` flag aggregates across sessions. New `getRoutingStats` on `CommandContext`. TUI rendering via new `packages/tui/internal/components/delegatorline.go` (glyph vocabulary: `◇` plan, `→` atom start, `✓` success, `✗` failure, `◆` done; lane distribution sorted by count desc then name asc for deterministic output) + `packages/tui/internal/transport/delegator_events.go` (Go struct decoders) + four new cases in `app.go` SSE switch. `sov drive` four new cases in `EventRenderer.handle` produce plain-text bracketed lines (`[delegator_plan] dispatching <n> atom(s)`, `[delegator_atom <idx>] starting on <lane>: <preview>`, etc.). `sov serve` streaming branch of `POST /v1/chat/completions` subscribes to the per-session bus and writes `event: hermes.delegator.progress\ndata: <json>\n\n` side-channel SSE frames interleaved with the OpenAI-shaped main stream (filtered to delegator_* event types; unsubscribe in `finally`); new `buildDelegatorProgressPayload(event)` JSON helper in `src/openai/streaming/chunks.ts`. New `resolveTaskRoutingEnabled(envValue, settingsValue)` pure helper honoring `SOV_TASK_ROUTING_ENABLED='1'`/`'0'` env override (anything else falls through to settings). Lane `timeoutMs` enforcement (Phase 1's R-D R-D deferred follow-up) wired end-to-end; the Phase 1 skipped `atomTimeout.test.ts` is unskipped and green. Purely additive — `taskRouting.enabled: false` (default) is byte-identical: no synthesis closure constructs, no side-channel writes, no `/routing-stats` data accrues. No bundle changes (Phase 2 surface entirely in `src/`, `tests/`, `packages/tui/`). No ADRs (purely additive — all design decisions captured in the spec at `docs/specs/2026-05-23-multi-provider-task-routing-design.md` and the plan at `docs/plans/2026-05-23-phase-2-task-routing.md`). TS suite **2306/0/14** (+63 from Phase 1's 2243). Go suite +21 tests across decoders / formatters / app SSE switch. T12 (cut v0.5.0 release) follows. |
| [`docs/state/2026-05-23-phase-1-task-routing.md`](docs/state/2026-05-23-phase-1-task-routing.md) | Prior — **Phase 1 (multi-provider task routing smart-router foundation) shipped 2026-05-23 late afternoon; Phase 1 closed.** New `taskRouting` config block in `src/config/schema.ts` (`LaneConfigSchema` + `TaskRoutingSchema`: `{ enabled: false, delegator: { model: 'claude-sonnet-4-6' }, lanes: { 'cheap-task'?, 'moderate-task'?, 'frontier-task'? } }`; per-lane partial overrides inherit `LANE_DEFAULTS`). New `src/router/` mini-suite (~250 LoC): `lanes.ts` (`LANE_DEFAULTS` per-lane Haiku/Sonnet/Opus defaults + pure `resolveLane(name, cfg)`); `laneRegistry.ts` (`buildLaneRegistry(cfg)` pre-resolves all four known lane names at construction, serves `lookup(role) | entries()`); `preflight.ts` (`runLanePreflight` aggregates failures across every configured cost lane — skipping `delegator` since its model rides the parent's existing preflight — into a single `LanePreflightError` so credentials can be fixed in one pass). Four new agent definitions under `bundle-default/agents/`: `cheap-task.md` (`role: cheap-task`, `inheritParentTools: true`, `maxTurns: 30`), `moderate-task.md` (`role: moderate-task`, `inheritParentTools: true`, `maxTurns: 50`), `frontier-task.md` (`role: frontier-task`, synthesis-aware prompt with explicit failed-atom handling, `inheritParentTools: true`, `maxTurns: 50`), `delegator.md` (`role: delegator`, `allowedTools: [AgentTool]`, `allowedSubagents: [cheap-task, moderate-task, frontier-task]`, `maxTurns: 50` — the load-bearing decision prompt with lane catalogue + trivial/compound/hard-reasoning decision rule + synthesis-atom pattern + failure handling). New `bundle-default/prompts/smart-router.md` segment injected into the parent's frozen system prompt when `taskRouting.enabled: true` (parent's first action on every turn MUST be `AgentTool(subagent_type: "delegator", ...)`; parent relays delegator's `summary` verbatim). Two surgical scheduler edits land the wire (`src/runtime/scheduler.ts`): `SubagentSchedulerOpts.resolveLane?: (role) => LaneConfig | undefined` consulted in `resolveProviderModel()` BEFORE the Phase 13.2 capability table; `filterToolsForChild(parentPool, allowedTools)` → `buildChildToolPool(parentPool, agent)` with new `inheritParentTools: true` branch returning `parentPool.filter(t => !exclusions.has(t.name))`. `AgentDefinition.inheritParentTools: boolean` + `allowedSubagents: string[]` added to `src/agents/types.ts`. `AgentTool.call` enforces `allowedSubagents` recursion guard. Runtime wiring (`src/server/runtime.ts`): lane registry built UNCONDITIONALLY (so cost-lane sub-agents stay reachable via `AgentTool` even when `enabled: false` — the B-via-D bridge baseline). Atom-failure handling: delegator does NOT re-dispatch on `terminal !== 'completed'`; continues to synthesis with `Atom N (failed: <reason>)` labeling. Synthesis is the final atom — same dispatch mechanism, lane chosen dynamically. Per-lane provider mixing flows through transparently. MockProvider extended (`src/providers/mock.ts`): static `toolUseScript: ToolCallScript[] | undefined` (`'tool_use'`/`'text'`/`'throw'` script kinds) for integration tests. 5 semantic test cases at `tests/semantic/suites/22-task-routing.cases.ts`. No ADRs (purely additive). TS suite **2243/0/15** (+51 from Phase 18 close-out's 2192). T18 (testing-log entry) + T19 (cut v0.4.1 release) followed; Phase 2 then opened. |
| [`docs/state/2026-05-23-phase-18-openai-api-server.md`](docs/state/2026-05-23-phase-18-openai-api-server.md) | Prior — **Phase 18 (OpenAI-compatible HTTP API server) shipped 2026-05-23 mid-day; Phase 18 closed.** New `src/openai/` top-level subsystem: bearer-auth middleware (`SOV_OPENAI_API_KEY` env > `openaiServer.apiKey` config > refuse to boot per D5/OQ1) + `/health` (unauthenticated, probe-friendly) + `POST /v1/chat/completions` (non-streaming JSON + SSE streaming branches, driven via `query()` directly NOT `AgentRunner` because the wire carries full multi-message history — D7) + `GET /v1/models` (catalog from SUPPORTED_MODELS) + per-request model resolution (`harness-default` → runtime bootstrap; explicit `claude-*` / `gpt-*` → fresh `resolveProvider(family, model, { harnessHome })` per call — T9) + pure Zod request/response schemas + pure ContentBlock ↔ OpenAI mappers + stream chunk builders + generator-driven SSE translator. New `sov serve` CLI subcommand boots `Bun.serve` on a stable port (default 8765, env `SOV_OPENAI_PORT` / config `openaiServer.port`, host defaults to `127.0.0.1`); SIGINT/SIGTERM → `server.stop()` + `runtime.dispose()` + exit 0. Tool execution loops INSIDE the single `/v1/chat/completions` call (D9 — `finish_reason` ALWAYS `'stop'` or `'length'`, NEVER `'tool_calls'`); tool invocations surface as `delta.tool_calls[]` chunks (D8 — whole arguments JSON in one chunk, no partial streaming) + `event: hermes.tool.progress\ndata: {tool_use_id, output?, is_error?}\n\n` SSE side-channel events for harness-aware UIs. Permission policy mirrors cron headless: `mode: 'default'` + auto-deny `ask` (D11); tool pool filtered against `SUBAGENT_EXCLUDED_TOOLS` (D12). SessionDb rows minted per request tagged `metadata.kind='openai-api'` with PK namespaced `openai:<id>` to prevent cross-surface pollution (post-H1 audit fix `7303e96`); the wire (`chatcmpl-<id>`) echoes the CLIENT's unprefixed `X-Session-Id` view. Stateless per D10 — request body's `messages[]` is the sole source of conversation history; the row exists purely for trace + learning observability. Abort-on-disconnect (T10): `c.req.raw.signal` → request-scoped `AbortController` → `query()`'s signal — no wasted provider tokens after the client gives up. Post-audit fixes layered in pre-close-out: H1 namespace X-Session-Id (`7303e96`); H2 wrap non-streaming drain in catch + return OpenAI error envelope via `buildProviderErrorResponse` classifier (`573a45b` — `CredentialUnavailableError` → 401 `invalid_api_key`; `ProviderHttpError` → mirror upstream status + `upstream_error`; else 500 `api_error`); H3 collapse non-spec `finish_reason` values to `'stop'` (`ad74f0c` — TypeScript narrows return type to `'stop' \| 'length'`). No ADRs (purely additive — all decisions captured inline in the plan at `docs/plans/2026-05-23-phase-18-openai-api-server.md`, D1-D14 + OQ1-OQ5). TS suite **2192/0/14** (+117 from morning Phase 17 close-out's 2075 baseline; +196 from prior session's 1996). T12 (cut v0.4.0 release) follows. |
| [`docs/state/2026-05-22-phase-17-cron.md`](docs/state/2026-05-22-phase-17-cron.md) | Prior — **Phase 17 (cron / scheduled jobs) shipped 2026-05-22 evening; Phase 17 closed.** New `src/cron/` subsystem (types / schedule parser supporting relative + interval + cron + ISO / atomic jobs.json CRUD / `CronRunner` with file-lock + fake-clock-injectable tick / executor factory / production glue) + `sov cron add | list | show | pause | resume | delete | run | tick` CLI surface + 60-second tick loop embedded in `buildRuntime` lifecycle (`.unref?.()` so tests don't hang; `cronEnabled: false` opts out). Each job runs in a fresh `metadata.kind='cron'` session via `AgentRunner` with auto-deny on permission asks (matches `sov drive` headless mode). Skill chaining via `expandSkillPrompt` (user-message injection — matches harness convention from M8); pre-agent scripts via `spawnSync` with interpreter inference (`.py → python3`, `.ts/.js → bun`, `.sh → bash`) + 120s default timeout + 16 KiB stdout cap. Delivery: `<harnessHome>/cron/outbox/<jobId>/<ts>.txt`; `[SILENT]` first-line prefix (case-insensitive, post-trim) short-circuits the file write. Recursion guard: six cron CRUD tool names (`cron_add` / `cron_list` / `cron_show` / `cron_pause` / `cron_resume` / `cron_delete`) blocked in `SUBAGENT_EXCLUDED_TOOLS`. No ADRs (purely additive — all decisions captured inline in the plan at `docs/plans/2026-05-22-phase-17-cron-scheduled-jobs.md`). TS suite **2053/0/14** (+57 from morning's 1996 baseline). T11 (cut v0.3.0 release) followed; Phase 18 then opened. |
| [`docs/state/2026-05-22-semantic-suite-revival.md`](docs/state/2026-05-22-semantic-suite-revival.md) | Prior — **semantic suite revival shipped 2026-05-22 late PM.** New `sov drive` subcommand restores the headless line-driven conversation surface that semantic tests need (M13 collapsed the readline REPL into the TUI launcher, which crashes on non-TTY stdin). Drive boots the same Hono server the TUI talks to, reads stdin line-by-line, emits plain-text events to stdout. Reconnects SSE after each turn (server closes on turn_complete). Auto-POSTs to `/approvals` on permission_request, auto-sends `promptToSend` for prompt-type slash commands. Also fixed an M10.5-era bug where `/init` + `/commit` + skill-sourced commands interpolated `ContentBlock[]` as `[object Object]`. Semantic suite re-baselined at **55/58 pass** against the installed binary (3 known flakes — all model-behavior or test-design issues, documented in the state file). TS suite **1996/0/14** (+14: 11 driveCommand + 3 promptToSend). |
| [`docs/state/2026-05-22-tui-tool-call-abstraction.md`](docs/state/2026-05-22-tui-tool-call-abstraction.md) | Prior — **TUI tool-call abstraction + two surgical UX fixes shipped 2026-05-22 PM.** Default tool rendering flipped to one-liner per `tool_result` (`Verb target stats ›`, Claude mobile aesthetic). New `packages/tui/internal/components/compactline.go` owns the verb mapping. `ui.toolOutput.mode = 'detailed'` opts back into the bordered ToolCard (output capped to `inlineLines`, default 10). `-v / --verbose` wired again — forwards as `--verbose-raw` (orthogonal raw escape hatch). Glyphs: `⚠` for permission-denied, `✗` for runtime errors. Fix A removed the `tui server listening` stderr boot line; Fix B switched dark chroma style to `monokai` for visible code highlighting. Spec at `docs/specs/2026-05-22-tui-tool-call-abstraction-design.md`. TS **1982/0/14** (+10); Go all packages green (+29 new tests); lint+typecheck clean. No new ADRs (post-close-out polish). |
| [`docs/state/2026-05-22-phase-21-m1.md`](docs/state/2026-05-22-phase-21-m1.md) | Prior — **Phase 21 M1 shipped 2026-05-22 AM.** Binary distribution: per-platform Bun `--compile` + Go cross-compile + side-car `bundle-default/` + public `github.com/yevgetman/sov-releases` repo with `curl ... | bash` installer. **Two releases shipped: `v0.2.0` (initial) + `v0.2.1` (splash/MCP version fix; current latest).** Tarball sizes: darwin-arm64 31.9 MB / darwin-x64 34.8 MB / linux-x64 47.5 MB; SHA256SUMS verified. Runtime: `shippedBundlePath()` + `findTuiBinary()` learn `process.execPath`-relative discovery; `sov upgrade` auto-detects binary mode + re-runs public installer; `src/version.ts` switched to build-time JSON import; harness version threaded into splash via `--harness-version` flag. ADRs P21-A + P21-B. TS suite **1972/0/14** (+14 from baseline). macOS smoke PASS; **end-to-end `sov upgrade` 0.2.0→0.2.1 PASS**; linux-x64 binary structural check PASS; linux container smoke deferred (Docker daemon issue — re-run separately); darwin-x64 deferred to first Intel-Mac beta user. |
| [`docs/state/2026-05-21-ux-fixes-r5.md`](docs/state/2026-05-21-ux-fixes-r5.md) | Prior ux-fixes round 5 snapshot — inline-mode refactor: drop `tea.WithAltScreen()` + mouse capture → terminal owns scrollback (wheel + trackpad scroll) and text selection natively. Committed history flows via `tea.Println`; View() is just a bottom live region (`LiveRegion` at `packages/tui/internal/components/liveregion.go`) + prompt + status. Round 3 fixed textbox auto-grow / splash polish / spinner alignment / markdown wrap. Round 4 added paste abstraction + ESC turn-cancel + prompt prefix first-line-only. TS suite green at **1955/0/14**; Go all packages green. Phase 16.1 stays closed. |
| [`docs/state/2026-05-20-m13.md`](docs/state/2026-05-20-m13.md) | Prior M13 close-out snapshot — **Phase 16.1 M13 shipped; Phase 16.1 closed.** terminalRepl removal complete: deleted `src/ui/terminalRepl.ts` (2334 LoC) + 9 REPL-only `src/ui/*` modules + their tests + M12 deprecation infra + M11 surface resolver + readline asker bits from `permissions/prompt.ts`. Dropped `--ui` flag + `SOV_UI` env + `ui.surface` config field. main.ts boot flow collapsed ~65 → ~13 lines. Missing binary = hard error (no fallback surface). **4 ADRs M13-01..04; suite green at 1949/1949**; 4/4 smoke pass; 4-Opus parity audit clean. |
| [`docs/state/2026-05-20-m13-smoke/`](docs/state/2026-05-20-m13-smoke/) | M13 smoke output — 4 boot-decision scenarios (default-TUI boot / missing-binary hard error / Commander rejects unknown `--ui` flag / `sov dispatch` headless round-trip). |
| [`docs/state/2026-05-19-m12.md`](docs/state/2026-05-19-m12.md) | Prior M12 close-out — Phase 16.1 M12 shipped (REPL deprecation warning: stderr line on explicit `--ui repl` / `SOV_UI=repl` / `ui.surface=repl`; silent on missing-binary fallback; `SOV_NO_DEPRECATION_WARNING=1` suppresses; **2 ADRs M12-01..02; suite green at 2073/2073**; 6/6 boot smoke pass). |
| [`docs/state/2026-05-19-m12-smoke/`](docs/state/2026-05-19-m12-smoke/) | M12 smoke output — 6 boot-decision scenarios asserting deprecation presence/absence across opt-in sources, suppression flag, missing-binary fallback, default-TUI. |
| [`docs/state/2026-05-19-m11-5.md`](docs/state/2026-05-19-m11-5.md) | Prior M11.5 close-out — Phase 16.1 M11.5 shipped (inline picker card: `pickerOpen` side-effect protocol; new `PickerCard` Go component; `/model`, `/resume`, `/export` migrated; T8 spacing fix; **3 ADRs M11.5-01..03; suite green at 2061/2061**; real-Anthropic smoke 2/2 pass). |
| [`docs/state/2026-05-19-m11-5-smoke/`](docs/state/2026-05-19-m11-5-smoke/) | M11.5 smoke output — 2 real-Anthropic picker round-trip scenarios + README summary. |
| [`docs/state/2026-05-17-m11.md`](docs/state/2026-05-17-m11.md) | Prior M11 close-out — Phase 16.1 M11 shipped (default-flip: `--ui` defaults to `'tui'`; new surface resolver with CLI > env > config > default precedence; auto-fallback to REPL when sov-tui missing; **3 ADRs M11-01..03; suite green at 2033/2033**; audit PASS-with-followups). |
| [`docs/state/2026-05-17-m11-parity-audit.md`](docs/state/2026-05-17-m11-parity-audit.md) | The M11 audit report — Opus subagent re-audit per Postmortem Rule 3 verifying M10 HIGH gaps remain closed and M11 code surface introduces no new HIGH/CRITICAL/MEDIUM gaps. PASS-with-followups (1 LOW fixed inline). |
| [`docs/state/2026-05-17-m11-smoke/`](docs/state/2026-05-17-m11-smoke/) | M11 smoke output — 13 boot-decision scenarios (`run-smoke.ts`) + 1 real-Anthropic dispatcher rerun + README summary. |
| [`docs/state/2026-05-16-m10-5.md`](docs/state/2026-05-16-m10-5.md) | Prior M10.5 close-out (predecessor to M11): slash-command dispatcher route + Go TUI slash router; closed #40; unblocked M11. |
| [`docs/state/2026-05-16-m10.md`](docs/state/2026-05-16-m10.md) | M10 close-out: parity audit by 4 parallel Opus subagents; 2 HIGH gaps fixed inline (HarnessInfoTool + repair-missing-tool-results); 1 HIGH scope-bounded (mission CLI-only); 1 HIGH deferred (closed by M10.5); 4 ADRs M10-01..04. |
| [`docs/state/2026-05-16-tui-parity-audit.md`](docs/state/2026-05-16-tui-parity-audit.md) | The M10 audit report itself — slice-by-slice findings, severity classification, fixes-applied, M11 disposition. Postmortem Rule 3 attestation. |
| [`docs/state/archive/`](docs/state/archive/) | Historical snapshots: `2026-05-07.md` (Phase 13.4), `2026-05-11.md` (Phase 16.0a), `2026-05-12.md` (Phase 16 revert), `2026-05-13.md` (Phase 16.1 M0–M3), `2026-05-14.md` (Phase 16.1 M4 + M5 + M5.1), `2026-05-14-pm.md` (Phase 16.1 M6 close-out + 2026-05-15 hardening + autonomous M6 smoke + PM #32/#37), `2026-05-15.md` (Phase 16.1 M7 close-out + post-close-out hardening + autonomous M7 smoke), `2026-05-16.md` (Phase 16.1 M8 close-out), `2026-05-16-m9.md` (Phase 16.1 M9 close-out), `2026-05-16-m9-5.md` (Phase 16.1 M9.5 close-out), `2026-05-16.md` again (Phase 16.1 M9.6 close-out). |

### Forward-looking

| File | What's in it |
|---|---|
| [`docs/backlog/post-phase-13-4.md`](docs/backlog/post-phase-13-4.md) | Open backlog (2 items): **#17 eval-gated auto-promote (P4, conditional)**; **#49 Node-20 GitHub Actions deprecation (P3, CI-maintenance — `actions/checkout@v4` forced to Node 24 from 2026-06-16)**. #48 (Phase 21 M2 release automation) closed 2026-05-25 — shipped as v0.6.0. #47 closed 2026-05-22 evening (dead `transcript.go` removed). #40 closed M10.5; #41 + #43 + #44 + #45 + #46 closed 2026-05-19; #29 / #38 / #39 closed 2026-05-19. **Phase 16.1 closed with M13 (2026-05-20); Phase 21 M1 closed 2026-05-22; Phase 17 (cron / scheduled jobs) closed 2026-05-22 evening; Phase 18 (OpenAI HTTP API server) closed 2026-05-23; Phase 1 + Phase 2 (multi-provider task routing) closed 2026-05-23; Phase 21 M2 (release automation) closed 2026-05-25.** Post-M2: v0.6.1 → v0.6.13 hardening/UX releases (no new phase) — see the post-M2 state snapshot. |
| [`docs/backlog/phase-16-rebuild-prereqs.md`](docs/backlog/phase-16-rebuild-prereqs.md) | 24 subsystems any Phase 16.1 foreground refactor must re-wire — **24/24 complete after M8; M9 visual polish landed 2026-05-16; M10 audit verified independently with caveat that slash-command-stack composition (#40) was not covered by the original prereq methodology** |
| [`docs/specs/2026-05-13-phase-16-1-tui-rebuild-design.md`](docs/specs/2026-05-13-phase-16-1-tui-rebuild-design.md) | Design spec for the Phase 16.1 TUI rebuild (executed; Phase 16.1 closed at M13, 2026-05-20) |
| [`docs/specs/2026-05-13-production-harness-roadmap-design.md`](docs/specs/2026-05-13-production-harness-roadmap-design.md) | Umbrella production polish roadmap |
| [`docs/specs/2026-05-16-phase-16-1-m10-parity-audit-design.md`](docs/specs/2026-05-16-phase-16-1-m10-parity-audit-design.md) | M10 audit design spec |
| [`docs/specs/2026-05-16-phase-16-1-m10-5-slash-dispatcher-design.md`](docs/specs/2026-05-16-phase-16-1-m10-5-slash-dispatcher-design.md) | M10.5 slash-dispatcher design spec |
| [`docs/specs/2026-05-17-phase-16-1-m11-default-flip-design.md`](docs/specs/2026-05-17-phase-16-1-m11-default-flip-design.md) | M11 default-flip design spec |
| [`docs/specs/2026-05-19-phase-16-1-m11-5-inline-picker-card-design.md`](docs/specs/2026-05-19-phase-16-1-m11-5-inline-picker-card-design.md) | M11.5 inline picker card design spec |
| [`docs/specs/2026-05-19-phase-16-1-m12-repl-deprecation-design.md`](docs/specs/2026-05-19-phase-16-1-m12-repl-deprecation-design.md) | M12 REPL deprecation design spec |
| [`docs/specs/2026-05-19-phase-16-1-m13-terminalrepl-removal-design.md`](docs/specs/2026-05-19-phase-16-1-m13-terminalrepl-removal-design.md) | M13 terminalRepl removal design spec (just shipped — Phase 16.1 closed) |
| [`docs/specs/2026-05-21-binary-distribution-design.md`](docs/specs/2026-05-21-binary-distribution-design.md) | Phase 21 binary distribution design — `bun build --compile` + Go cross-compile + public `sov-releases` repo + one-line installer script; M1 manual pipeline, M2 GitHub Actions automation |
| [`docs/specs/2026-06-05-run-anywhere-harness-roadmap-design.md`](docs/specs/2026-06-05-run-anywhere-harness-roadmap-design.md) | **Run-anywhere harness roadmap — ✅ COMPLETE (A–F shipped).** The umbrella program: secure gateway (A) → multi-client transport (B) → reference web UI (C) → persistent supervisor (D) → multi-user (E) → channels (F). Per-phase specs + plans below; close-out snapshots in `docs/state/2026-06-0*-phase-*.md`. |
| [`docs/specs/2026-06-05-phase-a-secure-remote-gateway-design.md`](docs/specs/2026-06-05-phase-a-secure-remote-gateway-design.md) | Phase A — secure remote gateway design (shipped v0.6.17/0.6.18) |
| [`docs/specs/2026-06-05-phase-b-multi-client-transport-design.md`](docs/specs/2026-06-05-phase-b-multi-client-transport-design.md) | Phase B — multi-client session transport design (shipped v0.6.19) |
| [`docs/specs/2026-06-05-phase-c-reference-web-ui-design.md`](docs/specs/2026-06-05-phase-c-reference-web-ui-design.md) | Phase C — reference web UI design (shipped v0.6.20) |
| [`docs/specs/2026-06-06-phase-d-persistent-supervisor-design.md`](docs/specs/2026-06-06-phase-d-persistent-supervisor-design.md) | Phase D — persistent multi-session supervisor / service install design (shipped v0.6.21) |
| [`docs/specs/2026-06-06-phase-e-multi-user-design.md`](docs/specs/2026-06-06-phase-e-multi-user-design.md) | Phase E — multi-user identity + state scoping design (shipped v0.6.22) |
| [`docs/specs/2026-06-06-phase-f-channels-design.md`](docs/specs/2026-06-06-phase-f-channels-design.md) | Phase F — channel framework + Slack/Telegram/webhook adapters design (shipped v0.6.23) |
| [`docs/specs/2026-06-06-sms-channel-design.md`](docs/specs/2026-06-06-sms-channel-design.md) | SMS channel (Twilio) design — a post-Phase-F channel: Twilio webhook (signature + sender allow-list) → safe-posture turn → async reply via the Messages REST API; allow-listed + per-sender-isolated + safe-by-default (shipped v0.6.26) |
| [`docs/specs/2026-06-06-imessage-channel-feasibility.md`](docs/specs/2026-06-06-imessage-channel-feasibility.md) | iMessage channel feasibility spike — **verdict: NO-GO by default.** Apple ships no official inbound-message API; a BlueBubbles-bridge route is feasible but ops-heavy + ToS-risky; cheaper substitutes are the shipped Telegram or a webhook+Shortcut. |
| [`docs/specs/2026-06-08-subscription-executor-spike.md`](docs/specs/2026-06-08-subscription-executor-spike.md) | **Subscription-executor spike (opt-in, off by default; shipped v0.6.27).** An opt-in sub-agent executor that branches `SubagentScheduler.delegate()` to a headless `claude -p` subprocess (Claude Code's own agentic loop) returning the native `drainRunner` result shape, so the whole scheduler tail is byte-unchanged. Per-tool work is replayed into the learning corpus + trace, **canonicalized to the native tool vocabulary** (`Read`→`FileRead`, `file_path`→`path`, drop `Bash.description`; unmapped pass through) so delegated turns co-cluster with native ones. Wired **only** to the interactive sub-agent seam (NOT cron/channels/gateway) — driving a Claude *subscription* credential as an automated/multi-tenant backend crosses the personal-use ToS boundary; the per-token API stays the path for everything else. Config `subscriptionExecutor` (`{ enabled:false, engine:'claude-code', binary?, permissionMode?('bypass'[default→`--dangerously-skip-permissions`]\|'plan'\|'acceptEdits'\|'default'), timeoutMs?, maxTurns? }`); H-0010 "rent the engine" context. **permissionMode default flipped to `bypass` (v0.6.32): a headless `claude -p` has no approver; bounded to the attended interactive seam, remote channels keep their own bypass rejection.** |
| [`docs/plans/`](docs/plans/) | Implementation plans (executed; left as record). Run-anywhere roadmap plans (A–F): [`2026-06-05-phase-a-secure-remote-gateway.md`](docs/plans/2026-06-05-phase-a-secure-remote-gateway.md), [`2026-06-05-phase-b-multi-client-transport.md`](docs/plans/2026-06-05-phase-b-multi-client-transport.md), [`2026-06-05-phase-c-reference-web-ui.md`](docs/plans/2026-06-05-phase-c-reference-web-ui.md), [`2026-06-06-phase-d-persistent-supervisor.md`](docs/plans/2026-06-06-phase-d-persistent-supervisor.md), [`2026-06-06-phase-e-multi-user.md`](docs/plans/2026-06-06-phase-e-multi-user.md), [`2026-06-06-phase-f-channels.md`](docs/plans/2026-06-06-phase-f-channels.md), [`2026-06-06-sms-channel.md`](docs/plans/2026-06-06-sms-channel.md) (SMS channel — Twilio; shipped v0.6.26). Earlier latest: `2026-05-24-phase-21-m2-release-automation.md` (Phase 21 M2 — GitHub Actions release automation; shipped + closed 2026-05-25). |

### Postmortems — required reading before similar work

| File | When to read |
|---|---|
| [`docs/postmortems/2026-05-12-phase-16-revert.md`](docs/postmortems/2026-05-12-phase-16-revert.md) | **Before any future foreground-surface refactor.** Rules 1–4. |
| [`docs/postmortems/loop-detector-orphaned-tool-use.md`](docs/postmortems/loop-detector-orphaned-tool-use.md) | When debugging tool_use/tool_result lifecycle bugs |

### Operational log

| File | What's in it |
|---|---|
| [`docs/testing-log.md`](docs/testing-log.md) | Append-only testing log (newest-first). Per the testing-log rule, every testing pass must be logged here |

### Source-adjacent

The `src/bundle/` subdirectory carries its own README — `src/bundle/README.md` — for surface-specific context that doesn't belong in the top-level docs.

## Hard rules

These apply every session and override defaults:

- **Autonomous feature builds** — any large build warranting a spec + plan runs spec → plan → subagent-execute → ship **fully autonomously**: NO approval gate at the spec or the plan, NO between-task check-ins. Self-review the spec; write the plan immediately; execute immediately; fix issues with judgment + prudence; final-pass; update docs + tests; run the gate; commit + push; `sov upgrade` + agent skills + cut a release when applicable. Founder-reserved/strategic decisions and destructive external actions still pause. Details: [`docs/conventions/autonomous-feature-builds.md`](docs/conventions/autonomous-feature-builds.md).
- **Subagent model policy** — Opus 4.7 default; Sonnet 4.6 only for trivially mechanical fully-specified tasks; **never Haiku**. Details: [`docs/conventions/subagent-policy.md`](docs/conventions/subagent-policy.md).
- **Pre-commit gate** — `bun run lint && bun run typecheck && bun run test`. All three. Details: [`docs/conventions/lint-and-commit.md`](docs/conventions/lint-and-commit.md).
- **Atomic commits + autonomous push** — one logical change per commit; push `origin/master` without asking. Same rule as the docs repo.
- **`sov upgrade` after runtime changes** — any `src/`, `bundle-default/`, or `packages/tui/` change. Details: [`docs/conventions/sov-upgrade.md`](docs/conventions/sov-upgrade.md).
- **Testing log obligation** — append to `docs/testing-log.md` for every testing pass. Details: [`docs/conventions/testing-log.md`](docs/conventions/testing-log.md).
- **No week-based estimates** — sessions / dispatches / wall-minutes only. Details: [`docs/conventions/estimation.md`](docs/conventions/estimation.md).
- **TUI style guide** — all spacing, padding, glyphs, brand colors, and typography in `packages/tui/` MUST reference `style.S.*` from `packages/tui/internal/style/`. Never hardcode layout values in components. Details: [`docs/conventions/tui-style-guide.md`](docs/conventions/tui-style-guide.md).
- **Plans and specs paths** — `docs/plans/YYYY-MM-DD-<feature>.md`, `docs/specs/YYYY-MM-DD-<topic>-design.md`. Never `docs/superpowers/`.
- **AGENTS.md ≡ CLAUDE.md** — byte-identical mirror. Verify with `diff` before commit.

## Don't

- Don't relitigate the 9 locked design principles ([`docs/design-principles.md`](docs/design-principles.md)).
- Don't put product-specific content under `src/` — Sovereign-AI-specific content belongs in the bundle.
- Don't delete empty `src/` subdirectories — they mark phase landing zones.
- Don't bypass the pre-commit gate with `--no-verify` unless you can name why.
- Don't dump content into `CLAUDE.md` — extend a conventions file and link to it.
